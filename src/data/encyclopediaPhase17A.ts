import type { EncyclopediaChapter } from '../domain/encyclopedia';

const testingStrategiesChapter: EncyclopediaChapter = {
  id: 'testing-strategies',
  title: 'Testing strategies for distributed systems',
  category: 'Architecture',
  summary: 'The testing pyramid, contract testing, property-based testing, and integration test trade-offs are not mechanical checklist items but a set of decisions about where to spend limited testing budget across system boundaries, and the highest-ROI investment at Staff level is usually reducing diagnosis time, not adding more tests.',
  problemStatement: 'A team\'s CI pipeline has 800 tests, takes 45 minutes, and is red 30-40% of the time on flaky failures unrelated to the change being merged, so engineers routinely re-run failing builds without reading why they failed, and the suite that was meant to give confidence now actively slows delivery while providing false signal.',
  interviewQuestion: 'Your team\'s CI pipeline has 800 tests, takes 45 minutes, and fails 40% of the time on flaky tests. How do you improve the testing strategy without just deleting tests?',
  coreTension: 'Deleting flaky tests fixes CI speed immediately but silently removes real coverage, while diagnosing each one properly is the only durable fix and is far slower than the deadline pressure that created the flaky suite in the first place will tolerate.',
  levelExpectations: {
    mid: 'Writes unit tests following the pyramid shape by convention. When a test is flaky, either re-runs it or deletes it to unblock the merge.',
    senior: 'Diagnoses flaky tests by root cause (shared state, async ordering, timing) before deciding to quarantine or fix. Introduces contract testing to reduce reliance on brittle E2E tests.',
    staff: 'Treats a red, flaky suite as a systems problem to diagnose, not tolerate or delete — tracks mean-time-to-diagnose as the metric that predicts whether the suite is actually helping, and shapes the pyramid ratio to the system\'s actual boundary stability rather than a fixed rule.',
  },
  coreConcepts: [
    'Testing pyramid — unit, integration, and end-to-end layers and their intended cost/confidence trade-off',
    'Test double taxonomy — mock, stub, fake, and spy, and when each is the correct choice',
    'Contract testing — consumer-driven contracts (Pact) verifying producer/consumer compatibility without full environments',
    'Property-based testing — generating inputs to verify invariants rather than hand-writing example cases',
    'Integration test scope and database strategy — real database, containerized database, or in-memory substitute',
    'Service virtualization for test isolation — stubbing external dependencies at the network boundary',
    'Test data management at scale — fixtures, factories, and data lifecycle across shared test environments',
    'End-to-end test fragility and maintenance cost as a function of UI and environment change rate',
    'Chaos and fault injection testing — verifying behavior under failure rather than only the happy path',
    'Production shadow testing — replaying live traffic against a new version without serving its response',
    'Shift-left testing philosophy — moving verification earlier in the development cycle',
    'Diagnosing flaky tests versus deleting them — distinguishing genuine non-determinism from environmental fragility',
  ],
  architectureDiagram:
    'flowchart TB\n  U[Unit tests — fast, isolated, cheap] --> I[Integration tests — real boundaries, moderate cost]\n  I --> C[Contract tests — producer/consumer compatibility]\n  C --> E[End-to-end tests — few, expensive, high confidence]\n  E --> P[Production shadow / canary verification]\n  P -->|feedback| U',
  solutionApproach: [
    'Classify every failing test in the 800-test suite as flaky (fails intermittently on unchanged code) versus legitimately broken, using CI history rather than intuition, before deciding what to fix or remove',
    'Quarantine confirmed-flaky tests into a non-blocking suite immediately so they stop causing false-negative re-run cycles, while a dedicated owner investigates root cause on a tracked ticket',
    'Diagnose flakiness root causes systematically — shared mutable test state, unordered async assertions, real network calls, or environment-dependent timing — since each has a different fix',
    'Introduce consumer-driven contract tests between services to catch breaking changes without needing full end-to-end environments for every integration point',
    'Right-size the pyramid to the system\'s actual boundary stability: a system with many stable, well-owned service boundaries benefits from more integration tests than the classic pyramid ratio suggests',
    'Replace brittle, broad end-to-end tests with a smaller number targeting only genuinely cross-cutting user journeys, moving the coverage those tests were approximating down into contract and integration tests',
    'Track mean time to diagnose test failure as an explicit metric alongside pass rate, since a red suite that is quickly and correctly diagnosed causes far less damage than a green suite hiding gamed or skipped assertions',
    'Add production shadow testing for the highest-risk change paths, verifying real traffic behavior before it ever reaches an end-to-end test rewrite cycle',
  ],
  designPatterns: [
    'Testing pyramid as a guideline — shaped by actual boundary stability, not applied as a fixed ratio',
    'Consumer-driven contract testing — Pact-style verification that a producer meets every consumer\'s expectations without deploying either',
    'Test double selection by fidelity — using the least powerful double (stub) that still exercises the behavior under test, reserving mocks for verifying interaction protocols',
    'Service virtualization — stubbing third-party or slow dependencies at the boundary to isolate test failures to the code under test',
    'Shadow traffic replay — validating a new version against real production requests without affecting real users',
  ],
  tradeoffs: [
    'Test suite speed versus coverage breadth: parallelizing and trimming the suite for a fast feedback loop can reduce the coverage surface, requiring compensating contract or shadow tests elsewhere',
    'Mocking versus real dependencies in integration tests: mocks make tests fast and deterministic but can pass against a mock that has drifted from the real dependency\'s actual behavior; real dependencies catch that drift at the cost of speed and flakiness',
    'End-to-end confidence versus maintenance cost: E2E tests give the highest behavioral confidence but are the most expensive to maintain and the most likely source of flaky, environment-dependent failures',
    'Quarantining flaky tests versus deleting them: quarantine preserves the original intent and coverage for eventual repair, but an unmanaged quarantine list silently rots into permanently-skipped, never-fixed tests',
  ],
  failureScenarios: [
    'Flaky test fatigue: engineers learn that re-running a failing build usually turns it green, so they stop reading failure output entirely, and a genuine regression eventually ships hidden among the noise of routine re-runs',
    'Mock drift: a service\'s integration tests mock a downstream API response shape that changed months ago in production; the mocked tests stay green while the real integration is silently broken',
    'End-to-end test rewrite spiral: a UI redesign breaks 200 brittle selector-based E2E tests simultaneously, consuming a sprint of engineering time on test maintenance that provides no new coverage over what contract tests would have given more cheaply',
    'Shared test database contamination: parallel test runs against a shared database corrupt each other\'s fixtures, producing failures that look like application bugs but are actually test infrastructure races',
    'Contract test skipped under deadline pressure: a team disables a failing consumer contract test to unblock a release, ships a breaking API change, and the downstream consumer fails in production days later',
  ],
  productionConsiderations: [
    'Instrument CI with per-test flakiness scores computed from historical pass/fail variance, surfaced automatically rather than relying on tribal memory of "that test is always flaky"',
    'Separate blocking (must-pass) suites from non-blocking (informational) suites explicitly in CI configuration, with an owner and SLA for promoting quarantined tests back to blocking',
    'Run contract tests as part of the deploy pipeline for every service boundary with more than one consumer, not only for externally-facing APIs',
    'Budget a fixed percentage of each sprint for test suite health (flake fixes, slow-test optimization) rather than treating it as work that happens only when the suite becomes unbearable',
    'Alert on rising mean-time-to-diagnose for test failures as a leading indicator of testing debt, before the lagging pass-rate metric degrades',
  ],
  staffDiscussion: [
    'The Staff-level move is treating a 45-minute, 40%-flaky suite as a systems problem to diagnose, not a suite to be silently tolerated or wholesale deleted — deleting tests to fix CI trades short-term relief for long-term regression risk',
    'Contract testing is the most underused tool in most organizations\' testing strategy: it catches the exact class of breaking change that end-to-end tests are usually (expensively) relied on to catch, at a fraction of the setup and runtime cost',
    'The testing pyramid is descriptive of a healthy equilibrium, not a law to enforce uniformly — a system with many stable, contract-tested service boundaries can and should carry more integration tests than the classic ratio suggests',
    'A test suite\'s value is a function of how quickly and correctly a failure can be diagnosed, not merely how many assertions it contains — a smaller suite with fast, clear failure attribution outperforms a larger suite that trains engineers to ignore red builds',
    'Shift-left is not free — pushing verification earlier only pays off when the earlier tests actually exercise the failure modes that would otherwise surface later; a unit test suite that passes while the system fails in integration is false shift-left confidence',
  ],
  relatedTopics: ['Progressive delivery and automated canary analysis', 'DORA metrics and engineering effectiveness', 'Chaos engineering', 'API and protocol selection'],
  realWorldSystems: ['Pact contract testing at Netflix and REA Group', 'Testcontainers for containerized integration testing', 'WireMock for service virtualization', 'Playwright and Cypress for end-to-end testing', 'Chaos Toolkit for fault injection'],
  followUpQuestions: [
    'How would you design a contract testing rollout across 30 microservices that currently rely entirely on end-to-end tests for integration confidence?',
    'A team wants to delete all flaky tests to make CI green immediately. What do you say, and what do you propose instead?',
    'How do you decide when an end-to-end test is worth its maintenance cost versus when it should be replaced by a combination of contract and integration tests?',
  ],
  cheatSheet: [
    'Testing pyramid: unit (many, fast) → integration (fewer, real boundaries) → E2E (few, expensive) — shape the ratio to actual boundary stability, not a fixed rule',
    'Contract testing (Pact-style) catches breaking changes between services without full E2E environments — the most underused high-ROI testing investment',
    'Diagnose flaky tests by root cause (shared state, async ordering, network calls, timing) before quarantining or deleting them',
    'Track mean time to diagnose test failure, not just pass rate, as the metric that predicts whether the suite is actually helping',
    'Use the least powerful test double (stub over mock) that still exercises the behavior under test',
  ],
  flashcards: [
    {
      question: 'Why is contract testing considered one of the highest-ROI testing investments for microservice architectures?',
      answer: 'Consumer-driven contract testing verifies that a producer service meets every consumer\'s expectations without requiring either side to be deployed or a full end-to-end environment to be running. It catches the specific class of breaking-API-change failures that teams otherwise rely on expensive, slow, and often flaky end-to-end tests to catch, at a fraction of the setup and runtime cost, and it fails fast and close to the actual boundary that broke.',
    },
    {
      question: 'A CI suite is red 40% of the time on flaky tests. What is the wrong fix and what is the right first step?',
      answer: 'The wrong fix is deleting the flaky tests outright, since that silently removes real coverage and risks shipping the regressions those tests existed to catch. The right first step is diagnosing each flaky test\'s root cause using CI history — shared mutable state, unordered async assertions, real network calls, or environment timing each require a different fix — then quarantining confirmed-flaky tests into a non-blocking suite with an owner and a repair SLA, rather than leaving them silently skipped or blocking every merge indefinitely.',
    },
  ],
  oneMinuteAnswer:
    'I start by separating genuinely flaky tests from legitimately broken ones using CI history rather than guessing, since treating both the same way leads to either deleting real coverage or ignoring real regressions. Confirmed-flaky tests get quarantined into a non-blocking suite immediately, with an owner and a repair SLA, so they stop causing false-negative re-run cycles without losing the coverage they represent. I diagnose root causes systematically — shared mutable state, unordered async assertions, real network calls, or timing dependencies each need a different fix. In parallel, I introduce consumer-driven contract tests between service boundaries, since that catches the exact class of breaking-change failure that teams often rely on expensive, brittle end-to-end tests to catch, at a fraction of the cost. I reshape the pyramid to the system\'s actual boundary stability rather than a fixed ratio — more integration tests where boundaries are stable and contract-tested, fewer sprawling E2E tests. Finally, I track mean time to diagnose test failure as an explicit metric alongside pass rate, since a suite that is red but quickly and correctly diagnosed is far healthier than a green suite that has quietly stopped meaning anything.',
};

const graphqlArchitectureChapter: EncyclopediaChapter = {
  id: 'graphql-architecture',
  title: 'GraphQL architecture at scale',
  category: 'Systems',
  summary: 'GraphQL solves over-fetching and under-fetching for flexible clients, but running it reliably in production requires solving the N+1 query problem with DataLoader, bounding query complexity against denial-of-service, and choosing federation deliberately when multiple teams own different parts of one schema.',
  problemStatement: 'A social commerce platform needs a single API surface for a feed, a product catalog, and a checkout flow, each owned by a different backend team with its own database, and a naive GraphQL gateway either becomes a monolithic schema no team can safely change or a set of stitched schemas that silently breaks when one team renames a field.',
  interviewQuestion: 'Design a GraphQL API gateway for a social commerce platform where the feed, product catalog, and checkout are owned by three different backend teams, each with their own database.',
  coreTension: 'GraphQL\'s flexibility to let clients request exactly the shape of data they need is precisely what makes query cost unpredictable at the server, turning every unbounded query into a potential denial-of-service vector that a fixed REST endpoint never had to defend against.',
  levelExpectations: {
    mid: 'Can design a single-service GraphQL schema and resolvers. Unaware of the N+1 query problem or query complexity limits.',
    senior: 'Implements DataLoader batching by default for list resolvers, enforces query depth/complexity limits at the gateway, and chooses federation over schema stitching for genuinely multi-team schemas.',
    staff: 'Treats DataLoader as a correctness requirement, not an optimization, and has an explicit answer for introspection and unbounded-query attack surfaces before launch, not after an incident. Frames entity ownership conflicts as a governance problem to resolve at design time.',
  },
  coreConcepts: [
    'Schema-first versus code-first design and their implications for cross-team schema ownership',
    'Resolver chain and execution model — how a single query fans out into a tree of resolver calls',
    'The N+1 query problem — a naive resolver issuing one database query per item in a list',
    'DataLoader — per-request batching and caching that collapses N+1 resolver calls into batched queries',
    'Persisted queries — pre-registering allowed queries to improve performance and close ad hoc query attack surface',
    'Depth and complexity limiting to prevent malicious or accidental denial-of-service queries',
    'Introspection in production — the trade-off between developer convenience and exposing the entire data model',
    'Subscriptions over WebSocket for real-time data, and their operational cost compared to query/mutation',
    'GraphQL federation — a gateway composing multiple subgraphs owned by different teams into one schema',
    'Schema stitching versus federation — merging schemas at the gateway versus each service declaring its ownership boundary',
    'Schema versioning and deprecation directives as the mechanism for evolving a shared schema without breaking clients',
    'Error handling under GraphQL\'s partial-success model — a response can contain both data and errors simultaneously',
  ],
  architectureDiagram:
    'flowchart TB\n  Client -->|single query| GW[Federated Gateway]\n  GW --> F[Feed Subgraph]\n  GW --> P[Product Catalog Subgraph]\n  GW --> C[Checkout Subgraph]\n  F --> FD[(Feed DB)]\n  P --> PD[(Catalog DB)]\n  C --> CD[(Checkout DB)]\n  GW -.->|query plan + complexity limit| GW',
  solutionApproach: [
    'Adopt GraphQL federation rather than schema stitching for the three-team platform, so the feed, catalog, and checkout teams each own and independently deploy their own subgraph schema behind a single composed gateway',
    'Define ownership boundaries at the entity level — a Product entity resolved primarily by the catalog subgraph but extended with fields (like inventory or price adjustments) contributed by other subgraphs via federation directives',
    'Require every resolver that fetches a list to use DataLoader-style batching by default, since without it a feed query rendering 50 items each fetching product details independently produces 50 separate database round trips',
    'Enforce query depth and complexity limits at the gateway before a query reaches any subgraph, rejecting queries whose estimated cost exceeds a budget rather than discovering the cost after execution',
    'Disable introspection in production for public-facing endpoints, or restrict it to authenticated internal tooling, since a fully introspectable schema hands an attacker a complete map of the data model',
    'Use persisted queries for the primary client applications, registering allowed query shapes ahead of deployment so the gateway can reject arbitrary ad hoc queries from untrusted clients',
    'Version schema changes with deprecation directives and a sunset window rather than breaking changes, since a shared federated schema has clients the platform team cannot always identify or coordinate with directly',
    'Design error handling so a failure in one subgraph (checkout is down) returns partial data for the rest of the query (feed and catalog still render) rather than failing the entire request',
  ],
  designPatterns: [
    'Apollo-style federation — independently owned subgraphs composed into one schema by a gateway, each team deploying on its own cadence',
    'DataLoader batching and per-request caching to eliminate N+1 resolver fan-out',
    'Persisted query allowlisting — trading ad hoc query flexibility for a bounded, pre-approved query surface',
    'Complexity-cost query analysis — assigning a cost to each field and rejecting queries exceeding a budget before execution',
    'Partial-success error model — returning a response with both a data payload and a scoped errors array rather than an all-or-nothing failure',
  ],
  tradeoffs: [
    'Federation versus schema stitching: federation gives each team true schema ownership and independent deployability at the cost of adopting a federation-aware gateway and entity-resolution conventions; stitching is simpler to start but couples schema evolution across teams at the gateway layer',
    'Persisted queries versus ad hoc query flexibility: persisted queries close a major DoS and introspection-abuse attack surface but require a build step to register queries, slowing rapid client iteration during early development',
    'DataLoader batching window versus request latency: batching resolver calls within a single event-loop tick reduces database load significantly, but incorrect batching scope (batching across requests instead of within one) introduces cross-request data leakage risk',
    'Introspection availability versus developer experience: disabling introspection in production closes reconnaissance for attackers but also removes a tool internal teams often rely on for debugging production issues',
  ],
  failureScenarios: [
    'N+1 without DataLoader: a feed query rendering 50 posts, each resolving its author and product tags independently, issues over 100 individual database queries for a single client request, saturating the database under moderate feed traffic',
    'Unbounded nested query DoS: a client (malicious or a misconfigured internal tool) sends a deeply nested query requesting every field\'s every relation recursively, and without depth limiting the gateway attempts to resolve an exponentially large response tree',
    'Introspection-driven attack reconnaissance: an attacker uses an open introspection endpoint to enumerate the entire schema, discovering an internal-only mutation that was never intended for public documentation but was still technically reachable',
    'Federation entity ownership conflict: two subgraphs both attempt to define the canonical Product entity\'s price field with different resolution logic, producing nondeterministic results depending on which subgraph the gateway\'s query planner favors',
    'Breaking schema change without deprecation window: the catalog team renames a field directly instead of deprecating it first, and a mobile client several release cycles behind fails silently because the field it depends on no longer resolves',
  ],
  productionConsiderations: [
    'Deploy query complexity and depth limits at the gateway as a hard gate, not a monitoring-only metric, so runaway queries are rejected before they reach any subgraph',
    'Monitor per-subgraph resolver latency and N+1 batching efficiency continuously, since a regression here degrades every query that touches the affected entity',
    'Require every schema change to go through a deprecation-and-sunset process with automated tooling that flags breaking changes against the federation composition before merge',
    'Log and alert on gateway-level partial-failure rates per subgraph, since a failing checkout subgraph silently degrading every composed query is an availability signal the individual subgraph\'s own health checks will not surface',
    'Restrict introspection to authenticated internal tooling in production, and rotate persisted query allowlists as part of the client release process',
  ],
  staffDiscussion: [
    'The decision between federation and schema stitching is the single most consequential architectural choice in a multi-team GraphQL platform — federation is worth its adoption cost specifically because it preserves independent team ownership and deployability, which stitching erodes as the schema grows',
    'DataLoader is not an optional performance optimization; it is a correctness requirement for any GraphQL API with non-trivial list resolution, and treating it as an afterthought is the single most common cause of GraphQL production incidents',
    'Introspection and unbounded query depth are the two attack surfaces unique to GraphQL that a REST API does not expose in the same way, and a Staff engineer designing a public GraphQL API is expected to have an explicit answer for both before launch, not after an incident',
    'Federation entity ownership conflicts are a governance problem before they are a technical one — the fix is establishing which subgraph owns which entity fields as an explicit convention reviewed at design time, not resolved implicitly by whichever subgraph the query planner happens to favor',
    'A GraphQL platform serving multiple client versions over time needs deprecation discipline treated with the same seriousness as a public REST API\'s versioning strategy — the flexibility GraphQL gives clients does not remove the platform team\'s obligation to avoid breaking them',
  ],
  relatedTopics: ['API and protocol selection', 'Load balancing and rate limiting', 'Microservices and service mesh', 'Cache stampede and caching strategy'],
  realWorldSystems: ['GitHub GraphQL API v4', 'Shopify GraphQL Admin API', 'Apollo Federation at Expedia and Netflix', 'Twitter API v2', 'Hasura and StepZen managed GraphQL gateways'],
  followUpQuestions: [
    'How would you migrate an existing schema-stitched GraphQL gateway to a federated architecture without a hard cutover?',
    'A client team reports that a specific query pattern is timing out under load. Walk through how you would diagnose whether the cause is missing DataLoader batching, an unbounded query, or a downstream subgraph slowdown.',
    'How do you design schema deprecation policy so client teams on different release cadences never experience a breaking change without warning?',
  ],
  cheatSheet: [
    'DataLoader batching is not optional — it is the fix for the N+1 problem that every non-trivial GraphQL resolver tree will otherwise hit',
    'Federation gives independent team schema ownership; schema stitching couples schema evolution at the gateway — choose federation for genuinely multi-team schemas',
    'Bound query depth and complexity at the gateway before execution, not after — this is the primary GraphQL-specific DoS vector',
    'Disable or restrict introspection in production for public endpoints — it hands an attacker a complete schema map',
    'Use deprecation directives and a sunset window for every schema change with external clients — GraphQL flexibility does not remove versioning discipline',
  ],
  flashcards: [
    {
      question: 'Why does a naive GraphQL resolver implementation almost always hit the N+1 query problem, and what fixes it?',
      answer: 'A GraphQL query resolving a list (for example, 50 feed posts) naturally results in the parent resolver returning the list, then a child resolver (like resolving each post\'s author) being invoked once per item — producing one database query per item instead of one batched query for all of them. DataLoader fixes this by batching and caching resolver calls within a single request tick, collapsing what would be N individual queries into a small number of batched queries, and it is considered a correctness requirement rather than an optional optimization for any GraphQL API with non-trivial list resolution.',
    },
    {
      question: 'When should a multi-team GraphQL platform choose federation over schema stitching?',
      answer: 'Federation should be chosen when multiple teams need true independent ownership and deployability of their part of the schema — each team owns a subgraph and deploys it on its own cadence, with the gateway composing them via defined entity-resolution conventions. Schema stitching merges schemas at the gateway layer instead, which is simpler to set up initially but couples schema evolution across teams as the platform grows, since a change in one stitched schema can silently affect the composed result in ways that are harder to attribute and test independently.',
    },
  ],
  oneMinuteAnswer:
    'I would federate the schema rather than stitch it, since federation gives the feed, catalog, and checkout teams independent ownership and deployment of their own subgraph while a gateway composes them into one API. I\'d define entity ownership explicitly at design time — Product primarily owned by catalog, extended by other subgraphs via federation directives — to avoid the ambiguous-ownership conflicts that occur when two subgraphs both try to resolve the same field. Every resolver touching a list must use DataLoader-style batching by default, since that is the fix for the N+1 problem that a feed rendering many items with independently-resolved authors or product details would otherwise hit immediately under load. At the gateway I\'d enforce query depth and complexity limits before any subgraph executes, since unbounded nested queries are the primary GraphQL-specific denial-of-service vector, and I\'d disable introspection for public clients to avoid handing attackers a full schema map. For the platform\'s main clients I\'d use persisted queries to bound the ad hoc query surface, and I\'d treat schema changes with deprecation directives and a sunset window, since GraphQL\'s flexibility for clients does not remove the platform team\'s obligation to avoid breaking them across release cycles.',
};

const infrastructureAsCodeChapter: EncyclopediaChapter = {
  id: 'infrastructure-as-code',
  title: 'Infrastructure as code at organizational scale',
  category: 'Architecture',
  summary: 'Terraform state is the most dangerous artifact in an infrastructure practice, modules are how IaC scales across teams without turning every change into a bottleneck review, and GitOps applies cleanly to Kubernetes workloads but needs more care when applied to higher-blast-radius cloud resources like networking and IAM.',
  problemStatement: 'A 200-module, 40-repository Terraform estate for a multi-region cloud deployment has turned the infrastructure team into a review bottleneck, since every change to any module requires their sign-off, and the organization needs a way to enable team self-service without losing governance over high-blast-radius resources.',
  interviewQuestion: 'Your team manages 200 Terraform modules across 40 repositories for a multi-region cloud deployment. The infra team is a bottleneck — every change requires their review. How do you restructure the IaC to enable self-service without losing governance?',
  coreTension: 'Removing the infra team as a review bottleneck requires decentralizing apply authority, but every decentralized change still risks touching the same shared, high-blast-radius resources that made central review necessary in the first place.',
  levelExpectations: {
    mid: 'Writes Terraform modules and applies changes through a standard review process. Treats state file management as an implementation detail handled by tooling.',
    senior: 'Classifies modules by blast radius, publishes versioned reusable modules with narrow contracts, and introduces policy-as-code gates to reduce reliance on manual review for low-risk changes.',
    staff: 'Reframes governance as tiered by blast radius rather than removed — reserves human review only for genuinely high-risk resources (IAM, VPC, DNS), and treats Terraform state with the same operational rigor as a production database, since losing it has comparable consequences.',
  },
  coreConcepts: [
    'Declarative versus imperative infrastructure as code and what each implies for drift detection',
    'Terraform HCL and the state file as the source of truth for what infrastructure actually exists',
    'Remote state storage and state locking to prevent concurrent-apply corruption',
    'Module design — versioned, reusable Terraform modules with clear input/output contracts',
    'Workspace isolation for separating environments (dev, staging, prod) with shared module code',
    'Pulumi and other imperative-IaC-in-general-purpose-language approaches as an alternative to HCL',
    'GitOps (ArgoCD, Flux) — reconciling a Git-declared desired state continuously against a running Kubernetes cluster',
    'Drift detection and remediation — identifying and correcting infrastructure that has diverged from its declared state',
    'Environment promotion pipelines — progressing a change from dev through staging to production',
    'Secrets management in IaC — keeping credentials out of state files and version control entirely',
    'Provider lock-in and multi-cloud portability trade-offs',
    'Policy-as-code and IaC testing (Checkov, Terratest) to catch violations before apply',
  ],
  architectureDiagram:
    'flowchart LR\n  Dev[Team writes module change] --> PR[Pull request]\n  PR --> Policy[Policy-as-code check]\n  Policy -->|pass| Plan[terraform plan — impact preview]\n  Plan -->|low blast radius| SelfApply[Self-service apply]\n  Plan -->|high blast radius: IAM/VPC/network| InfraReview[Infra team review gate]\n  SelfApply --> State[(Remote state, locked)]\n  InfraReview --> State',
  solutionApproach: [
    'Classify modules by blast radius — networking, IAM, and shared account-level resources are high blast radius; application-scoped resources (an S3 bucket for one service, a per-service IAM role within a pre-approved boundary) are low blast radius',
    'Delegate self-service apply authority to application teams for low-blast-radius modules, gated only by automated policy-as-code checks (Checkov or equivalent) rather than a human infra-team review',
    'Retain mandatory infra-team review only for high-blast-radius modules — shared VPCs, cross-account IAM roles, DNS zone changes — where a mistake affects multiple teams or the whole platform',
    'Publish versioned, well-documented modules with narrow, purpose-built input contracts for common patterns (a standard service deployment, a standard datastore) so teams consume proven modules instead of writing raw HCL for common needs',
    'Split the monolithic 40-repository, 200-module estate along team-ownership boundaries, with clear contracts (published module versions) between the infra team\'s foundational modules and each application team\'s consuming modules',
    'Introduce automated drift detection running on a schedule against remote state, alerting the owning team rather than only the infra team when reality has diverged from the declared configuration',
    'Require every apply to go through remote state with locking, regardless of self-service status, to prevent concurrent applies from corrupting state even as apply authority is decentralized',
    'Add a `terraform plan` impact preview step visible to the requesting team before any apply, so teams can self-assess blast radius and catch unintended changes before they reach the review gate at all',
  ],
  designPatterns: [
    'Blast-radius-tiered governance — automated gates for low-risk changes, human review reserved for high-risk changes',
    'Versioned module registry — infra team publishes reusable modules with semantic versioning, application teams consume specific pinned versions',
    'GitOps reconciliation loop — a controller continuously reconciles declared Git state against the running cluster rather than relying on one-shot apply',
    'Policy-as-code gating — Checkov/OPA-style automated checks enforced in CI before any apply, encoding governance rules as executable policy rather than manual checklist review',
    'State isolation per workspace/environment — separate state files per environment sharing the same module code to prevent cross-environment blast radius',
  ],
  tradeoffs: [
    'Self-service velocity versus governance risk: decentralizing apply authority for low-blast-radius modules unblocks application teams significantly, but requires investing upfront in policy-as-code maturity to replace the human judgment the infra team previously provided',
    'Module reusability versus flexibility: a highly reusable module with many configurable inputs is harder for consuming teams to use correctly than a narrow, opinionated one, but a narrow module may not fit every team\'s use case without a proliferation of near-duplicate modules',
    'GitOps for cloud resources versus for Kubernetes workloads: GitOps reconciliation is a natural fit for Kubernetes because a slightly-out-of-sync workload state is low risk and self-heals on the next reconcile; applying the same continuous-reconciliation pattern to IAM or VPC resources carries a higher blast radius per unintended reconciliation',
    'Remote state centralization versus team autonomy: a single shared remote state backend simplifies locking and governance but becomes a single point of failure and a review bottleneck if not paired with per-team state isolation',
  ],
  failureScenarios: [
    'Lost or corrupted state file: the Terraform state file for a production VPC is corrupted by a concurrent apply that bypassed locking, leaving the team unable to safely reconcile what infrastructure actually exists without a risky manual state import',
    'Overly broad module blast radius: a module intended for a single team\'s per-service IAM role is written with account-level permissions as a shortcut, and a self-service apply by one team inadvertently grants broader access than intended across the account',
    'Policy-as-code gap exploited under deadline pressure: a team\'s change passes automated policy checks because the specific misconfiguration (an overly permissive security group rule) wasn\'t yet covered by an existing policy rule, and it reaches production without the human review that would previously have caught it',
    'GitOps drift on a cloud resource with external side effects: an engineer manually patches a Kubernetes-adjacent cloud load balancer configuration during an incident, and the next GitOps reconciliation silently reverts the manual fix, reintroducing the original problem',
    'Secrets committed via state file: a module output containing a database password is stored in plaintext in the remote state backend, and an engineer with read access to state (but not the vault) can extract it',
  ],
  productionConsiderations: [
    'Store all remote state in a backend with encryption at rest and access logging, and audit who has read access to state files as carefully as who has apply access',
    'Run scheduled drift detection against all production state, alerting the owning team automatically rather than relying on the infra team to notice divergence',
    'Version every published module with semantic versioning and require consuming teams to pin versions explicitly, so a breaking module update never applies silently',
    'Enforce policy-as-code checks as a hard CI gate for every apply, self-service or reviewed, and treat policy rule gaps discovered in incidents as a required backlog item, not an afterthought',
    'Separate secrets management (a dedicated vault) from IaC entirely — no secret should ever be a plain Terraform output or variable that lands in state',
  ],
  staffDiscussion: [
    'The core Staff-level insight is that "every change needs infra review" is a governance model that scales linearly with change volume — the fix is not removing governance, it is tiering it by blast radius so human review is reserved for the changes that actually warrant it',
    'Terraform state is treated with far less operational care than it deserves in most organizations — it is functionally a database of what your infrastructure is, and losing or corrupting it has consequences comparable to losing a production database',
    'A module with too many input variables is nearly as unsafe as raw HCL, because it pushes the same decisions the module was meant to encapsulate back onto the consuming team — the discipline is in choosing narrow, opinionated module contracts for common patterns',
    'GitOps is the right pattern specifically where the cost of a slightly-stale reconciliation is low and self-correcting (Kubernetes workloads); applying it uncritically to account-level cloud resources without additional review gates trades one kind of risk for another',
    'Self-service infrastructure only works when policy-as-code has matured enough to encode the judgment a human reviewer used to provide — rolling out self-service before that maturity exists just moves the governance failures downstream and makes them harder to trace',
  ],
  relatedTopics: ['Platform engineering and developer experience', 'Kubernetes for engineers', 'Progressive delivery and automated canary analysis', 'Zero trust networking'],
  realWorldSystems: ['HashiCorp Terraform at Stripe and Shopify', 'Pulumi at AWS and Mercedes-Benz', 'ArgoCD for Kubernetes GitOps', 'Atlantis for Terraform GitOps automation', 'Checkov for infrastructure policy-as-code'],
  followUpQuestions: [
    'How would you retrofit policy-as-code coverage into an existing 200-module estate that currently relies entirely on human review?',
    'A GitOps reconciliation loop keeps reverting a manual emergency fix during an incident. How do you handle this in the moment, and how do you prevent it structurally afterward?',
    'How do you decide the boundary between what belongs in a shared foundational module versus what each application team should own independently?',
  ],
  cheatSheet: [
    'Terraform state is the source of truth for real infrastructure — protect it with remote storage, locking, encryption, and access auditing like a production database',
    'Tier governance by blast radius: automated policy-as-code gates for low-risk self-service changes, human review reserved for high-blast-radius resources (IAM, VPC, DNS)',
    'Narrow, versioned, opinionated modules scale better across teams than flexible modules with many inputs',
    'GitOps reconciliation fits Kubernetes workloads well (low-risk, self-healing); apply more cautiously to account-level cloud resources',
    'Secrets never belong in Terraform state or variables — use a dedicated vault and reference secrets, never store them',
  ],
  flashcards: [
    {
      question: 'Why does tiering IaC governance by blast radius solve the "infra team is a bottleneck" problem better than simply removing review?',
      answer: 'Removing review entirely trades a bottleneck for ungoverned risk, since every Terraform change — however small — could still touch shared, high-blast-radius resources like IAM or networking. Tiering governance by blast radius keeps human review only where a mistake genuinely affects multiple teams or the whole platform, while letting application teams self-serve low-blast-radius changes (their own service-scoped resources) through automated policy-as-code checks, which scales review capacity without removing governance where it actually matters.',
    },
    {
      question: 'Why is a module with many configurable input variables often not actually more useful than raw Terraform HCL?',
      answer: 'A module\'s value comes from encapsulating a decision so the consuming team does not have to make it themselves. A module with too many inputs pushes those same decisions back onto the consumer, who now has to understand the module\'s internals well enough to choose the right combination of inputs correctly — which is close to the cognitive load of writing the HCL directly, while adding an extra layer of indirection to debug when something goes wrong.',
    },
  ],
  oneMinuteAnswer:
    'I would restructure governance around blast radius rather than removing review outright. Modules that touch shared, account-wide resources — VPCs, cross-account IAM, DNS — keep mandatory infra-team review, since a mistake there affects multiple teams. Modules scoped to a single team\'s own resources, like a service-specific datastore or IAM role within a pre-approved boundary, move to self-service, gated by automated policy-as-code checks (Checkov or equivalent) instead of a human reviewer. I\'d publish versioned, narrow, opinionated modules for common patterns so teams consume proven building blocks instead of writing raw HCL, since a module with too many configurable inputs just pushes the same decisions back onto the consumer. Every apply, self-service or reviewed, still goes through remote state with locking to prevent concurrent-apply corruption, since state is effectively a database of what infrastructure actually exists and losing it is a severe incident. I\'d add scheduled drift detection alerting the owning team directly, and I\'d only extend GitOps-style continuous reconciliation to resources where a stale state is low-risk and self-healing, being more conservative about applying it to higher-blast-radius cloud resources like IAM.',
};

const domainDrivenDesignChapter: EncyclopediaChapter = {
  id: 'domain-driven-design',
  title: 'Domain-driven design patterns',
  category: 'Architecture',
  summary: 'The aggregate boundary is the single most consequential modeling decision in domain-driven design, since it defines the transactional consistency boundary and crossing it means accepting eventual consistency, and ubiquitous language is a living shared vocabulary between engineers and domain experts that must be actively maintained, not a one-time naming exercise.',
  problemStatement: 'An e-commerce platform\'s order management domain has grown organically into a tangle of services that all reach directly into each other\'s data, with no clear boundary for where an order\'s consistency guarantees begin and end, making it unclear which service owns the authoritative rules for order state transitions and where eventual consistency is acceptable.',
  interviewQuestion: 'Design the order management domain for an e-commerce platform using DDD. Identify the bounded contexts, the key aggregates in each context, and how they communicate across boundaries.',
  coreTension: 'The aggregate boundary defines what "atomic" means in the system, so drawing it too large creates contention across unrelated concerns while drawing it too small forces unnecessary cross-aggregate coordination — there is no boundary choice that avoids one cost or the other.',
  levelExpectations: {
    mid: 'Can identify entities and value objects in a domain model. Treats aggregate boundaries as an implementation detail rather than a deliberate consistency decision.',
    senior: 'Draws aggregate boundaries around genuine transactional invariants, communicates across bounded contexts via integration events rather than direct calls, and uses sagas for cross-aggregate workflows.',
    staff: 'Treats the aggregate boundary decision as the single most consequential modeling choice with lasting architectural consequences, and pushes back on applying tactical DDD patterns to simple CRUD subdomains as much as on under-modeling a genuinely complex one. Maintains ubiquitous language as an ongoing obligation, not a one-time exercise.',
  },
  coreConcepts: [
    'Bounded context — the fundamental DDD unit within which a model and its language are self-consistent',
    'Ubiquitous language — a shared vocabulary maintained actively between engineers and domain experts within a context',
    'Aggregate root and aggregate boundary rules — the entity that governs consistency for everything inside its boundary',
    'Domain event versus integration event — an event meaningful within a bounded context versus one published for other contexts to consume',
    'Value object versus entity — objects defined by their attributes versus objects defined by continuous identity',
    'Repository pattern for aggregate persistence — loading and saving a whole aggregate as a consistency unit',
    'Anti-corruption layer (ACL) — a translation layer insulating a bounded context from a legacy or external model',
    'Context map — the relationships between bounded contexts (shared kernel, customer-supplier, conformist, partnership)',
    'Event storming as a discovery technique for surfacing domain events, aggregates, and context boundaries collaboratively',
    'Tactical versus strategic DDD — the fine-grained patterns (aggregates, entities) versus the large-scale context boundaries',
    'Anemic domain model anti-pattern — data structures with no behavior, pushing all logic into external services',
    'Eventual consistency across aggregate boundaries — accepting that a workflow spanning aggregates cannot be a single transaction',
  ],
  architectureDiagram:
    'flowchart TB\n  subgraph Ordering[Ordering bounded context]\n    OA[Order aggregate] -->|domain event: OrderPlaced| OI[Order Item value objects]\n  end\n  subgraph Inventory[Inventory bounded context]\n    IA[Stock Reservation aggregate]\n  end\n  subgraph Payment[Payment bounded context]\n    PA[Payment aggregate]\n  end\n  Ordering -->|integration event: OrderPlaced| Inventory\n  Ordering -->|integration event: OrderPlaced| Payment\n  Inventory -->|integration event: StockReserved / StockUnavailable| Ordering\n  Payment -->|integration event: PaymentCaptured / PaymentFailed| Ordering',
  solutionApproach: [
    'Run an event storming session with domain experts to surface the actual business events (OrderPlaced, StockReserved, PaymentCaptured, OrderShipped) before designing any code structure, since the events reveal the natural aggregate and context boundaries',
    'Establish separate bounded contexts for Ordering, Inventory, and Payment, each with its own ubiquitous language — "reservation" means something specific and different in Inventory than a casual reading of "order" would suggest',
    'Model the Order aggregate as the consistency boundary for order-level invariants (an order\'s line items and total must be internally consistent) but explicitly not for cross-context invariants like stock availability, which belongs to the Inventory aggregate',
    'Keep aggregates small — the Order aggregate holds order-level state and line items, not the full inventory or payment state, since pulling those in would force an unnecessarily large transaction scope and create contention across concurrent orders',
    'Communicate across bounded contexts exclusively through integration events (OrderPlaced published by Ordering, consumed by Inventory and Payment) rather than direct synchronous calls into another context\'s internal model',
    'Accept eventual consistency explicitly for the cross-aggregate order fulfillment workflow — an order is placed immediately, then asynchronously confirmed or rejected as inventory and payment integration events arrive, using a saga to coordinate the outcome',
    'Introduce an anti-corruption layer between the new Ordering context and any legacy order-processing system it must still integrate with, translating the legacy model\'s concepts into the new context\'s ubiquitous language rather than letting legacy concepts leak in',
    'Document the context map explicitly — Ordering and Inventory as a customer-supplier relationship (Ordering depends on Inventory\'s stock data), avoiding an implicit shared-kernel coupling that would tie their release cycles together',
  ],
  designPatterns: [
    'Aggregate as transactional consistency boundary — everything inside one aggregate is saved atomically; everything outside requires eventual consistency and explicit coordination',
    'Domain events published from within an aggregate, translated to integration events at the context boundary for cross-context consumption',
    'Anti-corruption layer — isolating a bounded context\'s clean model from a legacy or external system\'s incompatible model',
    'Saga pattern for cross-aggregate workflows — coordinating a multi-step business process (place order → reserve stock → capture payment) that spans aggregate and context boundaries without a single distributed transaction',
    'Context mapping patterns — shared kernel, customer-supplier, conformist, and partnership as named relationship types between bounded contexts',
  ],
  tradeoffs: [
    'Aggregate size versus transactional convenience: a larger aggregate makes more invariants trivially enforceable within one transaction, but increases contention and couples unrelated concerns into one consistency boundary; a smaller aggregate reduces contention but pushes more coordination into eventual-consistency workflows',
    'Strategic DDD investment versus domain complexity: the cost of running event storming, defining bounded contexts, and maintaining a context map is worth it for a genuinely complex domain with many stakeholders, but is overhead for a small CRUD-shaped subdomain that doesn\'t warrant tactical DDD patterns at all',
    'Anti-corruption layer versus integration simplicity: an ACL protects a new bounded context\'s model integrity from a legacy system\'s leaking concepts, at the cost of an additional translation layer to build and maintain',
    'Eventual consistency versus workflow simplicity: accepting eventual consistency across aggregates enables independent scaling and deployment of each bounded context, but requires the business and engineering teams to accept and design for a window where the system is in an intermediate, not-yet-reconciled state',
  ],
  failureScenarios: [
    'Aggregate boundary drawn too large: the Order aggregate is modeled to include the full inventory reservation state directly, so placing any order requires locking inventory records, and concurrent order placement for popular items becomes a contention bottleneck',
    'Aggregate boundary drawn too small: order line items are modeled as separate aggregates from the order itself, so adding an item and updating the order total cannot happen in one transaction, forcing an awkward saga for what should be a single atomic operation',
    'Ubiquitous language drift: the Ordering team starts calling a customer\'s modified order a "revision" while the Inventory team\'s integration event still uses "amendment" for the same concept, and a new engineer implementing the integration silently assumes they are different events',
    'Anemic domain model: the Order aggregate is reduced to a plain data structure with getters and setters, and all business logic (validating state transitions, enforcing invariants) lives in a separate OrderService class, making it easy to bypass the aggregate\'s own consistency rules by mutating its data directly',
    'Missing anti-corruption layer: the new Ordering bounded context directly reuses the legacy system\'s order status enum, and when the legacy system introduces a new status value with different semantics, it silently breaks assumptions baked into the new context\'s business logic',
  ],
  productionConsiderations: [
    'Version integration events explicitly and treat their schema with the same discipline as a public API, since multiple bounded contexts consume them independently and cannot all be updated in lockstep',
    'Monitor saga completion rates and time-to-completion for cross-aggregate workflows, since a stuck saga (order placed but stock reservation never confirmed) represents a business-visible failure, not just a technical one',
    'Re-validate the context map periodically with domain experts as the business evolves — a context boundary that was correct at launch can become wrong as the business adds new order types or fulfillment models',
    'Enforce aggregate boundary discipline in code review, specifically watching for a repository or service reaching into another aggregate\'s internal state directly instead of going through its root',
    'Keep the anti-corruption layer\'s translation logic covered by tests pinned to the legacy system\'s actual behavior, since it is the single point protecting the new model from unnoticed upstream changes',
  ],
  staffDiscussion: [
    'The aggregate boundary decision is where most DDD implementations succeed or fail — it is not a stylistic choice but the definition of what "atomic" means in the system, and getting it wrong in either direction (too large causes contention, too small forces unnecessary sagas) has lasting architectural consequences',
    'Ubiquitous language is frequently treated as a one-time naming exercise during initial modeling, when it is actually an ongoing maintenance obligation — language drift between a bounded context\'s internal team and its integration consumers is a leading indicator of a context boundary that has started to blur',
    'Event storming earns its adoption cost specifically because it surfaces aggregate and context boundaries from the actual business events domain experts describe, rather than from an engineering team\'s guess at how the data should be normalized',
    'The anemic domain model anti-pattern is not primarily an aesthetic complaint — it is a correctness risk, because business rules that live outside the aggregate can be bypassed by any code path that mutates the aggregate\'s state directly without going through its enforced invariants',
    'DDD\'s tactical patterns (aggregates, value objects, repositories) are worth the investment for a genuinely complex core domain; applying them uniformly to every simple CRUD subdomain in a system is over-engineering that a Staff engineer should push back on as much as under-modeling a genuinely complex domain',
  ],
  relatedTopics: ['CQRS and event sourcing', 'Distributed transactions and sagas', 'Microservices and service mesh', 'Technical strategy and decision-making'],
  realWorldSystems: ['Axon Framework for DDD and event sourcing in banking systems', 'Microsoft eShopOnContainers reference DDD architecture', 'Shopify\'s modular monolith built on DDD principles', 'Vaughn Vernon\'s "Implementing Domain-Driven Design" reference examples'],
  followUpQuestions: [
    'How would you detect that an aggregate boundary has been drawn incorrectly after the system is already in production with real traffic?',
    'A saga coordinating order placement across Ordering, Inventory, and Payment gets stuck with stock reserved but payment never confirmed. How do you design compensation for this?',
    'How do you decide whether a subdomain deserves full tactical DDD treatment versus a simpler CRUD-style implementation?',
  ],
  cheatSheet: [
    'Aggregate = transactional consistency boundary — size it to the actual invariants that must be atomic, not to convenience',
    'Bounded context = a self-consistent model and ubiquitous language; cross-context communication happens via integration events, never direct model access',
    'Domain event (internal) vs integration event (published across context boundaries) — version integration events like a public API',
    'Anti-corruption layer isolates a bounded context from a legacy or external model — the translation point, not a shortcut',
    'Event storming surfaces real aggregate and context boundaries from actual business events, faster than guessing at data normalization',
  ],
  flashcards: [
    {
      question: 'Why is the aggregate boundary considered the most consequential modeling decision in DDD?',
      answer: 'The aggregate boundary defines the transactional consistency guarantee in the system — everything inside one aggregate is saved atomically, and everything outside it requires eventual consistency and explicit coordination (typically a saga). Drawing the boundary too large forces unrelated concerns into one transaction and creates contention under concurrent load; drawing it too small forces unnecessary cross-aggregate coordination for what should be a single atomic operation. Both mistakes have lasting architectural consequences that are expensive to unwind once other services depend on the resulting behavior.',
    },
    {
      question: 'What is the difference between a domain event and an integration event, and why does that distinction matter?',
      answer: 'A domain event is meaningful within a single bounded context and can carry that context\'s full internal model detail. An integration event is explicitly published for other bounded contexts to consume, and should be treated with the same versioning and schema discipline as a public API, since multiple independent contexts consume it and cannot all be updated in lockstep. Conflating the two — publishing a context\'s internal domain event directly as the integration contract — creates tight coupling to internal implementation details that makes the context much harder to evolve independently.',
    },
  ],
  oneMinuteAnswer:
    'I would start with event storming alongside domain experts to surface the real business events — OrderPlaced, StockReserved, PaymentCaptured — since those events reveal the natural aggregate and context boundaries better than guessing at data normalization. I\'d establish separate bounded contexts for Ordering, Inventory, and Payment, each with its own ubiquitous language, and keep the Order aggregate scoped to order-level invariants (line items, total) rather than pulling in inventory or payment state, which would create unnecessary contention. Cross-context communication happens exclusively through integration events, not direct calls into another context\'s model, and I\'d treat those integration events with the same versioning discipline as a public API since multiple contexts consume them independently. The order fulfillment workflow spans aggregates and contexts, so I\'d coordinate it with a saga and design explicitly for eventual consistency — an order is placed immediately, then confirmed or rejected asynchronously as inventory and payment events arrive. Where the new Ordering context has to integrate with a legacy system, I\'d add an anti-corruption layer to keep legacy concepts from leaking into the new model\'s ubiquitous language.',
};

export const phase17AChapters: EncyclopediaChapter[] = [
  testingStrategiesChapter,
  graphqlArchitectureChapter,
  infrastructureAsCodeChapter,
  domainDrivenDesignChapter,
];
