import type { EncyclopediaChapter } from '../domain/encyclopedia';

const cryptographyPrimitivesChapter: EncyclopediaChapter = {
  id: 'cryptography-primitives',
  title: 'Cryptography primitives for engineers',
  category: 'Architecture',
  summary: 'Choose the right cryptographic primitive for the job, understand envelope encryption and key hierarchies, and own the secrets lifecycle and supply chain security posture expected of Staff engineers.',
  problemStatement: 'Engineers reach for cryptography without a mental model of which primitive solves which threat, producing systems that are cryptographically correct in isolation but insecure in composition: unauthenticated encryption, custom key derivation, or a signing key with no rotation plan.',
  interviewQuestion: 'A security audit finds that your service stores user passwords using MD5. Design the migration to a secure scheme without forcing all users to reset their passwords simultaneously.',
  coreConcepts: [
    'Symmetric encryption (AES-GCM, AES-256) versus asymmetric (RSA, elliptic curve)',
    'Hash functions (SHA-256, SHA-3) and collision resistance',
    'HMAC for message authentication versus plain hashing',
    'Digital signatures (RSA, ECDSA, Ed25519) and certificate chains',
    'TLS 1.3 handshake and forward secrecy via ECDHE',
    'Key derivation functions: PBKDF2, bcrypt, Argon2',
    'Envelope encryption key hierarchy: DEK and KEK',
    'Hardware security modules (HSM) for key storage',
    'Certificate transparency logs',
    'Supply chain security: SBOM, artifact signing, Sigstore and Cosign',
    'Secrets lifecycle: generation, distribution, rotation, revocation, audit',
    'Common cryptographic mistakes: ECB mode, custom PRNGs, MD5 or SHA-1 for security',
  ],
  architectureDiagram:
    'flowchart TD\n  D[Application data] --> DEK{Data Encryption Key}\n  DEK --> ENC[AES-GCM encrypt data]\n  DEK --> WRAP[Wrap DEK with KEK]\n  KMS[(KMS / HSM holds KEK)] --> WRAP\n  WRAP --> STORE[(Store: ciphertext + wrapped DEK)]\n  ROT[Key rotation] -->|re-wrap DEK only| WRAP\n  SIGN[Artifact build] --> SBOM[Generate SBOM]\n  SBOM --> COSIGN[Sign with Sigstore/Cosign]\n  COSIGN --> VERIFY[Consumer verifies signature + transparency log]',
  solutionApproach: [
    'Identify the threat being addressed before choosing a primitive: confidentiality needs encryption, integrity needs a MAC or signature, authenticity needs a signature or HMAC with a shared secret',
    'Default to AES-GCM for symmetric encryption — it is authenticated, meaning tampering is detected, unlike AES-CBC or AES-ECB which provide confidentiality alone',
    'Use envelope encryption for any data encrypted at rest: generate a per-record or per-tenant Data Encryption Key (DEK), encrypt the data with it, then wrap the DEK with a Key Encryption Key (KEK) held in a KMS or HSM',
    'Never derive password hashes with a general-purpose hash function; use a deliberately slow, memory-hard KDF such as Argon2 or bcrypt so brute-force attacks remain expensive even after a database breach',
    'Terminate TLS with TLS 1.3 wherever possible, since it mandates forward secrecy (ECDHE) so a compromised private key later cannot decrypt previously captured traffic',
    'For any signed artifact or release, generate a Software Bill of Materials and sign the artifact with Sigstore or an equivalent keyless-or-KMS-backed signer, publishing the signature to a transparency log consumers can verify against',
    'Design the secrets lifecycle explicitly: automate generation, define a maximum age before forced rotation, provide immediate revocation, and log every access for audit — treat unrotatable secrets as a production defect',
    'Review any place a bespoke cryptographic scheme has been built in-house and replace it with a vetted library primitive; the failure mode is almost always in composition, not the underlying math',
  ],
  designPatterns: [
    'Envelope encryption — DEK per data unit, KEK per tenant or system, enabling cheap re-keying by re-wrapping only the DEK',
    'Key rotation without re-encryption — rotate the KEK and re-wrap DEKs in the background without touching ciphertext',
    'Signed artifact supply chain — SBOM generation, keyless signing, and transparency log verification at deploy time',
    'Password hashing with a memory-hard KDF and per-user salt, never a fast general-purpose hash',
    'Mutual TLS for service-to-service authentication, layered on top of network-level trust boundaries',
  ],
  tradeoffs: [
    'HSM-backed keys versus operational flexibility: hardware-backed keys cannot be exported for backup or migrated casually, trading convenience for a much stronger guarantee against key exfiltration',
    'Argon2 or bcrypt cost factor versus login latency: a higher cost factor increases attacker cost per guess but also increases legitimate login latency and server CPU load',
    'Keyless signing (Sigstore) versus long-lived signing keys: keyless signing removes key management burden but depends on an external transparency log and identity provider being available',
    'Per-record DEKs versus per-tenant DEKs: finer-grained keys shrink the blast radius of a single key compromise but multiply the number of keys the KMS must manage and audit',
    'Certificate rotation frequency versus operational risk: short-lived certificates limit the exposure window of a compromised key but require automation robust enough that a renewal failure does not cause an outage',
  ],
  failureScenarios: [
    'A team encrypts data with AES in ECB mode because it is the default in an old library; identical plaintext blocks produce identical ciphertext blocks, leaking structural information about the encrypted data',
    'Passwords are hashed with unsalted SHA-256; a rainbow table attack recovers a large fraction of passwords within hours of a database leak',
    'A signing key for software updates is stored unencrypted on a build server; a compromised CI pipeline signs a malicious update that clients trust because the signature is technically valid',
    'A KMS key is deleted without a grace period as part of a cleanup script, permanently destroying every DEK it had wrapped and making the encrypted data unrecoverable',
    'A custom random number generator is used to create session tokens instead of a cryptographically secure PRNG, allowing an attacker to predict future tokens after observing several',
    'TLS certificates expire silently because renewal automation failed weeks earlier with no alert, causing an outage the moment the certificate lapses',
    'An HMAC secret is reused across environments (staging and production), so a leak in the lower-security staging environment compromises production message authentication',
  ],
  productionConsiderations: [
    'Alert on certificate and key expiry at least 30 days in advance, and again at 7 days, with an owner assigned to every certificate',
    'Enforce a KMS deletion grace period (7-30 days) on all keys so an accidental delete does not immediately destroy encrypted data',
    'Audit every KMS decrypt and sign operation with the calling identity, and alert on unusual volume or access from an unexpected service',
    'Run automated static analysis for known-dangerous patterns: ECB mode, MD5/SHA-1 for security purposes, hardcoded secrets in source, disabled certificate validation',
    'Publish an SBOM for every release and verify artifact signatures at deploy time as a hard gate, not an advisory check',
    'Rotate the KEK on a fixed schedule (annually at minimum, or after any suspected exposure) and verify the re-wrap job completes and is auditable',
  ],
  staffDiscussion: [
    'Staff engineers are not expected to design new cryptographic algorithms; they are expected to compose vetted primitives correctly and to recognize when a proposed design reinvents something a library already solves safely',
    'The most consequential cryptography decision at Staff level is usually organizational: who owns key rotation, what the incident response is for a suspected key compromise, and whether the team can actually execute an emergency re-key under pressure',
    'Supply chain security (SBOM, signed artifacts) moved from optional to expected after SolarWinds and Log4Shell; a Staff engineer designing a build pipeline in 2024 onward is expected to have an opinion on artifact provenance, not just functional correctness',
    'Envelope encryption is worth defending in design reviews even when it looks like unnecessary complexity for a small system, because retrofitting it after a system has grown and after data already exists unencrypted is materially more expensive than building it in from the start',
    'When a security review flags an outdated primitive (MD5 passwords, RC4, self-signed internal certificates with no rotation), the fix requires a migration plan that preserves availability — the interesting engineering problem is rarely the new algorithm, it is the transition',
  ],
  relatedTopics: ['Security and multi-tenancy', 'Secrets management', 'Database selection and sharding', 'Progressive delivery and automated canary analysis', 'Distributed locks and leader election in practice'],
  realWorldSystems: ['AWS KMS envelope encryption', 'GCP Cloud KMS', 'HashiCorp Vault', 'Let\'s Encrypt automated certificate issuance', 'Sigstore and Cosign for artifact signing', 'GitHub artifact attestation'],
  followUpQuestions: [
    'Why is AES-GCM generally preferred over AES-CBC for new systems, and what does GCM give you that CBC does not?',
    'How would you design a zero-downtime rotation of the master key encrypting millions of existing data-encryption keys?',
    'What changes about your secrets architecture once a service must support bring-your-own-key (BYOK) customers?',
  ],
  cheatSheet: [
    'AES-GCM for symmetric encryption (authenticated); never AES-ECB',
    'Argon2 or bcrypt for passwords, never a fast general-purpose hash',
    'Envelope encryption: DEK encrypts data, KEK wraps DEK — rotate by re-wrapping only',
    'TLS 1.3 mandates forward secrecy via ECDHE',
    'Sign artifacts and publish an SBOM; verify signatures as a deploy-time gate, not advisory',
  ],
  flashcards: [
    {
      question: 'What does envelope encryption buy you that direct encryption with a single master key does not?',
      answer: 'Envelope encryption lets you rotate the master key (KEK) by re-encrypting only the small data-encryption keys (DEKs) it wrapped, not the underlying data itself. With a single master key encrypting data directly, rotation requires re-encrypting every byte of protected data, which is often infeasible at scale.',
    },
    {
      question: 'Why is a fast hash function like SHA-256 unsuitable for password storage even though it is cryptographically secure?',
      answer: 'SHA-256 is designed to be fast, which is exactly the wrong property for password hashing: an attacker with a leaked hash database can compute billions of guesses per second on commodity hardware. Password hashing needs a deliberately slow, memory-hard function like Argon2 or bcrypt, which makes brute-force guessing computationally expensive even after a breach.',
    },
  ],
  oneMinuteAnswer:
    'I migrate the password scheme in three phases to avoid forcing a mass reset. First, I add a new column for the secure hash and keep verifying against the old MD5 hash during login; when a user logs in successfully, I re-hash their password with Argon2 using their plaintext password (which I have at that moment) and populate the new column. Second, for users who have not logged in during a grace period, I send a password reset requirement rather than silently continuing to trust an insecure hash indefinitely. Third, once the old hash column\'s legitimate use has dropped to near zero, I remove it entirely. Throughout the migration, I treat the transition itself as the interesting engineering problem: the new algorithm choice (Argon2 with per-user salt) is straightforward, but doing this without an availability incident or a support-desk flood from forced resets requires careful sequencing, monitoring of the opportunistic re-hash rate, and a clear deadline for the forced-reset cohort.',
};

const changeDataCaptureChapter: EncyclopediaChapter = {
  id: 'change-data-capture',
  title: 'Change data capture',
  category: 'Data',
  summary: 'Stream a reliable, ordered feed of database changes from the transaction log for real-time replication, cache invalidation, and event-driven integration, without polling or modifying application code.',
  problemStatement: 'Teams bolt on cache invalidation and event publishing inside application code, and every write path that forgets to call the invalidation or publish step silently corrupts downstream consistency, while polling-based change detection adds latency and load that log-based capture avoids entirely.',
  interviewQuestion: 'Design a real-time cache invalidation system for a product catalog with 10 million SKUs, where the cache must reflect database changes within 500ms without any application-level cache invalidation code.',
  coreConcepts: [
    'Log-based CDC versus query-based polling versus trigger-based capture',
    'The database transaction log: WAL in Postgres, binlog in MySQL, redo log in Oracle',
    'Debezium architecture: connector reads the log, publishes to a Kafka topic',
    'Exactly-once delivery and idempotent consumers for CDC events',
    'Schema evolution and schema registry for CDC event compatibility',
    'Initial snapshot and consistency guarantee at connector startup',
    'Backfill versus incremental CDC',
    'CDC-driven cache invalidation instead of write-through caching',
    'Event ordering within a single table versus across tables',
    'Tombstone events representing deletes',
    'CDC lag and freshness SLAs',
    'Securing CDC pipelines: sensitive column exclusion and PII redaction',
  ],
  architectureDiagram:
    'flowchart LR\n  DB[(Source DB)] --> WAL[Transaction log / WAL]\n  WAL --> CONN[CDC connector — e.g. Debezium]\n  CONN --> SNAP{Initial snapshot done?}\n  SNAP -->|no| SS[Consistent snapshot read]\n  SNAP -->|yes| TOPIC[Kafka topic: ordered change events]\n  SS --> TOPIC\n  TOPIC --> SR[Schema registry validates event schema]\n  TOPIC --> C1[Cache invalidator]\n  TOPIC --> C2[Downstream event consumers]\n  TOPIC --> C3[Audit log sink]',
  solutionApproach: [
    'Read changes directly from the database transaction log (WAL, binlog, or redo log) rather than polling tables or relying on application-level triggers, since the log already captures every committed change in order at no extra write cost',
    'Deploy a log-based CDC connector (Debezium is the standard) that tails the log and publishes one event per row change to a partitioned, ordered topic, keyed by primary key so all changes to a given row stay in order',
    'Handle connector startup correctly: take a consistent snapshot of existing table state at a known log position, then seamlessly continue with log-tailing from that position so no change is missed or duplicated',
    'Publish events through a schema registry with enforced backward compatibility, since an unexpected column type change on the source table is the most common cause of a silently broken pipeline',
    'Model deletes as explicit tombstone events (not merely absence), so downstream consumers such as cache invalidators can distinguish "never existed" from "was deleted"',
    'Use the CDC stream to drive cache invalidation directly: a consumer subscribes to the topic and evicts or updates the corresponding cache key, eliminating the need for every application code path to remember to invalidate',
    'Redact or exclude sensitive columns at the connector level before events reach the broker, rather than relying on every downstream consumer to filter them correctly',
    'Monitor CDC lag as a first-class SLA metric — the gap between a committed transaction and its corresponding event reaching consumers — and alert before it threatens freshness guarantees made to downstream systems',
  ],
  designPatterns: [
    'Log-based CDC via a dedicated connector reading the transaction log directly',
    'Outbox-adjacent pattern: CDC on an explicit outbox table when you want events decoupled from the raw schema',
    'Tombstone events to represent deletes explicitly in a log-compacted topic',
    'Schema registry with enforced compatibility mode gating any producer schema change',
    'Cache invalidation via subscription rather than write-path instrumentation',
  ],
  tradeoffs: [
    'Log-based CDC versus trigger-based capture: log-based imposes no extra write-path cost and captures every change, but requires access to database internals (replication slots, binlog permissions) that some managed databases restrict',
    'Strict per-row ordering versus cross-table consistency: CDC guarantees order within a single table\'s partition but generally does not guarantee a consistent snapshot across multiple tables changed in the same transaction',
    'Snapshot-then-stream startup versus operational simplicity: a consistent initial snapshot is essential for correctness but adds meaningful operational complexity and a slow-start period for large tables',
    'Redacting sensitive columns at the source versus flexibility: excluding a column at the connector protects it universally but means any legitimate future consumer needing that column requires a connector reconfiguration',
    'CDC lag tolerance versus infrastructure cost: tighter freshness SLAs demand more connector and broker capacity and closer monitoring, especially during bulk write bursts on the source table',
  ],
  failureScenarios: [
    'An unannounced column rename on the source table breaks the connector\'s schema mapping silently, and downstream consumers begin receiving malformed or null-mapped fields without any error being raised',
    'A large bulk update (a migration script updating 50 million rows) floods the transaction log, causing CDC lag to spike from milliseconds to tens of minutes and downstream caches to serve stale data during the burst',
    'The initial snapshot is taken without proper isolation, producing a snapshot that mixes pre- and post-transaction state for concurrently modified rows, corrupting the starting point before streaming even begins',
    'A connector crashes and restarts from an outdated log position because the offset was not committed correctly, causing duplicate event delivery that a non-idempotent consumer applies twice',
    'Sensitive PII columns are included in CDC events by default configuration, and a downstream analytics consumer with looser access controls than the source database ends up storing regulated data it was never authorized to see',
    'A delete is represented only by the row disappearing from a later snapshot rather than an explicit tombstone event, so a cache invalidator has no signal to evict the now-deleted key and serves a phantom stale entry indefinitely',
  ],
  productionConsiderations: [
    'Monitor CDC lag continuously as a primary SLI, not an afterthought, since it directly determines the freshness guarantee any downstream consumer can rely on',
    'Alert on connector restarts and verify offset commit correctness after any restart, since a mishandled restart is the most common source of duplicate or missing events',
    'Require schema registry compatibility checks in CI for any source table migration, catching a breaking rename or type change before it reaches production',
    'Load-test the connector against realistic bulk-write scenarios (large migrations, batch imports) before they happen in production, since this is the most common trigger of severe lag spikes',
    'Explicitly configure column-level redaction or exclusion for regulated data at the connector, and audit the configuration as part of any compliance review',
    'Document and test the exactly-once or effectively-once guarantee your consumers actually need, and verify idempotency keys or upsert semantics are correctly implemented on the consumer side',
  ],
  staffDiscussion: [
    'CDC replaces a class of application-level bugs (forgotten cache invalidation, missed event publication) with an infrastructure-level guarantee, and recognizing when this trade is worth the added operational surface is a Staff-level architectural judgment',
    'The initial snapshot problem is consistently underestimated by teams adopting CDC for the first time; a Staff engineer reviewing a CDC rollout plan should explicitly ask how snapshot consistency is guaranteed before approving it',
    'Schema evolution discipline on the source table becomes a cross-team contract the moment CDC consumers depend on it — a database migration that used to be a single team\'s concern now requires coordination with every downstream consumer',
    'Treating the CDC stream as the integration boundary (rather than a shared database or a maintained set of application-level publish calls) is usually the right default for new event-driven systems, but retrofitting it onto an existing system with years of implicit coupling is a multi-quarter migration, not a quick win',
  ],
  relatedTopics: ['Messaging and delivery semantics', 'Distributed transactions', 'CQRS and event sourcing', 'Cache stampede', 'Database selection and sharding'],
  realWorldSystems: ['Debezium with Kafka', 'AWS Database Migration Service (DMS)', 'Airbyte CDC connectors', 'LinkedIn Databus (early CDC system)', 'Stripe event-sourced audit log via CDC'],
  followUpQuestions: [
    'How would you guarantee a consistent initial snapshot when the source table receives thousands of writes per second during the snapshot window?',
    'A downstream consumer needs strict cross-table transactional consistency that per-table CDC ordering cannot provide. How do you address this?',
    'How do you test that your CDC pipeline correctly handles a source-table schema migration before it happens in production?',
  ],
  cheatSheet: [
    'CDC reads the transaction log directly (WAL/binlog/redo log) — no polling, no extra write-path cost',
    'Snapshot-then-stream at startup; getting the snapshot consistency point right is the hardest part',
    'Model deletes as explicit tombstone events, not just row absence',
    'Schema registry with enforced compatibility catches breaking source schema changes before they reach consumers',
    'Monitor CDC lag as a first-class SLA metric, especially during bulk writes',
  ],
  flashcards: [
    {
      question: 'Why is log-based CDC generally preferred over trigger-based or polling-based change capture?',
      answer: 'Log-based CDC reads the transaction log the database is already writing for its own durability and replication purposes, so it adds no extra write-path cost and captures every committed change in guaranteed order. Trigger-based capture adds write-path overhead and risk on every table; polling adds latency and load, and can miss rapid changes between poll intervals.',
    },
    {
      question: 'What is the "initial snapshot problem" in CDC and why is it hard?',
      answer: 'A CDC connector must establish a consistent starting point: a point-in-time snapshot of existing table state plus the exact log position from which to continue streaming, with no gap or overlap. Getting this wrong means either missing changes that occurred during the snapshot or double-processing changes already captured in the snapshot — both corrupt downstream state silently.',
    },
  ],
  oneMinuteAnswer:
    'I deploy a log-based CDC connector, using Debezium, that tails the Postgres write-ahead log directly rather than polling the catalog table or instrumenting every write path in application code. The connector takes a consistent initial snapshot of the 10 million SKUs at a known log position, then continues streaming subsequent changes from that exact position with no gap or duplication. Every change publishes as an event keyed by SKU ID to a partitioned Kafka topic, with explicit tombstone events for deletes so the cache invalidator can distinguish a deleted product from one that never existed. A dedicated cache-invalidation consumer subscribes to this topic and evicts or updates the corresponding cache entry on every event — this replaces dozens of scattered application call sites that would otherwise each need to remember to invalidate correctly. I monitor CDC lag continuously as the metric that determines whether the 500ms freshness SLA is actually being met, with alerting before a bulk update or migration on the source table causes lag to spike. Schema registry compatibility checks gate any change to the product table\'s schema in CI, since an unannounced column change is the most common way this kind of pipeline breaks silently in production.',
};

export const phase11CChapters: EncyclopediaChapter[] = [cryptographyPrimitivesChapter, changeDataCaptureChapter];
