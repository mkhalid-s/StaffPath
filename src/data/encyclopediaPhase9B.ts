import type { EncyclopediaChapter } from '../domain/encyclopedia';

export const dnsCdnNetworkingChapter: EncyclopediaChapter = {
  id: 'dns-cdn-networking',
  title: 'DNS, CDN, and network fundamentals',
  category: 'Systems',
  summary: 'Trace how a request travels from client to origin — DNS resolution, anycast routing, CDN cache hierarchy, and TLS handshake cost — and use that path as a first-class latency and cost lever.',
  problemStatement: 'Teams treat network topology as an invisible substrate and blame the application layer for latency that is actually caused by DNS misconfiguration, a missed CDN cache tier, or an avoidable TLS round trip. Without a mental model of the request path, "make it faster" has no starting point.',
  interviewQuestion: 'Design global CDN routing for a video streaming platform that must serve 4K video to 200 countries with sub-50ms first-byte latency.',
  coreConcepts: [
    'DNS resolution chain — recursive resolver, root, TLD, authoritative',
    'TTL and propagation delay as a deployment lever',
    'DNS-based load balancing and geo-routing',
    'Anycast versus unicast IP routing',
    'CDN edge points of presence (PoPs)',
    'Cache hierarchy — browser, edge, regional, origin',
    'Origin shield',
    'Cache-Control, ETag, and stale-while-revalidate directives',
    'TLS/HTTPS handshake and session resumption',
    'Round-trip time and bandwidth-delay product',
    'BGP and autonomous system routing',
    'QUIC and HTTP/3 connection migration',
  ],
  architectureDiagram:
    'flowchart LR\n  U[User] --> R[Recursive resolver]\n  R --> RT[Root + TLD]\n  RT --> AU[Authoritative DNS — geo/latency routing]\n  AU --> A[Anycast edge PoP]\n  A --> BC{Cache hit?}\n  BC -->|hit| RESP[Serve from edge]\n  BC -->|miss| OS[Origin shield]\n  OS --> OR[(Origin)]\n  OR --> OS --> A --> RESP',
  solutionApproach: [
    'Model the full request path before optimizing anything: DNS lookup, TCP/TLS handshake, edge cache check, origin fetch if needed, response — each stage has a distinct latency and failure profile',
    'Use DNS-based or anycast routing to send users to the nearest healthy edge location; combine both when you need both geo-proximity and instant failover — DNS alone has propagation lag, anycast alone cannot express business routing rules',
    'Set TTL as a deliberate trade-off: short TTL (30-60s) enables fast failover and traffic shifting at the cost of higher resolver query volume; long TTL (hours) reduces load but slows recovery from a bad deploy',
    'Push cacheable content as far to the edge as content freshness allows; use stale-while-revalidate to serve a slightly stale response while refreshing in the background rather than blocking the user on a full origin round trip',
    'Add an origin shield — a single regional cache layer between edge PoPs and the origin — to collapse thousands of edge cache misses into one origin request, protecting origin capacity during a cache-cold event',
    'Minimize TLS handshake cost with session resumption (session tickets, TLS 1.3 0-RTT) and keep-alive connections; a cold TLS handshake can be a third of total latency on a mobile network',
    'Adopt HTTP/3 (QUIC) where head-of-line blocking or network switching (Wi-Fi to cellular) causes visible latency spikes — QUIC migrates connections across IP changes without a new handshake',
    'Instrument cache-hit ratio, origin fetch latency, and DNS resolution time as first-class metrics — a global service with no visibility into these three numbers cannot systematically improve latency',
  ],
  designPatterns: [
    'Origin shield — collapse concurrent edge misses into a single origin fetch',
    'Stale-while-revalidate — serve cached content immediately, refresh asynchronously',
    'Anycast + latency-based DNS — combine instant routing with business-aware geo rules',
    'Cache-Control layering — different TTLs per content type (static assets vs API responses vs live data)',
    'Connection coalescing — reuse one TLS connection across multiple hostnames sharing a certificate',
  ],
  tradeoffs: [
    'Short TTL versus resolver load: fast failover requires short TTL, but every recursive resolver re-queries authoritative DNS more often, increasing query volume and authoritative-server cost',
    'Aggressive edge caching versus freshness: caching further from the origin reduces latency and cost but risks serving stale data — the acceptable staleness window is a product decision, not a pure engineering one',
    'Anycast simplicity versus routing control: anycast gives instant failover with no propagation delay, but you cannot express fine-grained business routing rules (e.g. "route enterprise tier to a dedicated PoP") as easily as with DNS',
    'HTTP/3 adoption versus middlebox compatibility: QUIC runs over UDP, and some corporate firewalls and older network equipment block or throttle UDP, requiring a TCP fallback path',
  ],
  failureScenarios: [
    'DNS TTL set too long during an incident: even after fixing the origin, resolvers keep routing to a degraded PoP for the full TTL window, extending the outage for the slowest-to-refresh clients',
    'Origin shield absent during a cache-cold deploy: a CDN configuration change invalidates the entire edge cache simultaneously, and thousands of edge PoPs all miss at once, sending a traffic spike straight to the origin that it cannot absorb',
    'TLS session resumption disabled: every request pays a full handshake, and under high concurrency the origin\'s CPU is consumed by RSA/ECDHE key exchange rather than serving requests',
    'Cache-Control misconfigured to cache a personalized or authenticated response at a shared edge cache: one user\'s private data is served to another user from the same cache key',
    'Anycast route flapping: BGP route instability causes some users to bounce between two distant PoPs mid-session, each with a different cache state, producing inconsistent or duplicated content',
  ],
  productionConsiderations: [
    'Track cache-hit ratio per content type and alert on drops — a falling hit ratio during steady traffic usually means a cache-key or TTL misconfiguration, not a traffic change',
    'Load-test cache-cold scenarios explicitly: purge the edge cache in staging and observe origin load, not just steady-state cache-warm performance',
    'Monitor DNS resolution time from multiple global vantage points — a resolver issue in one region can silently degrade experience there while global averages look healthy',
    'Rotate and test TLS certificates well before expiry with automated renewal (ACME/Let\'s Encrypt) — certificate expiry is a fully preventable outage class',
    'Set explicit Cache-Control policy per route, never rely on CDN defaults for authenticated or personalized responses',
  ],
  staffDiscussion: [
    'Network topology is not somebody else\'s problem — a Staff engineer designing a global service must be able to draw the full request path and identify where each millisecond and each dollar goes',
    'The CDN cache-hit ratio is simultaneously a latency metric, a cost metric, and an origin-protection metric; treating it as only one of these under-invests in the others',
    'TTL, cache policy, and routing rules are deployment and incident-response levers, not one-time configuration; the on-call runbook should include "reduce TTL to X" as a mitigation step for specific failure classes',
    'When a latency problem is diagnosed as "the network," push for specificity: is it DNS resolution, TCP/TLS handshake, edge cache miss, origin compute, or transit distance? Each has a different owner and a different fix',
  ],
  relatedTopics: ['Load balancing and rate limiting', 'Multi-region active-active architecture', 'API and protocol selection', 'Capacity estimation', 'Advanced caching patterns'],
  realWorldSystems: ['Cloudflare global anycast network', 'AWS CloudFront with origin shield', 'Fastly real-time cache purge', 'Netflix Open Connect CDN', 'Google\'s QUIC/HTTP3 rollout'],
  followUpQuestions: [
    'How would you reduce time-to-first-byte for a user in a region with no nearby CDN PoP?',
    'Your CDN cache-hit ratio dropped from 95% to 60% overnight with no traffic change — how do you diagnose it?',
    'How would you safely reduce DNS TTL globally without overwhelming your authoritative DNS infrastructure?',
  ],
  cheatSheet: [
    'Request path: DNS → TCP/TLS handshake → edge cache check → origin shield → origin',
    'Short TTL = fast failover, higher resolver load; long TTL = opposite trade-off',
    'Origin shield collapses simultaneous edge misses into one origin fetch',
    'Never cache personalized/authenticated responses at a shared edge layer',
    'HTTP/3 (QUIC) survives network changes without a new handshake — useful for mobile',
  ],
  flashcards: [
    {
      question: 'What problem does an origin shield solve?',
      answer: 'When many edge PoPs miss their cache simultaneously (e.g. after a global cache purge), each would otherwise send its own request to the origin, multiplying load. An origin shield is a single regional layer between edges and the origin that collapses concurrent misses into one origin fetch, protecting origin capacity.',
    },
    {
      question: 'Why is DNS TTL a deployment lever, not just a technical setting?',
      answer: 'TTL controls how quickly changes propagate to clients. A short TTL lets you redirect traffic away from a bad deployment or failed region within seconds, at the cost of more frequent resolver queries. A long TTL reduces query load but means an outage persists for the full TTL window even after the fix is live.',
    },
  ],
  oneMinuteAnswer:
    'I start by modeling the full request path: DNS resolution, TCP/TLS handshake, edge cache check, and origin fetch if needed — each stage has distinct latency and failure characteristics. For global routing I combine anycast, which gives instant failover with no DNS propagation delay, with latency-based DNS routing where I need business-aware rules. I set TTL deliberately short enough to fail over quickly during an incident, while an origin shield protects the origin from simultaneous cache misses across thousands of edge PoPs. Cache-Control policy is set explicitly per route — never left to CDN defaults — because caching a personalized response at a shared edge is a data leak, not just a bug. I minimize TLS handshake cost with session resumption and consider HTTP/3 when clients frequently switch networks. Cache-hit ratio, DNS resolution time, and origin fetch latency are the three metrics I instrument first, because "the network is slow" is not actionable without knowing which of these three is actually the bottleneck.',
};

export const multiRegionActiveActiveChapter: EncyclopediaChapter = {
  id: 'multi-region-active-active',
  title: 'Multi-region active-active architecture',
  category: 'Architecture',
  summary: 'Accept writes in multiple regions simultaneously by choosing an explicit conflict-resolution policy, understanding replication lag, and reserving true active-active for the narrow set of systems that actually need it.',
  problemStatement: 'Teams claim "active-active" as a resilience goal without defining what happens when the same record is written in two regions within the replication lag window — the business question of "what wins?" gets answered accidentally by whichever database driver happens to run first, rather than deliberately by the business.',
  interviewQuestion: 'Design a globally consistent shopping cart that must accept writes in any region with sub-100ms latency, even during a regional network partition.',
  coreConcepts: [
    'Active-active versus active-passive versus hot standby',
    'Write conflict and last-write-wins semantics',
    'Conflict-free replicated data types (CRDTs) — counters, sets, registers',
    'Vector clocks and causal ordering',
    'Geo-routing — latency-based and geoproximity',
    'Cross-region replication lag',
    'Global secondary indexes under multi-region writes',
    'Read-your-writes consistency across regions',
    'Split-brain detection and resolution',
    'Regional blast-radius isolation (cell architecture)',
    'Failover automation and DNS propagation lag',
    'Data residency and sovereignty constraints',
  ],
  architectureDiagram:
    'flowchart TD\n  U1[User — US] --> R1[Region US — write]\n  U2[User — EU] --> R2[Region EU — write]\n  R1 <-->|async replication + conflict resolution| R2\n  R1 --> CR{Conflict?}\n  R2 --> CR\n  CR -->|LWW or CRDT merge| RS[Reconciled state]\n  CR -->|ambiguous| APP[Application-level merge]\n  RS --> R1\n  RS --> R2',
  solutionApproach: [
    'Before designing anything, force the business question: what happens when the same entity is written in two regions within the replication lag window? "Last write wins by timestamp," "reject the second write," and "merge both changes" are three different products, not three implementation details',
    'Reserve true active-active — accepting conflicting writes in multiple regions — for the narrow set of systems where users genuinely write from multiple regions with a real latency requirement; most "active-active" claims are actually active-passive with fast automated failover, which is simpler and usually sufficient',
    'Where conflicts are structurally simple (counters, sets, last-write-wins registers), use CRDTs — they merge deterministically without coordination and without a central arbiter',
    'Where conflicts are semantically complex (a shopping cart, an inventory count, a financial balance), CRDTs do not apply cleanly — design explicit application-level merge logic, or restructure the operation to be commutative (e.g. "add item" instead of "set cart contents")',
    'Route users to their nearest healthy region using latency-based or geoproximity DNS, but keep a documented failover path — a region failure must degrade to a known-good behavior, not an undefined one',
    'Measure and alert on cross-region replication lag continuously; a lag spike is the leading indicator that your conflict window is widening and stale-read risk is increasing',
    'Isolate blast radius with a cell architecture: partition tenants or user cohorts into independent cells so a single region\'s failure or a single conflict-resolution bug does not affect every user globally',
    'Plan for split-brain explicitly: define how the system detects a partition between regions and what each side does — continue serving with eventual reconciliation, or refuse writes until connectivity is restored — as a deliberate availability-versus-consistency choice',
  ],
  designPatterns: [
    'CRDTs — deterministic merge for counters, sets, and registers without coordination',
    'Cell-based architecture — isolate regional or tenant blast radius',
    'Commutative operations — design writes as deltas ("add", "increment") instead of full-state overwrites to make merging trivial',
    'Vector clocks — track causal history to distinguish concurrent writes from sequential ones',
    'Read-repair — reconcile divergent replicas lazily on read rather than requiring synchronous agreement on write',
  ],
  tradeoffs: [
    'True active-active versus active-passive-with-fast-failover: active-active gives lower write latency for globally distributed users but requires solving conflict resolution; active-passive is far simpler and meets most availability requirements without that complexity',
    'CRDT simplicity versus expressiveness: CRDTs merge automatically and safely, but only for a limited set of data types — most real business entities (an order, a user profile with dependent fields) do not have an obvious CRDT structure',
    'Strong cross-region consistency versus latency: forcing synchronous cross-region consensus on every write (e.g. via Spanner-style TrueTime or a quorum across regions) gives strict consistency but adds tens to hundreds of milliseconds per write',
    'Regional isolation versus global features: cell architecture contains blast radius but complicates any feature that requires a global view (global search, global uniqueness constraints) — those need explicit cross-cell coordination',
  ],
  failureScenarios: [
    'Two regions accept conflicting writes to the same shopping cart within the replication lag window; last-write-wins silently drops one user\'s added item with no error shown to either user',
    'A network partition splits two regions; both continue accepting writes assuming they are authoritative, and reconciliation after the partition heals surfaces thousands of unresolvable conflicts',
    'A global secondary index (e.g. "find user by email") becomes inconsistent across regions because index updates replicate on a different path than the primary record, producing duplicate or stale lookups',
    'Failover DNS change takes minutes to propagate due to a long TTL, during which a fraction of users are still routed to the failed region and receive errors',
    'A CRDT counter is used for available inventory, but decrementing available stock is not naturally commutative in the way a CRDT set-union is — the "merge" allows overselling because the CRDT cannot express "do not go below zero" as a constraint',
  ],
  productionConsiderations: [
    'Instrument cross-region replication lag as a primary SLI, not an internal implementation detail — it directly bounds your conflict window and your read-after-write guarantees',
    'Run regular regional failover drills (game days) to verify failover automation actually works and to measure real propagation and recovery time, not the theoretical value',
    'Build and test explicit conflict-resolution logic with production-like concurrent write scenarios — most conflict bugs only appear under real concurrent load, not in single-writer testing',
    'Log every conflict resolution decision with enough context (both conflicting writes, the resolution chosen, and why) to support customer support investigations and post-incident review',
    'Define and document data residency requirements per region before building cross-region replication — some data legally cannot leave its region, which changes the entire architecture',
  ],
  staffDiscussion: [
    'Whether to build true active-active is a business decision informed by engineering cost, not an engineering-only decision — the Staff engineer\'s job is to make the trade-off (cost, complexity, and risk of active-active versus the simplicity of active-passive) explicit to the people who own that business call',
    'CRDTs are a powerful but narrow tool; recommending them for every "multi-region" problem without checking whether the operation is actually commutative is a common and costly mistake',
    'A convincing multi-region design must include an explicit answer to "what happens during a partition" — proposing an architecture without defining split-brain behavior is an incomplete design at Staff level',
    'Regional isolation (cell architecture) is often the more valuable investment than perfecting conflict resolution — limiting the blast radius of a bad merge is more tractable than eliminating merge conflicts entirely',
  ],
  relatedTopics: ['CAP, PACELC, and consistency', 'DNS, CDN, and network fundamentals', 'Distributed transactions', 'Consensus and distributed coordination', 'Load balancing and rate limiting'],
  realWorldSystems: ['DynamoDB Global Tables (last-write-wins)', 'Cassandra multi-datacenter tunable consistency', 'CockroachDB distributed SQL with TrueTime-style ordering', 'Cloudflare Durable Objects', 'AWS Route 53 latency-based routing'],
  followUpQuestions: [
    'How would you redesign an inventory-decrement operation so it can be safely merged across regions without overselling?',
    'A customer reports that an item they added to their cart disappeared. How do you investigate whether this was a cross-region conflict?',
    'How do you test your split-brain behavior before it happens in production?',
  ],
  cheatSheet: [
    'Define "what wins on conflict" as a business decision before any implementation',
    'Reserve true active-active for systems that genuinely need multi-region write latency',
    'CRDTs solve narrow conflict classes (counters, sets); most business entities need application-level merge',
    'Replication lag is a primary SLI — it bounds your conflict window',
    'Design and test explicit split-brain behavior, do not leave it undefined',
  ],
  flashcards: [
    {
      question: 'Why do most systems that claim "active-active" not actually need it?',
      answer: 'True active-active requires solving write conflict resolution across regions, which is genuinely hard. Most systems only need high availability (survive a regional failure) and low read latency (serve reads from the nearest region), which active-passive with fast automated failover and read replicas provides at far lower complexity.',
    },
    {
      question: 'Why can\'t a CRDT safely represent a "decrement inventory, never go below zero" operation?',
      answer: 'CRDTs guarantee that merges are commutative, associative, and idempotent so that any merge order produces the same result — but they cannot enforce a global invariant like "never below zero" without coordination, because each region merges independently without seeing the other\'s concurrent decrements. This is exactly the class of problem where CRDTs do not apply and application-level or coordinated logic is required.',
    },
  ],
  oneMinuteAnswer:
    'Before designing anything I force the business question: what happens when the same entity is written in two regions within the replication lag window? That answer — last-write-wins, reject, or merge — is a product decision, not an implementation detail. I reserve true active-active for the narrow case where users genuinely write from multiple regions with a real latency requirement; most systems only need active-passive with fast automated failover, which is far simpler. Where conflicts are structurally simple, like counters or sets, I use CRDTs for deterministic merge without coordination. Where conflicts are semantically complex, like a shopping cart or inventory count, I redesign the operation to be commutative — "add item" instead of "overwrite cart" — or build explicit application-level merge logic. I isolate blast radius with a cell architecture so one region\'s failure or one bad merge does not affect every user globally, and I instrument cross-region replication lag as a primary SLI because it directly bounds staleness risk. Critically, I define and test split-brain behavior explicitly — what each region does during a partition — because an architecture that leaves that undefined is not actually finished.',
};

export const platformEngineeringChapter: EncyclopediaChapter = {
  id: 'platform-engineering',
  title: 'Platform engineering and developer experience',
  category: 'Leadership',
  summary: 'Build internal developer platforms as a product with a golden path compelling enough that teams choose it voluntarily, and measure success by downstream team outcomes rather than platform team output.',
  problemStatement: 'Platform teams mandate tools and processes without making them genuinely easier than the alternative, teams route around the platform with shadow infrastructure, and the platform team measures its own activity (features shipped) instead of the outcome that matters (whether product teams got faster and more reliable).',
  interviewQuestion: 'You are the Staff engineer leading platform for a 200-engineer organization where teams spend 40% of their time on infrastructure toil. How do you build and validate an internal developer platform?',
  coreConcepts: [
    'Internal developer platform (IDP)',
    'Golden path versus escape hatch',
    'Platform-as-a-product mindset',
    'Self-service infrastructure',
    'DORA metrics — deployment frequency, lead time, MTTR, change failure rate',
    'Cognitive load reduction',
    'Toil measurement and reduction',
    'Developer satisfaction — the SPACE framework',
    'Platform team topology (enabling team pattern)',
    'Service catalog and developer portal',
    '"You build it, you run it" versus centralized operations',
  ],
  architectureDiagram:
    'flowchart LR\n  T[Product team] --> GP{Use golden path?}\n  GP -->|yes| SC[Self-service platform: deploy, observe, provision]\n  GP -->|no, justified exception| EH[Escape hatch — supported, documented]\n  SC --> M[DORA metrics + toil measurement]\n  EH --> M\n  M --> PT[Platform team roadmap]\n  PT --> SC',
  solutionApproach: [
    'Treat the platform as a product with the product teams as its customers — run discovery (what is actually slowing teams down?), not just build what seems technically elegant',
    'Design a golden path — an opinionated, well-supported, end-to-end workflow (provision, deploy, observe, roll back) — that is compelling enough teams choose it because it is genuinely faster and safer than building it themselves',
    'Provide an escape hatch for justified exceptions, but make it visible and supported rather than an unowned, undocumented workaround; teams that need to deviate should not be forced into invisible shadow infrastructure',
    'Quantify toil before building anything: survey or instrument how much time teams spend on infrastructure work that is manual, repetitive, and does not require their product-specific judgment — this justifies platform investment with numbers, not intuition',
    'Measure platform success using downstream team outcomes: DORA metrics (deployment frequency, lead time for changes, mean time to recovery, change failure rate) for the teams using the platform, not the platform team\'s own velocity',
    'Reduce cognitive load as a design goal: a team adopting the platform should need to understand less about infrastructure, not more — if onboarding requires a week of platform-specific training, the abstraction has failed',
    'Structure the platform team using the "enabling team" topology — a team that builds capability and self-service tooling for other teams to use independently, rather than a team that does the work on their behalf (which does not scale past a certain org size)',
    'Build a service catalog or developer portal (Backstage-style) so teams can discover what capabilities exist, who owns them, and how to self-serve — invisible platform capability might as well not exist',
  ],
  designPatterns: [
    'Golden path — an opinionated default that is easier to follow than to deviate from',
    'Enabling team topology — platform team builds capability, product teams operate independently',
    'Service catalog / developer portal — discoverable, self-service capability inventory',
    'Toil quantification — measure manual repetitive work before prioritizing automation',
    'SPACE framework — Satisfaction, Performance, Activity, Communication, Efficiency as a multidimensional productivity measure beyond simple output metrics',
  ],
  tradeoffs: [
    'Opinionated golden path versus flexibility: a strongly opinionated path reduces cognitive load and support burden but frustrates teams with genuinely unusual requirements — the escape hatch must be real, not theoretical',
    'Centralizing platform capability versus team autonomy: a platform team owning deployment infrastructure reduces duplicated effort across teams but can become a bottleneck if it cannot scale support faster than adoption grows',
    'Investing in developer experience versus shipping product features: platform work is usually invisible to end users and competes directly with product roadmap for the same engineering capacity — justifying it requires translating toil reduction into delivered capacity',
    'Self-service automation versus guardrails: maximizing self-service speed can bypass safety checks that a centralized team would have enforced manually; the platform must encode the guardrails into the self-service path itself',
  ],
  failureScenarios: [
    'The platform team builds a golden path that is technically superior but ignores a real workflow need; teams route around it with shadow infrastructure that the platform team has no visibility into and cannot support',
    'A mandated tool replaces a working solution without measuring whether it is actually faster; adoption is technically 100% but developer satisfaction and deployment frequency both decline',
    'The platform team measures success by "features shipped" and ships an impressive roadmap, while product team deployment frequency and incident rate are unchanged or worse',
    'Onboarding to the platform requires a week of specialized training; the cognitive load the platform was meant to remove has simply moved from infrastructure knowledge to platform-specific knowledge',
    'An escape hatch exists on paper but is undocumented and unsupported in practice; teams that need it are effectively told no, and either give up on a legitimate need or build unsanctioned workarounds anyway',
  ],
  productionConsiderations: [
    'Instrument DORA metrics for platform-using teams as a standing dashboard, reviewed regularly with platform investment decisions tied to the trend',
    'Run regular developer satisfaction surveys (the "S" in SPACE) — quantitative deployment metrics alone miss the qualitative friction that predicts future attrition and shadow-IT risk',
    'Track escape-hatch usage explicitly; a rising trend signals the golden path is not meeting a real need and should inform the platform roadmap, not just be tolerated',
    'Measure and publish toil hours saved as a concrete, defensible input to platform team headcount and roadmap justification conversations',
    'Maintain a living service catalog with clear ownership for every platform capability — undocumented ownership is the single most common cause of platform capability rotting unmaintained',
  ],
  staffDiscussion: [
    'A platform team that mandates its own tools without making them genuinely better than the alternative is optimizing for control, not developer experience — teams will use judgment to find the fastest safe path regardless of what is mandated',
    'The Staff-level platform leader\'s primary metric is not what the platform team shipped, but whether product teams got measurably faster, safer, and less frustrated — this requires the discipline to measure and report on outcomes you do not fully control',
    'Toil quantification turns "we need more platform investment" from an assertion into an evidence-backed business case — a Staff engineer should be able to produce that evidence, not just the intuition',
    'The enabling-team topology only works if the platform team resists the temptation to become a ticket queue that does the work on behalf of others — that pattern does not scale and quietly reintroduces the bottleneck platform engineering exists to remove',
  ],
  relatedTopics: ['Technical strategy, RFCs, and ADRs', 'Safe delivery and migrations', 'Capacity estimation', 'Influence, conflict, and feedback', 'SLOs, observability, and incidents'],
  realWorldSystems: ['Spotify Backstage developer portal', 'Netflix\'s Paved Road platform philosophy', 'Google\'s Borg-to-Kubernetes internal platform evolution', 'Stripe\'s internal deployment tooling', 'Airbnb\'s developer experience organization'],
  followUpQuestions: [
    'How would you measure whether your golden path is actually reducing cognitive load, not just moving it elsewhere?',
    'A team has been using an unsanctioned deployment process for a year. How do you migrate them without a mandate-driven backlash?',
    'How do you justify platform team headcount to leadership when the team\'s output is invisible to end users?',
  ],
  cheatSheet: [
    'Platform is a product; product teams are the customer — run discovery before building',
    'Golden path must be genuinely faster, not just mandated — measure adoption vs escape-hatch usage',
    'Success = downstream DORA metrics for adopting teams, not platform team activity',
    'Quantify toil before prioritizing automation — turn intuition into an evidence-backed case',
    'Enabling-team topology: build capability, do not become a ticket queue',
  ],
  flashcards: [
    {
      question: 'Why should a platform team measure its success using product-team DORA metrics rather than its own output?',
      answer: 'A platform team can ship an impressive roadmap of internal tools while product teams see no improvement in deployment frequency, lead time, or incident rate — that would mean the investment did not deliver its intended value. Measuring downstream team outcomes keeps the platform team accountable to the actual goal: making other teams faster and safer, not staying busy.',
    },
    {
      question: 'What is the "enabling team" topology and why does it scale better than a team that does work on behalf of others?',
      answer: 'An enabling team builds self-service capability and tooling that other teams use independently, rather than executing infrastructure work on their behalf as a service queue. The service-queue pattern creates a bottleneck that grows linearly with the number of requesting teams; the enabling-team pattern lets adoption scale without the platform team\'s headcount scaling proportionally.',
    },
  ],
  oneMinuteAnswer:
    'I treat the platform as a product with product teams as its customers, starting with discovery on what is actually slowing them down rather than building what seems technically elegant. Before proposing any investment, I quantify toil — the manual, repetitive infrastructure work teams spend time on — to build an evidence-backed case rather than relying on intuition. The core deliverable is a golden path: an opinionated, self-service workflow for provisioning, deploying, and observing that is genuinely faster and safer than the alternative, with a real, documented escape hatch for justified exceptions rather than forcing teams into invisible shadow infrastructure. I structure the platform team using the enabling-team pattern — building capability other teams use independently — because a team that executes work on others\' behalf becomes a bottleneck that does not scale. Success is measured by downstream DORA metrics for the teams using the platform, plus regular developer satisfaction surveys, not by the platform team\'s own shipped feature count. I maintain a discoverable service catalog with clear ownership, because an unowned or invisible capability might as well not exist, and I track escape-hatch usage as a leading signal for where the golden path is failing to meet a real need.',
};

export const phase9BChapters: EncyclopediaChapter[] = [
  dnsCdnNetworkingChapter,
  multiRegionActiveActiveChapter,
  platformEngineeringChapter,
];
