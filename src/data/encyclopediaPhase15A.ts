import type { EncyclopediaChapter } from '../domain/encyclopedia';

const autoscalingCapacityChapter: EncyclopediaChapter = {
  id: 'autoscaling-capacity',
  title: 'Autoscaling and capacity management',
  category: 'Reliability',
  summary: 'Match compute capacity to demand automatically using resource requests, horizontal and vertical scalers, and cluster-level node scaling without starving or over-provisioning workloads.',
  problemStatement: 'Teams set arbitrary CPU and memory requests, wire up a horizontal pod autoscaler on a single metric, and assume the cluster autoscaler will always have room to grow. Under real traffic, scaling lag causes cascading throttling, pods get OOMKilled instead of gracefully rejecting load, and cost balloons from padding every deployment far above its actual usage.',
  interviewQuestion: 'A service is intermittently slow during traffic spikes even though horizontal pod autoscaling is enabled. Walk me through how you would diagnose and fix it.',
  coreTension: 'Autoscaling reacts to load only after it is detected, but the gap between detection and a new pod actually serving traffic is exactly where spikes cause visible failures — no amount of correct configuration eliminates that gap, it can only be shortened or absorbed.',
  levelExpectations: {
    mid: 'Configures HPA on CPU utilization with a target threshold. Sets resource requests based on rough guesses. Unaware of the difference between throttling and OOMKill.',
    senior: 'Selects HPA metrics based on what actually predicts saturation (latency, queue depth) rather than defaulting to CPU. Sets requests from measured p95 usage and avoids running VPA and HPA on the same deployment.',
    staff: 'Frames autoscaling as a capacity-planning decision requiring explicit review, not a Kubernetes config detail. Designs for the scale-up lag gap explicitly — pre-provisioned headroom, load shedding, priority classes — and treats cost and reliability as the same lever rather than separate concerns.',
  },
  coreConcepts: [
    'Horizontal Pod Autoscaler (HPA) — adds/removes replicas based on metrics',
    'Vertical Pod Autoscaler (VPA) — resizes container requests/limits',
    'Resource requests versus limits',
    'CPU throttling versus OOMKill',
    'Quality of Service (QoS) classes — Guaranteed, Burstable, BestEffort',
    'KEDA — event-driven autoscaling on external metrics',
    'Cluster autoscaler — adds/removes nodes',
    'Scaling lag and cold-start latency',
    'Load shedding and admission control under saturation',
    'Spot/preemptible instances and interruption handling',
  ],
  architectureDiagram:
    'flowchart TD\n  M[Metrics: CPU, memory, custom, external] --> H[HPA]\n  H -->|scale replicas| D[Deployment]\n  D --> P[Pods]\n  P -->|pending, unschedulable| CA[Cluster Autoscaler]\n  CA -->|add node| N[Node pool]\n  N -->|spot capacity| SP[Spot/preemptible nodes]\n  V[VPA] -->|resize requests| D\n  K[KEDA] -->|queue depth, custom events| H\n  P -->|saturated| LS[Load shedding / admission control]',
  solutionApproach: [
    'Set resource requests from measured p95 steady-state usage, not guesses — under-requesting causes noisy-neighbor CPU throttling and OOMKill, over-requesting wastes cluster capacity and money',
    'Use Guaranteed QoS (requests equal limits) for latency-sensitive workloads so pods are the last to be evicted under node pressure; use Burstable for tolerant background work',
    'Scale HPA on the metric that actually predicts saturation — request latency or queue depth, not always CPU — and set target utilization with margin for the time it takes new pods to become ready',
    'Combine HPA for request-driven services with KEDA when scaling should react to external signals like queue depth, Kafka lag, or cron schedules rather than only pod-level CPU/memory',
    'Reserve VPA for workloads without frequent horizontal scaling (batch jobs, singletons) since VPA-triggered pod restarts conflict with HPA-triggered replica changes on the same resource',
    'Size cluster autoscaler node pools with headroom for the worst-case single scale-up event, and pre-provision a buffer node pool for latency-critical services that cannot tolerate the time to boot a fresh node',
    'Add pod disruption budgets and readiness gates so scale-down and node rotation never drop below the minimum safe replica count',
    'Use load shedding and circuit breakers as the last line of defense: when autoscaling cannot react fast enough, reject or degrade gracefully instead of queuing until the process OOMs',
  ],
  designPatterns: [
    'Horizontal Pod Autoscaler with custom/external metrics adapter',
    'KEDA scaler for event-driven and queue-based workloads',
    'Cluster autoscaler with mixed on-demand/spot node pools',
    'Pod disruption budget to bound concurrent voluntary evictions',
    'Priority classes and preemption to protect critical workloads under node pressure',
    'Bulkhead / load shedding at the edge to protect against scaling lag',
  ],
  tradeoffs: [
    'Tight resource requests reduce waste but increase throttling and eviction risk under variance; generous requests protect latency but raise cluster cost',
    'Reactive HPA scaling is simple but always lags demand by the metric window plus pod startup time; predictive/scheduled scaling removes lag but requires accurate demand forecasting',
    'Spot/preemptible nodes cut compute cost significantly but require workloads tolerant of abrupt termination — not appropriate for stateful or latency-critical pods without careful design',
    'Cluster autoscaler headroom versus idle cost: keeping spare node capacity avoids scale-up lag but pays for unused compute continuously',
    'CPU-based scaling is easy to configure but is often a lagging indicator; request-latency or queue-depth scaling reacts earlier but requires more instrumentation',
  ],
  failureScenarios: [
    'HPA target based on average CPU across pods masks a single hot pod being CPU-throttled while the average looks healthy',
    'Cluster autoscaler cannot add nodes fast enough during a synchronized spike, so newly scheduled pods sit Pending while existing pods get progressively more overloaded',
    'A pod with request equal to a fraction of actual usage gets OOMKilled under load instead of the container simply running slower, causing crash-loop instead of graceful degradation',
    'VPA and HPA fight each other on the same deployment: VPA resizes and restarts pods while HPA is mid-scale-up, causing thrashing',
    'Spot node reclaimed mid-request with no graceful termination handling — in-flight requests are dropped with no retry',
    'Scale-down removes pods below the minimum needed to serve steady traffic because no PodDisruptionBudget or minReplicas floor was set',
  ],
  productionConsiderations: [
    'Set requests from measured p95/p99 usage under representative load, and revisit them after any code or dependency change that shifts resource footprint',
    'Alert on CPU throttling and OOMKill events directly, not only on aggregate CPU/memory utilization — throttling percentage is the earliest signal of under-provisioning',
    'Load-test the actual scale-up path: time from metric breach to new pod serving traffic, including image pull and readiness probe delay',
    'Run mixed node pools (on-demand baseline plus spot burst capacity) with taints/tolerations so critical pods never land on interruptible nodes unless explicitly designed for it',
    'Set PodDisruptionBudgets and minReplicas floors so cluster scale-down or node rotation cannot drop below a safe serving capacity',
    'Track and cap the ratio of requested-to-actual resource usage cluster-wide as a cost/efficiency metric, not just per-service',
  ],
  staffDiscussion: [
    'Autoscaling configuration is a capacity-planning decision, not a Kubernetes YAML detail — it encodes assumptions about traffic shape, cost tolerance, and acceptable degradation that should be made explicit and reviewed',
    'The interesting failures are rarely "autoscaling was off" — they are metric selection (CPU as a proxy for saturation), timing (scale-up lag versus spike duration), and interaction effects (HPA and VPA fighting, cluster autoscaler racing pod scheduling)',
    'At Staff level, the question to ask before a launch is not "does it autoscale" but "what happens in the seconds between the spike starting and the first new pod serving traffic" — that gap is where load shedding, priority classes, and pre-provisioned headroom earn their cost',
    'Cost and reliability are the same lever here: every unit of headroom you refuse to buy is a unit of scaling lag you must absorb some other way (shedding, queuing, degrading)',
    'Own the full chain — requests/limits, HPA metric choice, cluster autoscaler node pool design, and PodDisruptionBudgets are one system and should be reviewed together, not owned by four different teams that never look at the interactions',
  ],
  relatedTopics: ['Capacity estimation', 'Load balancing and rate limiting', 'Advanced caching patterns', 'Virtual waiting room', 'Backpressure'],
  realWorldSystems: ['Kubernetes HPA/VPA/Cluster Autoscaler', 'KEDA event-driven autoscaling', 'AWS Auto Scaling Groups', 'Google Cloud Autoscaler', 'Netflix Titus resource isolation'],
  followUpQuestions: [
    'Your HPA scales on CPU, but users still report timeouts during spikes. What metric would you switch to and why?',
    'How would you design autoscaling for a queue-consuming worker fleet where request-per-second metrics do not apply?',
    'A cost review shows your cluster is running at 20% average utilization. How do you tighten this without risking incidents?',
  ],
  cheatSheet: [
    'Requests from measured p95 usage; limits set QoS class, not sizing',
    'HPA metric should predict saturation (latency, queue depth) — CPU alone often lags',
    'KEDA for event/queue-driven scaling; VPA for singletons, not alongside active HPA',
    'Cluster autoscaler headroom absorbs scale-up lag; PDBs and minReplicas floor protect scale-down',
    'Alert on throttling/OOMKill directly, not just aggregate utilization',
  ],
  flashcards: [
    {
      question: 'Why can a pod get OOMKilled even though average CPU utilization looks healthy?',
      answer: 'Averages hide single hot pods; also, memory limits are hard caps enforced by immediate kill, unlike CPU which is throttled (slowed) rather than terminated — a memory spike on one pod can breach its limit while cluster-wide averages stay unremarkable.',
    },
    {
      question: 'Why do HPA and VPA conflict when applied to the same deployment simultaneously?',
      answer: 'VPA resizes container requests/limits by recreating pods, while HPA changes replica count based on current metrics — a VPA-triggered restart mid-scale-up can reset the pods HPA is counting, causing thrashing between the two controllers.',
    },
  ],
  oneMinuteAnswer:
    'I start from measured resource usage, not guesses: requests come from observed p95 utilization, and I choose Guaranteed QoS for latency-sensitive pods so they survive node pressure. For HPA, I pick the metric that actually predicts saturation — request latency or queue depth over raw CPU — and I account for the time between a metric breach and a new pod actually serving traffic, since that gap is where spikes cause visible timeouts. Cluster autoscaler needs enough headroom to absorb that same gap, or I pre-provision a buffer pool for critical services. I avoid running VPA and HPA against the same deployment simultaneously to prevent thrashing, use KEDA when scaling should react to external signals like queue depth, and set PodDisruptionBudgets so scale-down never drops below a safe floor. Finally, load shedding at the edge is the backstop for whatever scaling lag remains — degrade gracefully rather than queue until the process falls over.',
};

const searchEngineInternalsChapter: EncyclopediaChapter = {
  id: 'search-engine-internals',
  title: 'Search engine internals',
  category: 'Systems',
  summary: 'Understand how inverted indexes, relevance scoring, and distributed shard routing combine to deliver fast, ranked, near-real-time full-text search at scale.',
  problemStatement: 'Teams bolt a search index onto a relational database with LIKE queries and wonder why relevance is poor and latency degrades as data grows. Or they adopt Elasticsearch/OpenSearch without understanding shard sizing, refresh intervals, or scoring, and are surprised by inconsistent results, slow reindexing, or a cluster that falls over under write load.',
  interviewQuestion: 'Design a search feature for an e-commerce product catalog with millions of items that needs sub-100ms ranked results and near-real-time updates.',
  coreTension: 'Every shard added to increase query parallelism also increases per-query coordination overhead, so the shard count that best serves peak query load is never the shard count that best serves cluster stability or recovery time.',
  levelExpectations: {
    mid: 'Can stand up a basic Elasticsearch index and query it. Understands inverted index conceptually. Unaware of shard sizing or refresh interval tradeoffs.',
    senior: 'Chooses BM25 with function scoring for business signals, sizes shards deliberately from data volume and concurrency, and tunes refresh interval against indexing throughput. Understands scatter-gather query execution.',
    staff: 'Recognizes search as a distributed-systems problem wearing a relevance costume — treats analyzer/mapping changes with the same versioning discipline as a database schema migration, and drives the hybrid-search decision from a measured recall gap rather than novelty.',
  },
  coreConcepts: [
    'Inverted index — term to document postings list',
    'Tokenization and analysis pipeline (stemming, stop words, n-grams)',
    'TF-IDF — term frequency inverse document frequency',
    'BM25 — probabilistic ranking function, successor to TF-IDF',
    'Shard routing — primary and replica shards',
    'Near-real-time (NRT) search semantics and refresh intervals',
    'Index lifecycle management (hot-warm-cold, rollover)',
    'Segment merging and Lucene internals',
    'Query-time versus index-time analysis',
    'Relevance tuning — boosting, function scoring, learning-to-rank',
  ],
  architectureDiagram:
    'flowchart TD\n  D[Documents] --> A[Analysis pipeline: tokenize, stem, filter]\n  A --> I[Inverted index segments]\n  I --> S1[Primary shard 1]\n  I --> S2[Primary shard 2]\n  S1 --> R1[Replica shard 1]\n  S2 --> R2[Replica shard 2]\n  Q[Query] --> QA[Query analysis]\n  QA --> C[Coordinating node]\n  C --> S1\n  C --> S2\n  S1 --> SC[Score: BM25]\n  S2 --> SC\n  SC --> M[Merge and rank results]\n  M --> Q',
  solutionApproach: [
    'Design the analysis pipeline first: tokenizer, stemmer, stop-word filter, and synonym handling determine what "matches" even means before any scoring happens',
    'Choose BM25 (the modern default over raw TF-IDF) for lexical relevance, and layer function scoring or boosts for business signals like recency, popularity, or in-stock status',
    'Size shards from expected data volume and query concurrency — too many small shards waste overhead per shard, too few large shards limit parallelism and slow recovery',
    'Route queries through a coordinating node that fans out to all relevant shards, scores locally, and merges/re-ranks globally — understand that per-shard scoring can produce slightly different IDF statistics than a single-index would',
    'Tune the refresh interval to balance near-real-time visibility of new documents against indexing throughput — frequent refreshes create many small segments that must be merged, costing CPU',
    'Use index lifecycle management to age data from hot (frequent writes, fast SSD) to warm to cold storage, especially for time-series or log-style data',
    'For advanced relevance beyond lexical matching, add semantic/vector search as a complementary signal (hybrid search) rather than replacing BM25 outright, since exact-match lexical queries still matter for product SKUs, names, and codes',
    'Load test with realistic query distributions (including long-tail rare terms and pagination depth) since worst-case latency is dominated by rare terms hitting many small postings lists across shards, not average-case terms',
  ],
  designPatterns: [
    'Inverted index with postings lists',
    'Primary/replica shard replication for availability and read scaling',
    'Coordinating-node scatter-gather query execution',
    'Hot-warm-cold tiered index lifecycle',
    'Hybrid lexical + vector (dense) retrieval with re-ranking',
    'Bulk indexing with backpressure-aware ingestion pipelines',
  ],
  tradeoffs: [
    'More shards increase query parallelism but add per-shard overhead and slow cluster recovery after a node failure',
    'Shorter refresh intervals give near-real-time visibility but increase segment merge overhead and reduce indexing throughput',
    'BM25 alone is fast and explainable but misses semantic/paraphrase matches that vector search captures — hybrid search adds latency and complexity for better recall',
    'Aggressive caching of query results improves latency but risks staleness for frequently updated catalogs (price, stock)',
    'Rich analysis pipelines (synonyms, stemming, n-grams) improve recall but can also introduce false-positive matches and slower indexing',
  ],
  failureScenarios: [
    'Oversharded index: thousands of tiny shards overwhelm cluster state and slow every query due to per-shard coordination overhead',
    'Undersharded index: a single hot shard cannot keep up with write throughput, causing indexing backpressure and search staleness',
    'Refresh interval set too aggressively for write-heavy workload: constant segment merging starves query-serving CPU',
    'Relevance regression after a mapping/analyzer change: reindexing with a new analyzer silently changes tokenization, breaking previously working queries until a full reindex completes',
    'A rare, low-frequency term triggers scatter-gather across many shards with tiny postings lists, producing a long tail of slow queries invisible in average-latency dashboards',
    'Deep pagination (page 500 of results) forces every shard to compute and transmit far more candidates than needed, spiking latency and memory',
  ],
  productionConsiderations: [
    'Monitor segment count and merge activity as leading indicators of indexing/query contention, not just query latency',
    'Track p99 latency by query shape (term rarity, filter cardinality, pagination depth), since averages hide the long tail that scatter-gather produces',
    'Version index mappings and analyzers; changing tokenization requires a full reindex, so plan blue-green index aliases for zero-downtime migration',
    'Separate hot (recent, frequently queried/written) data onto faster nodes from warm/cold historical data using ILM policies',
    'Cap deep pagination (use search-after/cursor-based pagination) instead of allowing arbitrary offset-based paging that scales linearly with offset depth',
    'Load test bulk indexing throughput against target refresh interval before assuming a given write rate is sustainable',
  ],
  staffDiscussion: [
    'Search is a distributed systems problem wearing a relevance costume: the scoring model gets the attention, but shard sizing, refresh intervals, and scatter-gather tail latency determine whether it survives production',
    'Relevance tuning is never "done" — treat it as a continuous evaluation loop (offline relevance judgments, A/B tests on ranking changes) rather than a one-time configuration',
    'The decision to add vector/semantic search should be driven by a measured recall gap on real queries, not novelty — hybrid retrieval adds real operational cost (embedding pipeline, vector index, re-ranking latency)',
    'At Staff level, the mapping/analyzer choice is effectively a schema migration with the same blast radius as a database schema change — it deserves the same versioning and rollout discipline',
    'Index lifecycle and shard topology decisions are capacity planning decisions in disguise — they should be reviewed alongside expected data growth, not set once at launch and forgotten',
  ],
  relatedTopics: ['Capacity estimation', 'Advanced caching patterns', 'Semantic cache for LLM applications', 'Load balancing and rate limiting', 'Data partitioning'],
  realWorldSystems: ['Elasticsearch', 'OpenSearch', 'Apache Solr', 'Apache Lucene', 'Algolia', 'Amazon CloudSearch'],
  followUpQuestions: [
    'Your e-commerce search returns technically-relevant but poorly-ranked results for typo-heavy queries. How would you improve this?',
    'A search cluster shows healthy average latency but customers report occasional multi-second searches. How do you find the cause?',
    'How would you migrate a live search index to a new analyzer/mapping without downtime?',
  ],
  cheatSheet: [
    'Inverted index: term → postings list of document IDs (with positions/frequency for scoring)',
    'BM25 improves on TF-IDF with term-frequency saturation and document-length normalization',
    'Shard count set at index creation — plan for growth, since resharding requires reindexing',
    'Refresh interval trades near-real-time visibility against segment-merge overhead',
    'Deep pagination is expensive — prefer search-after/cursor pagination over offset paging',
  ],
  flashcards: [
    {
      question: 'What does BM25 improve over raw TF-IDF scoring?',
      answer: 'BM25 adds term-frequency saturation (diminishing returns for repeated terms) and document-length normalization, producing more robust relevance ranking than raw TF-IDF, which can over-weight long documents or repeated terms.',
    },
    {
      question: 'Why can a search cluster have healthy average latency but a bad long tail?',
      answer: 'Rare terms produce small postings lists scattered thinly across many shards; the scatter-gather query pattern still pays per-shard coordination overhead for these low-frequency terms, producing occasional slow queries that averages mask.',
    },
  ],
  oneMinuteAnswer:
    'I start with the analysis pipeline — tokenization, stemming, stop words, synonyms — because that defines what counts as a match before any scoring happens. For ranking I use BM25 as the lexical baseline and layer in function scoring for business signals like recency or stock status. I size shards from expected data volume and query concurrency, since too many shards add coordination overhead and too few limit parallelism and slow recovery. Queries fan out through a coordinating node via scatter-gather, so I watch p99 latency by query shape — rare terms and deep pagination are where tail latency hides even when averages look fine. I tune the refresh interval to balance near-real-time visibility against segment-merge cost, use index lifecycle management to move aging data to cheaper tiers, and treat analyzer/mapping changes as schema migrations requiring a full reindex with blue-green index aliases. If lexical relevance alone misses too many paraphrased queries, I add vector search as a complementary hybrid signal rather than a wholesale replacement.',
};

const technicalDebtManagementChapter: EncyclopediaChapter = {
  id: 'technical-debt-management',
  title: 'Technical debt management',
  category: 'Leadership',
  summary: 'Treat technical debt as a quantifiable, prioritizable engineering-leadership discipline rather than an ambient complaint, using a shared vocabulary and business-case framing to sequence paydown against feature work.',
  problemStatement: 'Engineers label anything they dislike as "tech debt," which makes the term useless for prioritization. Debt paydown competes poorly against features because it is described in engineering terms (code smell, coupling) instead of business terms (incident risk, delivery slowdown, hiring cost), so it never gets sequenced and compounds until it causes an outage or blocks a roadmap commitment.',
  interviewQuestion: 'Your team has accumulated significant technical debt and leadership wants to prioritize new features. How do you make the case for paying down debt, and how do you decide what to pay down first?',
  coreTension: 'Debt paydown only wins prioritization against features when it is expressed in the business terms leadership already uses, but the people who understand the debt best usually only know how to describe it in the engineering terms that make it lose that comparison.',
  levelExpectations: {
    mid: 'Identifies technical debt accurately and can describe why specific code is hard to work with. Frames the problem in engineering terms when raising it.',
    senior: 'Classifies debt using deliberate/inadvertent and reckless/prudent axes, quantifies interest rate versus payoff cost, and maintains a visible debt register. Prefers incremental paydown over rewrites.',
    staff: 'Translates engineering risk into a business case (incident risk, delivery slowdown, hiring cost) that competes fairly against features in prioritization. Protects dedicated paydown capacity explicitly and reports measured outcomes to build credibility for future investment asks.',
  },
  coreConcepts: [
    "Cunningham's debt quadrant — deliberate/inadvertent × reckless/prudent",
    'Debt as a financial metaphor — principal, interest, compounding',
    'Interest rate of debt — ongoing cost versus one-time payoff cost',
    'Paydown sequencing strategy',
    '"Pave the road" — incremental, continuous improvement versus rewrite',
    'Business-case framing for non-engineering stakeholders',
    'Debt visibility and tracking (debt register, tagged issues)',
    'Opportunity cost of unaddressed debt',
    'Strangler fig migration as a debt-reduction pattern',
    'Debt introduced by velocity pressure versus by learning',
  ],
  architectureDiagram:
    'flowchart TD\n  ID[Identify debt item] --> C{Classify: deliberate/inadvertent, reckless/prudent}\n  C --> Q[Quantify cost: interest rate, blast radius, frequency hit]\n  Q --> B[Translate to business terms: risk, velocity, cost]\n  B --> P[Prioritize against roadmap]\n  P --> S[Sequence paydown: incremental vs dedicated]\n  S --> E[Execute alongside feature work]\n  E --> M[Measure: incident rate, cycle time, delivery predictability]\n  M -.feedback.-> ID',
  solutionApproach: [
    'Classify each debt item using the deliberate/inadvertent and reckless/prudent axes — prudent-deliberate debt (a conscious shortcut to hit a deadline, with a plan to repay) is fundamentally different from reckless-inadvertent debt (accidental complexity from not knowing better) and needs different handling',
    'Quantify the "interest rate": how much extra time or risk does this debt cost per week/sprint it remains unpaid, versus the one-time cost to fix it — this turns a vague complaint into a comparable investment decision',
    'Translate engineering language into business impact before pitching leadership: "this coupling slows every deploy by two days" beats "this code is messy"',
    'Maintain a visible debt register (tagged issues, a shared document) so debt items compete for prioritization the same way features do, instead of living only in engineers\' heads',
    'Favor "pave the road" incremental improvement — fixing debt in the seams of ongoing feature work, or via strangler-fig migration — over big-bang rewrites, which carry high risk and rarely ship on schedule',
    'Reserve dedicated debt-paydown time (a fixed percentage of capacity, or periodic sprints) for items too large or too risky to fix incrementally, and protect that time from being silently reallocated to features',
    'Sequence paydown by expected value: prioritize debt with high interest rate and low payoff cost first, and debt that blocks an upcoming roadmap commitment regardless of its interest rate',
    'Measure the outcome, not just the activity: track incident rate, cycle time, and delivery predictability before and after paydown to demonstrate ROI and build credibility for the next investment ask',
  ],
  designPatterns: [
    'Debt register / tagged backlog for visibility and prioritization',
    'Strangler fig migration for incremental replacement of risky subsystems',
    '"Pave the road" — bundling debt fixes into adjacent feature work',
    'Fixed-capacity allocation (e.g., 20% time) protected from feature reallocation',
    'Business-case memo translating engineering risk into cost/velocity/incident terms',
  ],
  tradeoffs: [
    'Dedicated debt sprints make progress visible and protected but can feel disconnected from user value and are the first thing cut under deadline pressure',
    'Incremental "pave the road" paydown has lower risk and continuous progress but can stall on debt that no feature work happens to touch',
    'Big-bang rewrites can fully retire debt but carry high delivery risk, opportunity cost, and a track record of running over schedule and losing tacit knowledge embedded in the old system',
    'Being maximally strict about avoiding all debt slows delivery velocity; some deliberate, well-tracked debt is a legitimate tool for hitting a real deadline',
    'Overinvesting in debt paydown without measuring outcomes risks becoming perpetual "cleanup" work that never demonstrably improves velocity or reliability',
  ],
  failureScenarios: [
    'A prudent-deliberate shortcut taken for a deadline is never revisited because it was never logged anywhere, and it silently becomes load-bearing infrastructure',
    'Debt paydown is pitched purely in engineering terms ("we need to refactor this") and is deprioritized indefinitely because leadership cannot compare it to a feature with a clear business outcome',
    'A dedicated rewrite to "fix everything" runs 3x over estimate, and the team ships zero features for two quarters while the business commitments made against the old timeline slip',
    'Debt accumulates invisibly until it causes a customer-facing incident, at which point the fix is rushed and reactive rather than planned and validated',
    'Debt paydown time is nominally allocated but consistently reclaimed for urgent feature requests, so the register grows monotonically and trust in the process erodes',
  ],
  productionConsiderations: [
    'Track incident post-mortems for root causes traceable to known, unaddressed debt items — this is the strongest evidence for prioritization',
    'Instrument cycle time and deploy frequency per service; a service whose cycle time degrades over time relative to peers is a signal of compounding debt',
    'Keep the debt register current and reviewed on a cadence (e.g., quarterly) alongside roadmap planning, not as a one-off exercise',
    'Require a stated payoff plan and owner for any deliberately incurred debt at the time it is introduced, not after the fact',
    'Report paydown outcomes (incident rate, cycle time change) back to the stakeholders who approved the investment, to build a track record for future asks',
  ],
  staffDiscussion: [
    'The core Staff-level skill is translation: converting "this code is bad" into a quantified, comparable business risk or cost so it can compete fairly against feature work in prioritization',
    'Not all debt is equal, and treating it as a single undifferentiated pile is itself a failure mode — prudent-deliberate debt taken knowingly for a real deadline is a legitimate tool, not a moral failing',
    'The pave-the-road philosophy — improving the seams whenever you are already touching them — usually beats big-bang rewrites in expected value, even though rewrites feel more satisfying to propose',
    'A Staff engineer\'s job includes building and defending a credible debt register and cadence, so paydown does not depend on any one person remembering or advocating for it indefinitely',
    'Debt conversations that stay abstract lose to feature conversations that are concrete; make the debt case concrete with real cycle-time numbers, real incident counts, and a real payoff estimate',
  ],
  relatedTopics: ['Capacity estimation', 'Strangler fig migration', 'Incident postmortems', 'Engineering velocity metrics', 'Roadmap prioritization'],
  realWorldSystems: ['Large-scale monolith-to-microservices migrations', 'Legacy payment system modernization programs', 'Long-lived internal platform teams managing shared library debt'],
  followUpQuestions: [
    'How would you quantify the cost of a specific piece of technical debt to justify prioritizing it over a feature?',
    'A team wants a full rewrite to address debt. How do you evaluate whether that is the right call versus incremental paydown?',
    'How do you keep debt-paydown time from being silently reallocated to urgent feature work?',
  ],
  cheatSheet: [
    "Cunningham's quadrant: deliberate/inadvertent × reckless/prudent — not all debt is the same kind of problem",
    'Quantify interest rate (ongoing cost) versus payoff cost (one-time fix) to compare debt items like investments',
    'Translate engineering risk into business terms: incident rate, cycle time, delivery predictability',
    'Prefer pave-the-road incremental fixes and strangler-fig migration over big-bang rewrites',
    'Protect dedicated paydown capacity explicitly, and report outcomes to build credibility for future investment',
  ],
  flashcards: [
    {
      question: 'What are the two axes of Cunningham\'s technical debt quadrant?',
      answer: 'Deliberate versus inadvertent (was the shortcut a conscious choice or an accident of not knowing better) and reckless versus prudent (was it taken carelessly or with a considered plan) — the four combinations require different responses, from "acceptable and tracked" to "should never have happened."',
    },
    {
      question: 'Why do big-bang rewrites often fail to resolve technical debt as promised?',
      answer: 'They carry high delivery risk and commonly run over schedule, they pause feature delivery for an extended period creating real opportunity cost, and they risk losing tacit knowledge and edge-case handling embedded in the old system that was never fully documented.',
    },
  ],
  oneMinuteAnswer:
    'I start by classifying the debt using Cunningham\'s quadrant — deliberate versus inadvertent, reckless versus prudent — because prudent-deliberate debt taken for a real deadline is a legitimate, trackable tool, not the same problem as accidental complexity from not knowing better. I quantify each item\'s interest rate, the ongoing cost in incidents, cycle time, or developer time, against its one-time payoff cost, and I translate that into business terms leadership can compare against feature work: this coupling adds two days to every deploy, not "this code is messy." I keep a visible debt register reviewed alongside roadmap planning so items compete fairly for prioritization instead of living only in engineers\' heads. For paydown, I favor incremental "pave the road" fixes and strangler-fig migration over big-bang rewrites, which carry high risk and often stall feature delivery for quarters. Where a dedicated investment is needed, I protect that capacity explicitly and report back measured outcomes — incident rate, cycle time — to build credibility for the next ask.',
};

export const phase15AChapters: EncyclopediaChapter[] = [
  autoscalingCapacityChapter,
  searchEngineInternalsChapter,
  technicalDebtManagementChapter,
];
