export interface HandbookTemplate {
  title: string;
  body: string;
  example: string;
}

export const handbookTemplates: HandbookTemplate[] = [
  {
    title: 'System design',
    body: 'Problem and users\nFunctional requirements\nQuality attributes and scale\nAPI and data model\nHigh-level design\nCritical deep dives\nFailure, security, observability, cost\nAlternatives and evolution\nRecommendation',
    example: 'Problem and users\nDesign a URL shortener for internal marketing links. Users: marketing team (create links), all employees (click links), analytics team (traffic reports).\n\nFunctional requirements\nCreate a short code for a long URL. Redirect short code to original URL. Optional custom aliases and expiry dates. Click analytics per link.\n\nQuality attributes and scale\n~50K new links/month, ~5M redirects/month. Redirect latency <50ms p99. Availability 99.9% — redirects matter more than the write path. Reads outnumber writes ~100:1.\n\nAPI and data model\nPOST /links {longUrl, alias?, expiresAt?} -> {shortCode}. GET /{shortCode} -> 302 redirect. Table: links(short_code PK, long_url, created_by, created_at, expires_at, click_count). Redis cache short_code -> long_url with 24h TTL.\n\nHigh-level design\nAPI gateway -> Link Service (write path, generates base62 code from an auto-increment ID) -> Postgres primary. Redirect path: CDN edge -> Redis cache -> Link Service fallback on cache miss -> async click-event to a queue for analytics.\n\nCritical deep dives\nCode generation: base62 encode a sharded counter to avoid collisions without a global lock. Cache stampede on a viral link: use request coalescing (single-flight) so only one request repopulates cache per key.\n\nFailure, security, observability, cost\nCache down -> fall back to DB with a rate limiter to protect it. Validate and deny-list redirect targets to prevent open-redirect abuse. Emit redirect latency and cache hit-rate metrics. Cost dominated by CDN egress, not compute.\n\nAlternatives and evolution\nConsidered a NoSQL store for links (rejected — low write volume doesn\'t need it). Evolution: add per-link QR codes and UTM-aware analytics once the core path is proven.\n\nRecommendation\nShip the Postgres + Redis design behind the existing API gateway; it meets latency and availability targets with the least new operational surface.',
  },
  {
    title: 'Architecture decision record',
    body: 'Title and status\nContext and problem\nDecision drivers\nOptions considered\nDecision\nConsequences and risks\nValidation and revisit date',
    example: 'Title and status\nADR-014: Adopt event sourcing for the billing ledger. Status: Accepted.\n\nContext and problem\nOur billing service currently mutates account balances in place. Support cannot reconstruct how a balance was reached, and a recent reconciliation bug took three days to diagnose because prior states were overwritten.\n\nDecision drivers\nNeed a full audit trail for compliance. Need to replay/reprocess history after bug fixes. Team has some CQRS experience already from the orders service.\n\nOptions considered\n(1) Add an append-only audit log alongside the existing mutable table. (2) Full event sourcing with the ledger as a projection. (3) Third-party billing platform migration.\n\nDecision\nAdopt option 2: events are the source of truth, current balance is a projection rebuilt from the event stream.\n\nConsequences and risks\nGain full history and replay-based debugging. Cost: higher initial complexity, and the team must learn eventual-consistency read patterns. Risk: query performance on projections must be validated under load before cutover.\n\nValidation and revisit date\nValidate with a shadow-write period (events written alongside old table for 30 days, balances compared nightly). Revisit this decision in Q1 2027 if projection rebuild time exceeds 10 minutes at 2x current volume.',
  },
  {
    title: 'Technical proposal / RFC',
    body: 'Outcome requested\nCurrent state and evidence\nConstraints\nOptions and trade-offs\nRecommendation\nDelivery and migration plan\nRisks and mitigations\nMeasures of success',
    example: 'Outcome requested\nApproval to replace our synchronous inter-service HTTP calls in the checkout flow with an async event bus for non-critical side effects (email receipts, loyalty points, analytics).\n\nCurrent state and evidence\nCheckout p99 latency is 1.8s; tracing shows 60% of that time is spent waiting on three fire-and-forget downstream calls that don\'t affect the customer-visible result. Two of the last four checkout incidents were caused by a downstream service timeout cascading into checkout failures.\n\nConstraints\nMust not change the customer-facing checkout API contract. Must preserve at-least-once delivery for loyalty points (financial impact if dropped). No budget for a new message broker — must reuse the existing Kafka cluster.\n\nOptions and trade-offs\n(1) Keep synchronous calls, add aggressive timeouts — reduces blast radius but not latency. (2) Move side effects to Kafka events consumed asynchronously — removes them from the critical path, adds eventual-consistency lag (~1-2s) for receipts/points, which is acceptable to product.\n\nRecommendation\nOption 2. The latency and reliability wins outweigh the small consistency delay for non-critical effects.\n\nDelivery and migration plan\nPhase 1: publish checkout-completed events without removing synchronous calls (dual-write, 2 weeks). Phase 2: verify consumer parity via reconciliation job. Phase 3: remove synchronous calls behind a feature flag, roll out 10% -> 100% over one week.\n\nRisks and mitigations\nConsumer lag under load — mitigate with per-consumer-group autoscaling and a dead-letter queue with alerting. Duplicate events — consumers are idempotent by checkout ID.\n\nMeasures of success\nCheckout p99 latency under 800ms. Zero checkout incidents attributable to downstream timeouts within 60 days of full rollout.',
  },
  {
    title: 'Incident update',
    body: 'Customer impact\nCurrent status\nMitigation underway\nConfirmed facts\nUnknowns under investigation\nHelp or decision needed\nNext update time',
    example: 'Customer impact\n~8% of checkout requests are failing with 500 errors since 14:12 UTC. Customers see a generic error and can retry, but retries are also failing intermittently.\n\nCurrent status\nSEV-2, actively mitigating. Error rate has dropped from 22% to 8% after the first mitigation step.\n\nMitigation underway\nRolled back the payments-service deploy from 15 minutes ago (v2.41.0 -> v2.40.3). Rollback is 70% complete across the fleet.\n\nConfirmed facts\nErrors began within 3 minutes of the v2.41.0 deploy. Errors are concentrated on the new retry-with-backoff code path added in that release. Database and downstream payment processor are healthy.\n\nUnknowns under investigation\nWhy the retry logic causes a 500 instead of falling back gracefully — reviewing the diff now. Whether any payments were double-charged during the window (checking idempotency-key logs).\n\nHelp or decision needed\nNeed a second engineer to audit payment logs for possible double-charges while the primary responder finishes the rollback.\n\nNext update time\n15:00 UTC, or sooner if the error rate changes materially.',
  },
  {
    title: 'STAR leadership story',
    body: 'Situation and stakes\nTask and your responsibility\nActions and judgment\nInfluence and collaborators\nMeasured result\nTrade-offs and lessons\nWhat you would change',
    example: 'Situation and stakes\nOur checkout service had grown to depend on 14 synchronous downstream calls, and outage frequency had tripled over two quarters. Leadership was considering a full rewrite, which would have cost roughly two quarters of the team\'s roadmap.\n\nTask and your responsibility\nAs the staff engineer on payments, I was asked to recommend a path forward — rewrite, or something cheaper and faster.\n\nActions and judgment\nI traced the last six incidents and found 80% of failures traced back to just three non-critical downstream calls (receipts, loyalty, analytics) that didn\'t need to be synchronous at all. I proposed moving only those to an async event bus rather than rewriting the whole service, and built a working prototype in a week to prove the latency and reliability gains before asking for a roadmap commitment.\n\nInfluence and collaborators\nI walked the prototype and incident data through an RFC with the SRE and payments teams, addressed their concern about at-least-once delivery for loyalty points with an idempotency design, and got sign-off without needing a VP-level escalation.\n\nMeasured result\nCheckout p99 latency dropped from 1.8s to 650ms, and checkout-related incidents fell from 3/quarter to 0 in the following two quarters — achieved in 3 weeks of work instead of the two-quarter rewrite that was on the table.\n\nTrade-offs and lessons\nThe async path introduced a 1-2s eventual-consistency delay for receipt emails, which I explicitly flagged to product rather than hiding it — they accepted it as a non-issue. Lesson: a scoped, evidence-backed fix beats a full rewrite when you can isolate where the actual risk lives.\n\nWhat you would change\nI\'d instrument the reconciliation job earlier — we found a small class of duplicate events only after full rollout, which a pre-rollout dry run would have caught sooner.',
  },
  {
    title: 'Weekly reflection',
    body: 'What I learned\nWhat I applied\nWhere I struggled\nFeedback received\nEvidence created\nWhat to revisit\nNext week’s deliberate practice',
    example: 'What I learned\nDeepened my understanding of consensus protocols — specifically why Raft\'s leader election term numbers prevent split-brain in a way I hadn\'t fully internalized before.\n\nWhat I applied\nUsed the capacity-estimation framework from this week\'s session in an actual planning doc for a new notifications service, instead of guessing at instance counts.\n\nWhere I struggled\nStill rushing the "clarify requirements" phase in mock system-design interviews — jumped to a solution twice before nailing down scale and consistency needs.\n\nFeedback received\nA peer reviewing my mock interview recording pointed out I talk over interruptions instead of pausing to actually answer the interviewer\'s question.\n\nEvidence created\nOne ADR draft, one mock interview recording with self-scored rubric, one ownership section added to my behavioral story bank.\n\nWhat to revisit\nRevisit distributed transactions (2-phase commit vs. sagas) — my mock interview exposed a shallow answer there.\n\nNext week\'s deliberate practice\nRun two more mock interviews specifically drilling the clarify-requirements phase with a hard 5-minute cap before moving on.',
  },
];

export const amazonLeadershipPrinciples = ['Customer Obsession', 'Ownership', 'Invent and Simplify', 'Are Right, A Lot', 'Learn and Be Curious', 'Hire and Develop the Best', 'Insist on the Highest Standards', 'Think Big', 'Bias for Action', 'Frugality', 'Earn Trust', 'Dive Deep', 'Have Backbone; Disagree and Commit', 'Deliver Results', 'Strive to be Earth’s Best Employer', 'Success and Scale Bring Broad Responsibility'];
