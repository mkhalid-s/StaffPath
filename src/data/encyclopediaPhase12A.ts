import type { EncyclopediaChapter } from '../domain/encyclopedia';

export const keyValueStoreInternalsChapter: EncyclopediaChapter = {
  id: 'key-value-store-internals',
  title: 'Key-value store internals',
  category: 'Data',
  summary: 'Understand how LSM-tree-based key-value stores achieve high write throughput through sequential writes, memtables, and background compaction, trading it against read amplification that bloom filters and compaction strategy must manage.',
  problemStatement: 'Teams pick a key-value store by benchmark headline numbers without understanding whether its write path or read path is optimized for their access pattern, then discover in production that write-heavy workloads stall on compaction or read-heavy workloads suffer from unbounded read amplification.',
  interviewQuestion: 'Design a single-node key-value store that supports get, put, delete, and range scans with write throughput of 1 million ops/second and read latency under 1ms for the 95th percentile.',
  coreTension: 'No storage engine is simultaneously optimal for reads and writes — LSM trees buy write throughput through sequential I/O at the cost of read amplification across multiple SSTables, and every compaction strategy is really just choosing which side of that trade to pay for.',
  levelExpectations: {
    mid: 'Knows a key-value store persists data to disk. Has not distinguished LSM from B-tree behavior or considered compaction as an operational concern.',
    senior: 'Designs the LSM write path (WAL, memtable, SSTable flush), uses bloom filters to avoid unnecessary disk reads, and chooses leveled vs size-tiered compaction based on the read/write balance of the workload.',
    staff: 'Monitors compaction debt as the leading indicator of a coming read-latency incident rather than waiting for the lagging latency metric itself. Questions a benchmark-driven datastore choice by asking what amplification profile the benchmark actually measured and whether it matches production access patterns.',
  },
  coreConcepts: [
    'Memtable — in-memory sorted write buffer',
    'Write-ahead log (WAL) for crash durability',
    'SSTable (Sorted String Table) — immutable sorted disk format',
    'LSM tree (Log-Structured Merge Tree) write path',
    'Bloom filter for negative-lookup optimization',
    'Compaction strategies — size-tiered vs leveled',
    'Read amplification vs write amplification vs space amplification',
    'B-tree vs LSM tree trade-off',
    'Consistent hashing for horizontal distribution',
    'Hash index vs sorted (range-scannable) index',
    'Tombstones for deferred deletes',
    'Block cache and page cache interaction',
  ],
  architectureDiagram:
    'flowchart LR\n  W[Write] --> WAL[(Write-ahead log)]\n  WAL --> MT[Memtable — sorted in-memory]\n  MT -->|flush at threshold| SST1[SSTable L0]\n  SST1 -->|compaction| SST2[SSTable L1]\n  SST2 -->|compaction| SST3[SSTable L2]\n  R[Read] --> BF{Bloom filter}\n  BF -->|maybe present| MT\n  BF -->|maybe present| SST1\n  BF -->|maybe present| SST2\n  BF -->|absent| MISS[Fast negative — no disk read]',
  solutionApproach: [
    'Write path: append to the write-ahead log first for crash durability, then insert into an in-memory sorted memtable — this converts random writes into sequential log writes plus fast in-memory inserts',
    'When the memtable reaches a size threshold, flush it to disk as an immutable SSTable in sorted order, and start a fresh memtable — flushes are sequential writes, never in-place updates',
    'Attach a bloom filter to each SSTable so a read can cheaply determine "definitely not present" without touching disk, since a read must otherwise check every SSTable that might contain the key',
    'Run background compaction to merge SSTables, discarding overwritten and tombstoned entries and reducing the number of files a read must check — this is the mechanism that bounds read amplification over time',
    'Choose leveled compaction when read latency matters most: it merges into non-overlapping sorted runs, bounding the SSTables checked per read at the cost of higher write amplification',
    'Choose size-tiered compaction when write throughput matters most: it merges same-sized files together with less rewriting, at the cost of more SSTables (and worse read amplification) accumulating between compactions',
    'Use a hash-based partitioning scheme with consistent hashing for horizontal scale-out, and keep each partition\'s data range-scannable within the partition for efficient range queries',
    'Size the block cache and rely on the OS page cache for hot SSTable blocks — most production read latency wins come from cache hit rate, not from the underlying storage engine choice',
  ],
  designPatterns: [
    'LSM tree — sequential writes, background compaction, bounded read amplification',
    'Bloom filter — probabilistic negative lookup that avoids unnecessary disk reads',
    'Write-ahead log — durability without waiting for the full data structure to persist',
    'Leveled compaction — non-overlapping sorted runs bound worst-case read cost',
    'Consistent hashing — horizontal partitioning with minimal data movement on rebalance',
  ],
  tradeoffs: [
    'LSM tree versus B-tree: LSM optimizes write throughput via sequential writes and buffering, accepting read amplification across multiple SSTables; B-tree optimizes read latency via in-place updates, accepting random-write I/O cost',
    'Leveled versus size-tiered compaction: leveled bounds read amplification but multiplies write amplification (each key may be rewritten across many levels); size-tiered minimizes write amplification but lets read amplification and space amplification grow between compaction cycles',
    'Bloom filter size versus false-positive rate: a larger bloom filter reduces false positives (unnecessary disk reads) but consumes more memory per SSTable — this is a direct memory-for-latency trade',
    'Compaction aggressiveness versus foreground latency: running compaction more frequently keeps read amplification low but steals I/O bandwidth from foreground reads and writes, causing latency spikes during compaction storms',
    'In-memory versus disk-based store: an all-in-memory store (Redis) gives the lowest possible latency but bounds dataset size to available RAM and complicates durability; disk-based LSM stores scale to far larger datasets at higher per-operation latency',
  ],
  failureScenarios: [
    'Compaction storm: write volume outpaces compaction throughput, SSTables accumulate faster than they can be merged, and read latency degrades progressively as each read must check more and more files',
    'Write stall: the memtable fills faster than it can flush to disk (slow disk, saturated I/O), and the store must block writes until a flush completes — an operational symptom often misdiagnosed as "the database is down"',
    'Bloom filter false-positive storm: an undersized bloom filter produces frequent false positives, causing reads to hit disk for keys that do not actually exist, silently degrading read latency without any error being raised',
    'Tombstone accumulation: mass deletes create tombstones that are not yet compacted away; range scans and reads must skip over them, and if compaction lags, a table can appear to contain far more "logical" data than actually exists',
    'Space amplification blowup: size-tiered compaction with a low compaction frequency allows multiple copies of overwritten keys to coexist across SSTables, consuming disk space far beyond the logical dataset size until the next compaction cycle',
    'Hot partition under consistent hashing: a poorly chosen partition key concentrates traffic on one physical node despite consistent hashing being correctly implemented, because the key distribution itself — not the hashing algorithm — is skewed',
  ],
  productionConsiderations: [
    'Monitor read amplification (SSTables checked per read) and write amplification (bytes written to disk per byte of logical write) as first-class metrics, not just latency percentiles',
    'Alert on compaction debt (pending compaction bytes or SSTable count per level) before it becomes a read-latency incident — compaction debt is the leading indicator, latency is the lagging one',
    'Size bloom filters based on measured false-positive rate against production key distribution, not a default configuration value',
    'Rate-limit compaction I/O so background compaction does not starve foreground reads and writes during peak traffic',
    'Test crash recovery from the write-ahead log explicitly — a WAL that has never been replayed in a disaster-recovery drill is an unverified assumption, not a durability guarantee',
    'Track tombstone ratio per table and force compaction on tables with high delete volume before it degrades scan performance',
  ],
  staffDiscussion: [
    'The choice between LSM and B-tree storage engines is a choice about which half of the read/write latency spectrum you are willing to sacrifice — there is no engine that is simultaneously optimal for both, and vendors that claim otherwise are hiding a trade-off in fine print',
    'Compaction strategy is a tuning decision that must be revisited as workload shifts — a store provisioned for size-tiered compaction under a write-heavy launch phase may need to move to leveled compaction once the workload matures into a read-heavy steady state',
    'Bloom filter and cache sizing are capacity-planning decisions, not defaults to leave untouched — at Staff level you are expected to reason about the memory budget required to keep the hot working set cached, not just accept the storage engine\'s default configuration',
    'When a team proposes a new datastore based on a benchmark, the first Staff-level question is what compaction and read/write amplification profile that benchmark actually measured, and whether it matches the production access pattern',
  ],
  relatedTopics: ['Database selection and sharding', 'Distributed transactions', 'Caching patterns', 'Change data capture'],
  realWorldSystems: ['RocksDB (Meta, embedded in MySQL MyRocks, CockroachDB, TiKV)', 'Cassandra (leveled and size-tiered compaction)', 'HBase (Bigtable-model)', 'Redis (in-memory hash table and skip list)', 'WiredTiger (MongoDB storage engine)'],
  followUpQuestions: [
    'Your read latency has been degrading gradually over the past week with no change in traffic volume. Walk me through how you would diagnose whether this is a compaction, bloom filter, or cache problem.',
    'Why might leveled compaction be the wrong choice for a workload dominated by bulk overwrites of the same small key range?',
    'How would you choose the partition key for a key-value store serving a multi-tenant workload where one tenant is 1000x larger than the median tenant?',
  ],
  cheatSheet: [
    'LSM tree = sequential writes + memtable + SSTables + background compaction — optimizes write throughput',
    'Bloom filter = fast "definitely not present" check that avoids unnecessary disk reads on misses',
    'Leveled compaction = bounded read amplification, higher write amplification; size-tiered = the reverse',
    'Read/write/space amplification are the three metrics that actually characterize a storage engine — not headline QPS',
    'Compaction debt is the leading indicator of a coming read-latency incident — monitor it directly',
  ],
  flashcards: [
    {
      question: 'Why do LSM-tree stores achieve higher write throughput than B-tree stores?',
      answer: 'LSM trees buffer writes in an in-memory memtable and flush them sequentially to disk as immutable SSTables, converting random writes into sequential I/O. B-trees update pages in place, which requires random disk I/O for each write — sequential writes are dramatically faster than random writes on both spinning disks and, to a lesser degree, SSDs.',
    },
    {
      question: 'What problem does a bloom filter solve in an LSM-tree key-value store?',
      answer: 'A read for a key that does not exist would otherwise require checking every SSTable that might contain it — potentially many disk reads for a single miss. A bloom filter attached to each SSTable gives a fast, memory-only "definitely not present" answer for most non-existent keys, avoiding that disk I/O at the cost of a small, tunable false-positive rate.',
    },
  ],
  oneMinuteAnswer:
    'I design the write path around an LSM tree: writes append to a write-ahead log for crash durability, then insert into an in-memory sorted memtable, converting random application writes into sequential disk I/O. When the memtable fills, it flushes to disk as an immutable SSTable. For the read path, I attach a bloom filter to every SSTable so most non-existent key lookups are answered from memory without touching disk, and I rely on a block cache plus the OS page cache to serve hot reads without disk access at all. Because SSTables accumulate over time, I run background compaction to merge and prune them — I choose leveled compaction when read latency matters most, since it bounds the number of SSTables a read must check, accepting higher write amplification as the cost. If write throughput matters more than read latency, I choose size-tiered compaction instead. I monitor compaction debt and read/write amplification as leading indicators, not just latency percentiles, because a compaction storm degrades read latency well before it shows up as an outright outage. For horizontal scale, I partition with consistent hashing and keep each partition\'s data range-scannable, so range queries stay efficient within a shard.',
};

export const objectStorageChapter: EncyclopediaChapter = {
  id: 'object-storage',
  title: 'Object storage architecture',
  category: 'Systems',
  summary: 'Design a durable, cost-effective object storage system using erasure coding for space-efficient redundancy, a separated metadata service for namespace and location lookups, and multipart upload for large, resumable writes.',
  problemStatement: 'Teams assume object storage behaves like a file system with strong consistency and cheap listing operations, then discover in production that bucket listing is expensive, consistency is eventual on some operations, and large uploads fail without resumability, causing data loss and cost overruns at scale.',
  interviewQuestion: 'Design an object storage system that durably stores exabytes of data across commodity servers, achieves 99.999999999% (eleven nines) annual durability, and costs less per gigabyte than triple-replicated block storage.',
  coreTension: 'Object storage looks like a file system but its consistency model varies per operation — treating PUT/GET, LIST, and cross-region replication as uniformly consistent is the single most common cause of a subtle, hard-to-reproduce production bug.',
  levelExpectations: {
    mid: 'Treats object storage as a simple durable file store. Assumes list operations and cross-region reads are as consistent as a direct GET.',
    senior: 'Designs erasure coding for durability at lower overhead than replication, separates metadata from data paths, and implements multipart upload for resumable large writes. States consistency guarantees explicitly per operation.',
    staff: 'Validates that physical shard placement actually matches the theoretical failure-domain redundancy model, rather than trusting the abstract shard math alone. Actively audits for cold data still sitting in hot storage tiers as one of the highest-leverage, lowest-risk cost interventions available.',
  },
  coreConcepts: [
    'Object storage versus block storage versus file storage',
    'Metadata service — namespace, object location, and ACL lookup',
    'Chunk/data servers for object content storage',
    'Erasure coding (Reed-Solomon) versus replication for durability',
    'Multipart upload — atomic commit and resumability for large objects',
    'Object versioning and delete markers',
    'Eventual consistency on some object storage operations',
    'Presigned URLs for delegated, time-bounded access',
    'Storage tiers (hot, warm, cold, archive)',
    'Lifecycle policies for automated tiering and expiry',
    'Object lock and compliance (WORM) mode',
    'Cross-region replication',
  ],
  architectureDiagram:
    'flowchart LR\n  C[Client] --> API[Object API — PUT/GET/DELETE]\n  API --> META[(Metadata service — namespace + location)]\n  API --> ENC[Erasure encoder]\n  ENC --> D1[Data shard 1]\n  ENC --> D2[Data shard 2]\n  ENC --> D3[Data shard 3]\n  ENC --> P1[Parity shard 1]\n  ENC --> P2[Parity shard 2]\n  D1 & D2 & D3 & P1 & P2 --> NODES[(Commodity storage nodes)]\n  API --> LIFE[Lifecycle manager]\n  LIFE --> TIER[Tier transitions + expiry]',
  solutionApproach: [
    'Separate the metadata path from the data path: a metadata service tracks the namespace, object-to-shard mapping, and access control, while independent data servers store the actual bytes — this separation lets each scale independently',
    'Use erasure coding rather than full replication for durability: encode each object into k data shards and m parity shards (e.g. Reed-Solomon RS(6,3)) so any m shard losses can be reconstructed, achieving high durability at roughly 1.5x storage overhead instead of 3x for triple replication',
    'Distribute shards across failure domains (racks, availability zones) so a single rack or zone failure does not take out enough shards to prevent reconstruction',
    'Implement multipart upload for large objects: split the upload into independently-uploadable parts, track completion per part, and only make the object visible after an atomic "complete multipart upload" call — this makes large uploads resumable after a failure',
    'Support object versioning by treating "delete" as writing a delete marker rather than removing data immediately, enabling recovery from accidental overwrites and deletes',
    'Provide presigned URLs so clients can be granted time-bounded, scoped access to a specific object without requiring the storage service\'s primary credentials to pass through the client',
    'Implement lifecycle policies that transition objects between storage tiers (hot to cold to archive) based on access recency, and automatically expire objects past a retention period — this is the primary lever for storage cost optimization at scale',
    'Choose the consistency model deliberately per operation: strong read-after-write consistency for new object PUTs is achievable and expected in modern object stores, but list operations and cross-region replication typically remain eventually consistent',
  ],
  designPatterns: [
    'Erasure coding — durability via parity shards at lower storage overhead than full replication',
    'Metadata/data separation — independent scaling of namespace lookup and byte storage',
    'Multipart upload — atomic commit with resumable, parallel chunk uploads',
    'Presigned URL — delegated, time-bounded access without credential sharing',
    'Lifecycle tiering — automated cost optimization by moving cold data to cheaper storage classes',
  ],
  tradeoffs: [
    'Erasure coding versus replication: erasure coding gives comparable durability at lower storage overhead but costs more CPU to encode/decode and has higher latency to reconstruct a lost shard than simply reading a replica',
    'Strong consistency versus availability: guaranteeing strong read-after-write consistency on every operation (including cross-region replication and bucket listing) requires coordination that trades off against the availability object storage is valued for',
    'Storage tier cost versus retrieval latency and cost: archive tiers are dramatically cheaper per gigabyte but incur retrieval latency (minutes to hours) and per-retrieval fees, making them wrong for data that might need fast access',
    'Object granularity versus metadata overhead: storing many small objects multiplies metadata service load and per-object overhead; batching small objects into larger containers reduces metadata pressure but complicates individual object access',
    'Multipart chunk size versus overhead: smaller chunks improve resumability and parallelism but increase the number of round trips and metadata tracking entries per upload',
  ],
  failureScenarios: [
    'Silent bit rot: a data shard is silently corrupted on disk without the storage node detecting it; without periodic scrubbing and checksum verification, the corruption is only discovered when a read or reconstruction attempt fails',
    'Metadata service bottleneck: a bucket with hundreds of millions of objects causes list operations to time out or become extremely slow, because listing queries the metadata index sequentially and metadata service capacity was provisioned for object count, not query pattern',
    'Interrupted multipart upload leaves orphaned parts: a client disconnects mid-upload without calling abort, leaving uploaded parts consuming storage indefinitely unless a lifecycle policy specifically cleans up incomplete multipart uploads',
    'Failure domain correlation: erasure-coded shards are distributed across nodes but multiple nodes share an underlying failure domain (same rack, same power circuit) that the placement algorithm did not account for, and a single physical failure takes out enough shards to prevent reconstruction',
    'Eventual consistency surprise: an application assumes a newly uploaded object is immediately visible in a subsequent list operation, but list operations are eventually consistent in the given object store, causing intermittent "missing object" bugs that are difficult to reproduce',
    'Lifecycle policy misconfiguration deletes live data: an overly broad lifecycle expiration rule intended for temporary upload staging objects accidentally matches production data with a similar key prefix, causing silent, gradual data loss that is only noticed once retention periods have passed and recovery is no longer possible',
  ],
  productionConsiderations: [
    'Run periodic data scrubbing that reads and verifies checksums on stored shards, proactively detecting and repairing bit rot before it accumulates to the point of unrecoverable data loss',
    'Design placement algorithms that are failure-domain aware — verify that erasure-coded shards for a given object are actually distributed across independent racks, power circuits, and availability zones, not just across distinct physical nodes',
    'Monitor metadata service latency separately from data path latency; a metadata service that has not been load-tested against the target object count and listing pattern will silently degrade well before storage capacity is exhausted',
    'Configure and test lifecycle policies against a staging bucket before applying broadly; test the interaction between multiple lifecycle rules for overlapping key prefixes explicitly',
    'Instrument and alert on incomplete multipart upload accumulation, and configure automatic cleanup after a bounded time window to prevent orphaned storage cost',
    'Validate that cross-region replication lag and consistency guarantees actually match what downstream applications assume — test failover scenarios where a read immediately follows a write to a different region',
  ],
  staffDiscussion: [
    'Erasure coding versus replication is fundamentally a cost-per-durability trade-off, and the correct choice depends on the actual failure-domain topology available, not just the abstract shard math — a Staff engineer must validate that the physical placement matches the theoretical redundancy model',
    'The consistency model of an object store is not a single property — it varies per operation (PUT, GET, LIST, cross-region replication), and Staff-level system design requires stating explicitly which consistency guarantee each application code path actually depends on',
    'Storage cost optimization through lifecycle tiering is one of the highest-leverage, lowest-risk cost interventions available in most systems, and Staff engineers are expected to actively audit for cold data still sitting in hot storage tiers rather than treating storage cost as fixed',
    'The metadata service is almost always the real scaling bottleneck in an object storage system long before the data storage layer is stressed — capacity planning conversations that focus only on total bytes stored are missing the actual constraint',
  ],
  relatedTopics: ['Database selection and sharding', 'Distributed transactions', 'Capacity estimation', 'Multi-region active-active architecture'],
  realWorldSystems: ['Amazon S3', 'Google Cloud Storage', 'Azure Blob Storage', 'Ceph (open source object storage)', 'MinIO (self-hosted S3-compatible)', 'Cloudflare R2'],
  followUpQuestions: [
    'A customer reports that an object they uploaded is missing from a bucket listing, but a direct GET on the known key succeeds. What consistency behavior explains this and how do you communicate the guarantee to the customer?',
    'How would you design the placement algorithm to guarantee that erasure-coded shards survive a full rack failure, not just individual node failures?',
    'Design the lifecycle policy and cost model for a bucket storing 10 petabytes of log data with a 90-day hot access pattern and a 7-year compliance retention requirement.',
  ],
  cheatSheet: [
    'Erasure coding (e.g. RS 6+3) ≈ 1.5x storage overhead for durability comparable to 3x triple replication',
    'Metadata service scales independently from data servers — it is usually the real bottleneck, not raw storage capacity',
    'Multipart upload = resumable + parallel chunks + atomic visibility only after explicit "complete" call',
    'Consistency guarantees differ per operation — PUT/GET often strongly consistent, LIST and cross-region replication often eventually consistent',
    'Lifecycle tiering is the single highest-leverage storage cost lever — audit for cold data sitting in hot tiers',
  ],
  flashcards: [
    {
      question: 'Why does erasure coding cost less storage overhead than replication for equivalent durability?',
      answer: 'Erasure coding (e.g. Reed-Solomon RS(6,3)) splits an object into k data shards and m parity shards; any m shard losses can be reconstructed from the remaining k shards. This achieves durability comparable to triple replication (3x overhead) at roughly 1.5x overhead (9 total shards storing 6 shards worth of data), because the parity shards protect against loss more space-efficiently than storing full redundant copies.',
    },
    {
      question: 'Why is listing objects in a very large bucket often slow even though individual GET operations are fast?',
      answer: 'GET operations resolve through the metadata service to a specific object location and read directly from data servers — a targeted, indexed lookup. LIST operations must query the metadata index across a potentially very large key range, which is a fundamentally more expensive operation that scales with the number of objects in the queried prefix, not with the size of any individual object.',
    },
  ],
  oneMinuteAnswer:
    'I separate the metadata path from the data path: a metadata service tracks the namespace, object-to-shard mapping, and access control, independently scalable from the data servers that store actual bytes. For durability I use erasure coding rather than full replication — splitting each object into data and parity shards using something like Reed-Solomon RS(6,3), which survives up to 3 shard losses at roughly 1.5x storage overhead instead of 3x for triple replication — and I explicitly design shard placement to be failure-domain aware, spreading shards across racks and power circuits, not just distinct nodes. For large objects I implement multipart upload: independently uploadable parts with an atomic "complete" call that only makes the object visible once, giving resumability on failure. I treat object deletes as versioned delete markers rather than immediate removal, enabling recovery from accidental deletes. I am explicit about consistency guarantees per operation — PUT and GET are typically strongly consistent in modern object stores, but list operations and cross-region replication are usually eventually consistent, and application code must not assume otherwise. For cost, lifecycle policies automatically tier cold objects to cheaper storage classes and expire data past retention — this is usually the highest-leverage cost optimization available.',
};

export const distributedIdGenerationChapter: EncyclopediaChapter = {
  id: 'distributed-id-generation',
  title: 'Distributed ID generation',
  category: 'Systems',
  summary: 'Generate unique, often time-sortable identifiers across many nodes without a central coordination bottleneck, choosing between Snowflake-style composite IDs, UUID v7, and ULIDs based on sortability, monotonicity, and coordination requirements.',
  problemStatement: 'Teams default to database auto-increment or random UUIDv4 without considering that auto-increment does not scale past a single writable node and random UUIDs fragment database indexes and destroy insert locality at scale.',
  interviewQuestion: 'Design an ID generation service for a distributed e-commerce platform that generates 100,000 order IDs per second globally, where IDs must be sortable by creation time, unique across all nodes, and fit in a 64-bit integer.',
  coreTension: 'An ID scheme becomes embedded in primary keys, API contracts, and client caching logic the moment it ships, so a choice that looks reversible in a design doc becomes as irreversible as a schema decision within weeks.',
  levelExpectations: {
    mid: 'Uses database auto-increment or random UUIDv4 without considering horizontal scale or index locality implications.',
    senior: 'Chooses between UUID v7, ULID, and Snowflake based on sortability and coordination requirements, and handles machine ID allocation via a coordination service. Implements explicit backwards-clock handling.',
    staff: 'Treats the ID scheme choice with the same care as an irreversible schema decision, since it embeds across the entire system\'s primary keys and contracts. Asks about boundary conditions (clock skew, sequence exhaustion, timestamp budget) rather than which algorithm, since that is where naive implementations actually fail in production.',
  },
  coreConcepts: [
    'UUID v4 — random, 122 bits of entropy, not sortable',
    'UUID v7 — time-ordered, sortable, standardized in RFC 9562',
    'ULID — sortable, Crockford base32 encoded, URL-safe',
    'Snowflake ID — 64-bit composite of timestamp, machine ID, and sequence',
    'Ticket server — centralized sequential ID allocation',
    'Database auto-increment — single writable node, not horizontally distributed',
    'Time-based ordering versus strict monotonic ordering',
    'Clock skew and the backwards-clock problem',
    'Machine ID allocation and coordination requirement',
    'ID space exhaustion and bit-budget allocation',
    'Lexicographic sortability and its effect on B-tree index locality',
    'Probabilistic uniqueness (UUID) versus deterministic uniqueness (Snowflake)',
  ],
  architectureDiagram:
    'flowchart LR\n  REQ[ID request] --> GEN{Generator type}\n  GEN -->|coordination-free, sortable| V7[UUID v7 / ULID — timestamp + random bits]\n  GEN -->|coordination-free, not sortable| V4[UUID v4 — fully random]\n  GEN -->|per-machine, sortable, compact| SF[Snowflake — timestamp + machine ID + sequence]\n  SF --> MID[(Machine ID allocator — ZooKeeper/etcd/DB)]\n  V7 --> OUT[64/128-bit sortable ID]\n  SF --> OUT2[64-bit compact sortable ID]',
  solutionApproach: [
    'Determine the actual requirement first: do IDs need to be sortable by creation time, must they fit in 64 bits, and is coordination at generation time acceptable — these three answers eliminate most of the options immediately',
    'For a coordination-free, sortable requirement without a strict 64-bit constraint, default to UUID v7: it embeds a millisecond timestamp in the high bits followed by random bits, giving sortability and standardized interoperability (RFC 9562) without any machine coordination',
    'For a strict 64-bit budget with per-machine generation, use a Snowflake-style scheme: allocate bits for a timestamp (typically 41 bits, millisecond precision, ~69 years of range), a machine/shard identifier (typically 10 bits, up to 1024 machines), and a per-millisecond sequence counter (typically 12 bits, 4096 IDs per machine per millisecond)',
    'Solve machine ID allocation explicitly — assign machine IDs via a coordination service (ZooKeeper, etcd) at startup, or via a static configuration deployed per shard, since Snowflake\'s uniqueness guarantee depends entirely on no two machines sharing a machine ID at the same moment',
    'Handle the backwards-clock problem explicitly: if NTP adjusts the local clock backward, a naive Snowflake generator could produce an ID that collides with (or sorts before) a previously issued ID; the standard mitigation is to refuse to generate new IDs until the clock catches up to the last recorded timestamp',
    'For range-scan-friendly sortable IDs with human-readable encoding, consider ULID: functionally similar to UUID v7 in embedding a timestamp, but encoded in Crockford base32 for URL-safety and case-insensitive readability',
    'Avoid a centralized ticket server or database auto-increment for anything beyond moderate scale — a single writable node for ID allocation is both a throughput bottleneck and a single point of failure, even if the IDs it produces are perfectly correct',
    'Validate the ID scheme against the actual database index implications: monotonically increasing IDs (Snowflake, ULID, UUID v7) preserve B-tree insert locality and avoid page-split fragmentation; fully random IDs (UUID v4) scatter inserts across the index and degrade write performance at scale',
  ],
  designPatterns: [
    'Snowflake ID — composite timestamp + machine ID + sequence for compact, sortable, coordination-light IDs',
    'UUID v7 — standardized time-ordered UUID for coordination-free sortable identifiers',
    'ULID — sortable, URL-safe alternative encoding to UUID v7',
    'Ticket server — centralized allocation, simple but a scaling and availability bottleneck',
    'Machine ID coordination via ZooKeeper/etcd — solves the collision risk in Snowflake-style schemes',
  ],
  tradeoffs: [
    'Coordination-free (UUID v4/v7, ULID) versus coordinated (Snowflake, ticket server): coordination-free schemes have no single point of failure or bootstrap dependency, but Snowflake-style schemes are more compact (64 bits vs 128) and simpler to reason about for sequence ordering within a shard',
    'Sortability versus entropy: a fully random ID (UUID v4) has more entropy per bit and is simpler to reason about for uniqueness, but a time-ordered ID (Snowflake, UUID v7, ULID) enables efficient range scans and preserves database index locality at the cost of leaking approximate creation time',
    'Compactness versus ID lifespan: a 64-bit Snowflake ID with a 41-bit timestamp budget exhausts its useful range in roughly 69 years from epoch — a real constraint that must be sized against the system\'s expected lifetime, whereas a 128-bit UUID has effectively unlimited range',
    'Machine ID allocation simplicity versus collision risk: static machine ID assignment is simple to reason about but risks collision if two instances are misconfigured with the same ID; dynamic allocation via a coordination service eliminates that risk but adds a startup-time dependency',
  ],
  failureScenarios: [
    'Backwards clock collision: an NTP correction moves a machine\'s clock backward by several seconds, and a naive Snowflake generator produces an ID with a timestamp component that duplicates or precedes a previously issued ID from the same machine, silently violating the uniqueness guarantee',
    'Machine ID collision from misconfiguration: two instances are deployed with the same static machine ID due to a configuration copy-paste error, and both begin generating IDs that collide under concurrent load, corrupting any system relying on ID uniqueness as a primary key',
    'Sequence counter exhaustion: traffic on a single machine exceeds the sequence counter\'s per-millisecond budget (e.g. 4096 IDs/ms in a 12-bit sequence), forcing the generator to either block until the next millisecond or, if unhandled, silently wrap and produce duplicate IDs',
    'Auto-increment single point of failure: a database used for centralized ID generation becomes unavailable, and because every downstream write path depends on obtaining an ID first, the entire write path halts even though the actual data stores are healthy',
    'Index fragmentation from random IDs: a system migrates from sequential auto-increment IDs to random UUIDv4 primary keys without recognizing the index locality trade-off, and write throughput on the primary table degrades due to random-order B-tree page splits',
    'Timestamp epoch exhaustion unplanned: a Snowflake scheme is deployed with a custom epoch and a 41-bit timestamp budget calculated for "many years" without documenting the actual exhaustion date, and the system approaches that date with no migration plan in place',
  ],
  productionConsiderations: [
    'Document and monitor the actual timestamp budget exhaustion date for any Snowflake-style scheme, and treat approaching that date as a planned migration event, not a surprise',
    'Implement and test the backwards-clock handling path explicitly — inject a simulated clock rollback in a test environment and verify the generator refuses to produce colliding IDs rather than assuming it will "probably be fine"',
    'Use a coordination service for machine ID allocation in any deployment where manual static assignment risk of human error is unacceptable, and alert on any detected machine ID collision immediately',
    'Benchmark actual database insert throughput under the chosen ID scheme against realistic production concurrency, since the index locality effect of random versus sequential IDs is often invisible at low load and only manifests under production write volume',
    'Choose an ID length and encoding that fits actual constraints across the full request path — API payload size, database column type, log line searchability — before committing to a scheme, since migrating ID formats after launch is expensive',
  ],
  staffDiscussion: [
    'The choice of ID generation scheme is rarely revisited once made because it becomes embedded in database primary keys, API contracts, and client-side caching logic across the entire system — treat this decision with the same care as an irreversible schema choice',
    'The backwards-clock problem is the single most commonly mishandled edge case in home-grown Snowflake implementations; a Staff engineer should always ask to see the specific mitigation before accepting a custom ID generation scheme into a critical path',
    'Adopting UUID v7 (standardized in 2024) over a custom Snowflake variant is now often the better default for new systems, since it provides sortability without inventing and operating custom machine ID coordination infrastructure',
    'At Staff level, the real design question is rarely "which algorithm" but "what happens at the boundary conditions" — clock skew, machine ID exhaustion, sequence counter overflow, and timestamp budget exhaustion are where naive implementations actually fail in production',
  ],
  relatedTopics: ['Database selection and sharding', 'Time, clocks, and ordering', 'Distributed locks and leader election in practice', 'Capacity estimation'],
  realWorldSystems: ['Twitter Snowflake (open-sourced, now widely emulated)', 'Instagram sharded 64-bit IDs (shard + timestamp + sequence)', 'Discord Snowflakes', 'MongoDB ObjectID (12-byte timestamp + machine + process + counter)', 'UUID v7 (RFC 9562 standard)'],
  followUpQuestions: [
    'Your Snowflake-based ID generator is approaching its timestamp budget exhaustion date in 18 months. Walk me through the migration plan for a system that has embedded these IDs as primary keys across 40 microservices.',
    'How would you detect and prevent a machine ID collision in a fleet of 500 auto-scaling instances without introducing a coordination service as a new single point of failure?',
    'Why might a UUIDv4 primary key that performed fine in staging cause a production incident under 10x the write volume?',
  ],
  cheatSheet: [
    'UUID v4 = random, not sortable, no coordination needed — bad for B-tree index locality at scale',
    'UUID v7 / ULID = time-ordered, sortable, no coordination — the modern coordination-free default',
    'Snowflake = timestamp + machine ID + sequence in 64 bits — compact and sortable but needs machine ID allocation',
    'Always handle the backwards-clock case explicitly — this is the most common home-grown Snowflake bug',
    'Sequential/time-ordered IDs preserve database insert locality; fully random IDs fragment B-tree indexes under write load',
  ],
  flashcards: [
    {
      question: 'Why does a Snowflake ID generator need explicit handling for a clock that moves backward?',
      answer: 'Snowflake IDs embed the current timestamp in the high bits. If NTP corrects the local clock backward, a naive generator could produce a new ID with a timestamp value equal to or earlier than one it already issued, breaking the uniqueness and monotonic-ordering guarantee. The standard fix is to track the last timestamp used and refuse to generate new IDs until the clock advances past it, rather than trusting the system clock unconditionally.',
    },
    {
      question: 'Why can random UUIDv4 primary keys hurt database write performance at scale in a way that sequential IDs do not?',
      answer: 'Most relational and many NoSQL databases store data in B-tree structures ordered by primary key. Sequential or time-ordered IDs insert at the end of the tree, preserving locality and minimizing page splits. Fully random UUIDv4 keys insert at random points throughout the tree, causing frequent page splits, poor cache locality, and increased write amplification — a cost that is often invisible at low write volume and only manifests under production-scale concurrent writes.',
    },
  ],
  oneMinuteAnswer:
    'I start by pinning down the actual requirements: does the system need strict sortability, a hard 64-bit budget, and is any generation-time coordination acceptable. If coordination-free sortable IDs suffice, I default to UUID v7 — it embeds a millisecond timestamp in the high bits, is now a standardized format under RFC 9562, and needs no machine ID allocation infrastructure. If a strict 64-bit budget is required, I use a Snowflake-style composite: a timestamp field, a machine or shard ID field, and a per-millisecond sequence counter, with machine IDs allocated through a coordination service like ZooKeeper or etcd at startup rather than static configuration, to eliminate collision risk from human error. Critically, I explicitly handle the backwards-clock case — if NTP corrects the clock backward, the generator must refuse to produce IDs until the clock catches up to the last recorded timestamp, since this is the most common bug in home-grown Snowflake implementations. I avoid centralized ticket servers or database auto-increment beyond moderate scale, since they introduce both a throughput ceiling and a single point of failure on the write path. Finally, I validate the choice against actual database index behavior: time-ordered or sequential IDs preserve B-tree insert locality, while fully random IDs fragment the index and degrade write throughput as data volume grows.',
};

export const phase12AChapters: EncyclopediaChapter[] = [
  keyValueStoreInternalsChapter,
  objectStorageChapter,
  distributedIdGenerationChapter,
];
