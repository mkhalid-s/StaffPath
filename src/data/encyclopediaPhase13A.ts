import type { EncyclopediaChapter } from '../domain/encyclopedia';

export const consistentHashingChapter: EncyclopediaChapter = {
  id: 'consistent-hashing',
  title: 'Consistent hashing',
  category: 'Systems',
  summary: 'Distribute keys across nodes on a hash ring so that adding or removing a node remaps only a small fraction of keys, using virtual nodes to smooth out uneven load that a naive ring placement would otherwise produce.',
  problemStatement: 'Teams use simple modular hashing (key mod N) to distribute load across a cluster, then discover that adding or removing a single node remaps almost every key, causing a cache stampede or a full data rebalance that a smarter partitioning scheme would have avoided.',
  interviewQuestion: 'You are adding a 10th cache node to a 9-node Memcached cluster. With simple modular hashing, roughly 90% of cache keys become invalid. Design an approach that minimizes cache invalidation when nodes are added or removed.',
  coreTension: 'Minimizing data movement on topology change requires giving up the simplicity of a globally recomputed partition function, forcing every client or coordinator to track ring state that can drift out of sync.',
  levelExpectations: {
    mid: 'Knows modular hashing remaps almost every key on cluster resize. Can describe the hash ring concept but struggles to explain why virtual nodes are needed.',
    senior: 'Designs a ring with virtual nodes, sized proportionally to node capacity, and can reason about the 1/n data movement bound. Handles replica placement via ring successors.',
    staff: 'Treats virtual node count and replication factor as governed capacity-planning parameters, not defaults. Diagnoses hotspots as skewed key distribution rather than a hashing flaw, and designs rebalance staging (rate limiting, pre-warming) as a first-class production concern, not an afterthought.',
  },
  coreConcepts: [
    'Hash ring — keyspace mapped onto a fixed-size circle',
    'Node placement on the ring via hashing the node identifier',
    'Key-to-node mapping — walk clockwise to the first node encountered',
    'O(k/n) data movement on topology change versus O(k) for modular hashing',
    'Virtual nodes (vnodes) for uniform load distribution',
    'Vnode count versus ring metadata overhead trade-off',
    'Weighted virtual nodes for heterogeneous hardware capacity',
    'Hotspot detection and mitigation',
    'Consistent hashing versus rendezvous (highest random weight) hashing',
    'Replication via successor nodes on the ring',
    'Gossip protocol for ring membership propagation',
    'Client-side versus server-side ring awareness',
  ],
  architectureDiagram:
    'flowchart LR\n  K[Key] --> H{Hash function}\n  H --> RING((Hash ring 0..2^32))\n  RING --> N1[Node A — vnodes]\n  RING --> N2[Node B — vnodes]\n  RING --> N3[Node C — vnodes]\n  N1 -.successor replica.-> N2\n  N2 -.successor replica.-> N3\n  ADD[New node D] -.joins ring.-> RING\n  RING -->|only 1/n keys move| N1',
  solutionApproach: [
    'Map both nodes and keys onto the same fixed-size hash ring using the same hash function, so a key\'s position is deterministic and independent of cluster size',
    'Assign each key to the first node encountered walking clockwise from the key\'s position on the ring — this is the core lookup rule that makes the scheme work',
    'Add many virtual nodes per physical node (typically 100–200) so that the physical node\'s share of the ring is an average over many small arcs rather than one large contiguous arc, smoothing out load distribution',
    'Weight virtual node count by physical node capacity when the cluster is heterogeneous, so a node with double the RAM or disk gets roughly double the virtual nodes and therefore roughly double the key share',
    'On node addition, only the keys that fall between the new node\'s ring position and its predecessor\'s position need to move — bound this to roughly 1/n of total keys, not a full rebalance',
    'On node removal, only the keys that node owned need to move, to its ring successor — implement this as an explicit rebalance step, not an implicit assumption',
    'Replicate each key to the N successor nodes on the ring (not just the primary owner) so a single node failure does not cause data loss, and so reads can be served from any replica',
    'Monitor per-node load continuously and detect hotspots explicitly — a hotspot can occur even with correct consistent hashing if the underlying key distribution itself (not the hash function) is skewed, such as one celebrity key or one oversized tenant',
  ],
  designPatterns: [
    'Consistent hashing ring — deterministic key placement with bounded remapping on topology change',
    'Virtual nodes — smooth load distribution across a small number of physical nodes',
    'Successor replication — read/write availability across N replicas without a single owner bottleneck',
    'Rendezvous (HRW) hashing — an alternative that avoids the ring metadata entirely at the cost of an O(n) lookup',
    'Gossip-based membership — eventually consistent view of ring topology without a central coordinator',
  ],
  tradeoffs: [
    'Virtual node count versus ring metadata size: more vnodes smooth load distribution more evenly but increase the metadata every client or coordinator must store and keep in sync as the ring changes',
    'Consistent hashing versus rendezvous hashing: consistent hashing gives O(1) ring lookups via binary search but requires ring metadata propagation; rendezvous hashing needs no ring state at all but requires an O(n) computation per key lookup across all nodes',
    'Number of replicas versus write cost: replicating to more successor nodes improves read availability and durability but increases write latency and network cost for every write',
    'Gossip propagation delay versus coordination overhead: a gossip-based membership view is simple and has no single point of failure, but nodes may briefly disagree on ring topology during a change, causing transient misrouted requests',
    'Simplicity versus necessity: for a stable cluster with a genuinely uniform key distribution, plain modular hashing is simpler and faster — introducing consistent hashing is only justified once node membership changes are frequent enough, or the cluster large enough, that full rebalances become operationally expensive',
  ],
  failureScenarios: [
    'Uneven load without virtual nodes: a naive single-position-per-node ring produces wildly uneven arc sizes by chance, so one physical node ends up owning a disproportionate share of keys even though the hash function itself is unbiased',
    'Cascading rebalance under flapping nodes: a node repeatedly leaves and rejoins the ring (flapping due to a health-check flake), triggering a rebalance storm each time as keys are moved off and then back onto the node',
    'Hot key despite correct hashing: a single celebrity key or oversized tenant lands on one node\'s arc, and because consistent hashing distributes key ranges rather than individual key load, that one node becomes a hotspot regardless of how well-tuned the virtual node count is',
    'Stale ring view during a topology change: a client or coordinator using an outdated ring snapshot routes a request to a node that no longer owns that key range, producing a miss or a stale read until the ring view converges',
    'Replica placement ignoring failure domains: successor replicas for a key are correctly chosen by ring position, but if virtual nodes for one physical rack happen to cluster on the ring, a single rack failure can take out multiple replicas of the same key simultaneously',
    'Cache stampede on rebalance: when a large batch of keys moves to a newly added node during rebalance, all those keys are simultaneously cold on the new node, and a spike of cache misses hits the origin database at once',
  ],
  productionConsiderations: [
    'Measure actual per-node load distribution in production, not just theoretical vnode-based expectations — real key distributions are rarely perfectly uniform and monitoring reveals skew that math alone will not',
    'Rate-limit and stagger rebalance operations after a topology change so that data movement does not saturate network bandwidth or overwhelm the newly joined node with a flood of simultaneous transfers',
    'Make virtual node count and replication factor explicit, versioned configuration — changing either after a cluster is in production requires a full data movement plan, not a quiet config update',
    'Ensure replica placement is failure-domain aware: verify that successor replicas for any given key land on physically independent racks or availability zones, not just distinct virtual node positions',
    'Pre-warm caches for keys about to move during a planned rebalance, when the operation is predictable (e.g. scheduled node addition), to avoid a stampede on the origin store',
    'Test node addition and removal under production-representative load in a staging environment before running it against a live cluster, since rebalance behavior under real traffic patterns often differs from idle-cluster testing',
  ],
  staffDiscussion: [
    'Consistent hashing is frequently proposed as a default without first confirming that node membership actually changes often enough, or that the cluster is large enough, to justify the added ring-metadata complexity over simpler alternatives',
    'Virtual node count is a capacity-planning parameter, not a default to leave untouched — a Staff engineer should be able to justify the chosen count against the measured load-distribution variance it produces, not just cite a common value',
    'The distinction between "the hashing algorithm is uneven" and "the underlying key distribution is uneven" is one every Staff engineer must be able to diagnose quickly — a hotspot after correctly implemented consistent hashing is almost always the latter, and no amount of vnode tuning fixes a skewed access pattern',
    'Rebalance behavior — not steady-state lookup — is where consistent hashing designs actually fail in production; a Staff-level design must specify how data movement is staged, rate-limited, and verified after every topology change',
    'Choosing consistent hashing versus rendezvous hashing versus a static shard map is a decision that should be driven by how often topology changes and how much ring-metadata propagation the system can tolerate, not by which algorithm is more familiar',
  ],
  relatedTopics: ['Key-value store internals', 'Database selection and sharding', 'Load balancing and rate limiting', 'Distributed locks and leader election in practice'],
  realWorldSystems: ['Amazon Dynamo (the origin paper)', 'Apache Cassandra (with virtual nodes)', 'Memcached (libketama client-side hashing)', 'Redis Cluster (hash slots, a related but distinct approach)', 'Chord peer-to-peer protocol', 'Amazon DynamoDB'],
  followUpQuestions: [
    'Your consistent hashing ring is correctly distributing keys evenly across nodes by count, but one node is still receiving 5x the traffic of the others. What would you check first?',
    'How would you change the replica placement strategy to survive a full availability-zone failure, not just a single node failure?',
    'A node is being decommissioned during a peak traffic window. Walk me through how you would stage the key migration to avoid a cache stampede on the origin database.',
  ],
  cheatSheet: [
    'Ring lookup rule: hash the key, walk clockwise, first node encountered owns it',
    'Virtual nodes (100–200 per physical node) smooth load — without them, arc sizes are wildly uneven by chance',
    'Node add/remove moves ~1/n of keys, not the whole keyspace — this is the entire point versus modular hashing',
    'Replicate to N successor nodes on the ring for availability, and verify they span independent failure domains',
    'A hotspot after correct hashing usually means the key distribution is skewed, not the algorithm',
  ],
  flashcards: [
    {
      question: 'Why do virtual nodes matter for consistent hashing, and what happens without them?',
      answer: 'Without virtual nodes, each physical node occupies one position on the hash ring, and the arc of keyspace it owns is determined by chance — the gap to the next node\'s position. With only a handful of physical nodes, these gaps vary wildly, so some nodes end up owning far more keyspace than others purely by the luck of hash placement. Virtual nodes assign each physical node many small positions scattered around the ring (typically 100–200), so its total share becomes an average over many small arcs, converging toward a fair, even distribution.',
    },
    {
      question: 'Why does consistent hashing move only a fraction of keys when a node is added, unlike simple modular hashing?',
      answer: 'Modular hashing (key mod N) recomputes every key\'s owner from scratch whenever N changes, because the modulus itself changes — this remaps nearly all keys even for a one-node change. Consistent hashing assigns keys based on ring position relative to existing node positions; adding one node only affects the keys that fall in the arc between the new node and its immediate predecessor, leaving every other key\'s owner unchanged. This bounds data movement to roughly 1/n of the total keyspace instead of remapping everything.',
    },
  ],
  oneMinuteAnswer:
    'I place both nodes and keys onto the same fixed-size hash ring using a shared hash function, and assign each key to the first node found walking clockwise from its ring position. To avoid the uneven load a small number of raw node positions would produce by chance, I assign each physical node roughly 100 to 200 virtual node positions scattered around the ring, weighting the count by each node\'s actual capacity if the cluster is heterogeneous. The payoff comes on topology change: adding or removing a node only moves the keys in the arc adjacent to that node — roughly 1/n of the keyspace — instead of the near-total remap that simple modular hashing would cause. I replicate each key to the next N successor nodes on the ring, verifying those successors span independent racks or availability zones so a single failure domain outage does not take out every replica of a key. In production I monitor actual per-node load, not just theoretical vnode-based expectations, because a hotspot after correctly implemented consistent hashing almost always traces back to a skewed underlying key distribution — a celebrity key or oversized tenant — rather than a flaw in the hashing itself, and I rate-limit rebalance operations after any topology change to avoid saturating the network or stampeding the origin store.',
};

export const metricsMonitoringAlertingChapter: EncyclopediaChapter = {
  id: 'metrics-monitoring-alerting',
  title: 'Metrics monitoring and alerting system',
  category: 'Reliability',
  summary: 'Build the full pipeline from metric emission to actionable alert — collection, time-series storage, aggregation, and rule evaluation — while governing label cardinality so the system does not collapse under its own metadata.',
  problemStatement: 'Teams stand up a metrics stack, add labels freely as new dashboards are requested, and eventually a single high-cardinality label choice creates millions of unplanned time series, degrading query performance and inflating storage cost until the monitoring system itself becomes the outage.',
  interviewQuestion: 'Design a metrics monitoring system that collects 1 million metrics per second from 10,000 services, stores 90 days of data, and evaluates 10,000 alert rules every 30 seconds with sub-second evaluation latency.',
  coreTension: 'The label flexibility that makes metrics useful for ad-hoc debugging is the exact mechanism that causes cardinality explosions — every unbounded label is a latent outage waiting for the wrong debugging instinct.',
  levelExpectations: {
    mid: 'Knows metrics have a name, labels, and a value over time. Can set up basic scraping and a static threshold alert. Unaware of cardinality risk.',
    senior: 'Designs cardinality governance at ingestion, chooses pull vs push deliberately, and implements tiered hot/cold storage. Knows multi-window burn-rate alerting reduces false positives.',
    staff: 'Frames cardinality governance as an organizational process problem, not just a technical guardrail — treats the monitoring system\'s own reliability (scrape success, rule evaluation lag) as a first-class production signal, and designs alert inhibition against the actual service dependency graph rather than per-service in isolation.',
  },
  coreConcepts: [
    'Pull versus push metric collection models',
    'Metric types — counter, gauge, histogram, summary',
    'Time-series data model — metric name, labels, timestamp, value',
    'Cardinality — the product of all label value combinations per metric',
    'Time-series database storage engine — chunking, compression, retention',
    'Query language and range vector selectors (PromQL-style)',
    'Alerting rules and evaluation intervals',
    'Alert routing, grouping, and inhibition',
    'Alert fatigue and actionability as a design constraint',
    'Federation and remote-write for multi-cluster aggregation',
    'Long-term storage layered beneath a short-term hot store',
    'SLO-based (burn rate) alerting versus static threshold alerting',
  ],
  architectureDiagram:
    'flowchart LR\n  SVC[Services] -->|expose /metrics| SCRAPE[Scraper — pull model]\n  SCRAPE --> TSDB[(Time-series DB — local, short retention)]\n  TSDB -->|remote write| LTS[(Long-term store — Thanos/Cortex-style)]\n  QUERY[Query engine] --> TSDB\n  QUERY --> LTS\n  RULES[Alert rule evaluator] --> TSDB\n  RULES -->|firing| ROUTER[Alert router — group, inhibit, dedupe]\n  ROUTER --> PAGE[Page / ticket / Slack]\n  DASH[Dashboards] --> QUERY',
  solutionApproach: [
    'Choose the collection model deliberately: pull-based scraping (the service exposes a metrics endpoint and the monitoring system fetches on an interval) gives the monitoring system control over load and makes service discovery explicit; push-based collection suits short-lived batch jobs and works across restrictive network boundaries but requires every emitter to run an agent',
    'Model every metric as a time series keyed by metric name plus a fixed set of labels — treat the label schema as a governed contract, not a free-for-all, since the label set determines cardinality',
    'Enforce cardinality budgets at ingestion: reject or flag metrics whose label combinations would explode the time-series count (never label with unbounded values like user ID, request ID, or raw URL path)',
    'Store recent data (hours to days) in a fast local time-series store optimized for high-throughput writes and low-latency queries, using chunked, compressed storage on a fixed retention window',
    'Layer a long-term storage tier beneath the local store for historical queries beyond the local retention window, accepting higher query latency for that older data in exchange for far lower storage cost per sample',
    'Design alerting rules to evaluate against pre-aggregated, rate-normalized queries (e.g. error rate over a rolling window) rather than raw counters, and set evaluation intervals short enough to catch fast-moving incidents without over-taxing the query engine',
    'Route firing alerts through a dedicated router that groups related alerts, deduplicates repeated firings, and inhibits lower-priority alerts when a higher-priority root-cause alert is already firing, to reduce alert fatigue',
    'Prefer SLO-based burn-rate alerting over static thresholds for the primary paging signal: alert on the rate at which an error budget is being consumed, using both a short window (catches fast burns) and a long window (catches slow, sustained degradation)',
  ],
  designPatterns: [
    'Pull-based scraping with service discovery — explicit, centrally controlled collection',
    'Hot/cold storage tiering — fast local retention backed by cheaper long-term storage',
    'Multi-window, multi-burn-rate alerting — balances fast detection against noise',
    'Alert grouping and inhibition — collapse related symptoms into one actionable notification',
    'Cardinality governance at ingestion — reject unbounded label values before they become time series',
  ],
  tradeoffs: [
    'Pull versus push: pull-based systems give central control and explicit service discovery but struggle with short-lived jobs and firewalled environments; push-based systems handle both easily but distribute agent operational burden across every emitting service',
    'Query flexibility versus cardinality cost: allowing arbitrary label combinations gives maximal query flexibility later but risks unbounded time-series growth; strict label governance keeps cardinality controlled but requires anticipating query needs at instrumentation time',
    'Alert sensitivity versus fatigue: tight thresholds and short evaluation windows catch incidents faster but generate more false positives and pager fatigue; loose thresholds reduce noise but delay detection of real incidents',
    'Local retention window versus storage cost: a longer local hot-store retention window keeps recent queries fast but multiplies storage and memory cost; shorter retention forces more queries onto the slower long-term tier',
    'Metric-level alerting versus SLO burn-rate alerting: alerting on individual metric thresholds is simple to reason about per-signal but produces many uncorrelated pages; SLO-based alerting requires more upfront modeling but produces fewer, higher-signal pages tied directly to user impact',
  ],
  failureScenarios: [
    'Cardinality explosion from an unbounded label: an engineer adds a request_id or raw URL path as a metric label intending to aid debugging, and the resulting millions of unique time series overwhelm the TSDB\'s memory and query performance within hours',
    'Alert storm during a real incident: a single root-cause failure (a downstream dependency outage) triggers dozens of downstream alerts simultaneously across every service that depends on it, flooding on-call with duplicate signals for the same underlying cause because inhibition rules were never configured',
    'Scrape target overload during a deploy: a rolling deployment causes many service instances to restart simultaneously, and the scraper attempts to collect from all of them at once, saturating network and CPU on the monitoring system during the exact window incidents are most likely',
    'Silent data gap from a crashed scraper: the metric collection agent itself crashes or falls behind, and dashboards silently show flat or missing data that on-call mistakes for "everything is fine" rather than "monitoring is broken"',
    'Alert rule evaluation lag under high cardinality: a poorly written alert rule performs an expensive aggregation across a high-cardinality metric, and rule evaluation falls behind its interval, delaying alert firing well past the point where it would still be actionable',
    'Long-term storage query timeout: a dashboard queries 90 days of high-resolution data against the long-term store without any downsampling, and the query times out or returns partial data during exactly the retrospective analysis an incident review needed',
  ],
  productionConsiderations: [
    'Enforce a cardinality budget per metric and per team at ingestion time, with automated alerts when a new metric or label threatens to exceed it, rather than discovering the problem after the TSDB degrades',
    'Monitor the monitoring system itself — scrape success rate, rule evaluation duration, and time-series count — as first-class signals, since a silent gap in metrics collection is itself an incident',
    'Stagger and rate-limit scrape targets during coordinated events like rolling deployments, so the monitoring system\'s own load does not spike exactly when service instability is most likely',
    'Downsample and aggregate data as it ages into long-term storage (e.g. 10-second resolution for the last day, 5-minute resolution beyond 30 days) to keep historical queries fast and storage cost bounded',
    'Configure alert grouping and inhibition explicitly for known dependency chains, so a single root-cause failure produces one actionable page instead of a flood of correlated symptom alerts',
    'Test alert rules against historical incident data (backtesting) before relying on them in production, to validate that the chosen thresholds and windows would actually have fired at the right time for past real incidents',
  ],
  staffDiscussion: [
    'Cardinality governance is an organizational problem as much as a technical one — without a review process for new labels, cardinality grows unchecked as different teams instrument independently, and no storage engine tuning fixes an ungoverned label schema',
    'The choice between metric-threshold alerting and SLO burn-rate alerting is a maturity signal: teams that graduate from individual metric thresholds to burn-rate alerting on error budgets consistently report fewer pages with a higher proportion of genuinely actionable ones',
    'A Staff engineer designing a monitoring system must treat the monitoring system\'s own reliability as a first-class requirement — a silent scraper failure or a rule-evaluation lag during an incident is itself a production failure, not an acceptable degraded mode',
    'Alert grouping and inhibition require modeling the actual dependency graph between services; retrofitting this after alert fatigue has already eroded on-call trust is far more expensive than designing it in from the start',
    'Downsampling and tiered retention are cost-optimization levers that most teams under-invest in until storage cost becomes visible in a budget review — proactively designing the retention and downsampling policy is a Staff-level responsibility, not an afterthought',
  ],
  relatedTopics: ['SLOs, observability, and incidents', 'Chaos engineering as a discipline', 'Capacity estimation', 'Time, clocks, and ordering'],
  realWorldSystems: ['Prometheus and Alertmanager (CNCF, pull-based standard)', 'Grafana (visualization layer)', 'Thanos, Cortex, and Mimir (Prometheus long-term storage patterns)', 'Datadog (push-based SaaS)', 'InfluxDB (push-based, high-cardinality IoT-oriented)', 'Victoria Metrics (high-performance Prometheus-compatible store)'],
  followUpQuestions: [
    'A dashboard that was working fine last week is now timing out. How do you determine whether this is a cardinality problem, a query problem, or a storage tier problem?',
    'Your on-call team says they are getting paged 40 times a night for the same underlying database failover event. What would you change about the alerting design?',
    'How would you migrate an existing metrics system from static threshold alerts to SLO burn-rate alerts without a gap in incident detection during the transition?',
  ],
  cheatSheet: [
    'Never label a metric with an unbounded value — user ID, request ID, or raw URL path will explode cardinality',
    'Pull model = central control + explicit service discovery; push model = handles short-lived jobs + firewalls, needs agents',
    'Multi-window, multi-burn-rate SLO alerting produces fewer, higher-signal pages than static per-metric thresholds',
    'Downsample as data ages — full resolution near-term, coarser resolution in long-term storage',
    'Monitor the monitoring system itself: scrape success rate and rule evaluation lag are leading indicators of blind spots',
  ],
  flashcards: [
    {
      question: 'Why is an unbounded label like user_id or request_id dangerous on a metric, even though it seems useful for debugging?',
      answer: 'A time-series database creates one distinct series per unique combination of label values. A label with millions of possible values (a user ID or request ID) multiplies the metric\'s time-series count by that many, often overwhelming the TSDB\'s memory and query performance within hours. High-cardinality debugging data belongs in logs or traces, which are designed for that access pattern, not in metrics, which are designed for aggregation across a bounded label space.',
    },
    {
      question: 'What is the advantage of multi-window, multi-burn-rate alerting over a single static threshold?',
      answer: 'A single static threshold on a raw metric either fires too often on transient noise (short window) or misses fast incidents (long window). Multi-window burn-rate alerting evaluates error-budget consumption over both a short window (catching fast, severe burns) and a long window (catching slow, sustained degradation) simultaneously, requiring both to breach their thresholds before paging. This produces fewer false positives while still catching both incident shapes with acceptable detection latency.',
    },
  ],
  oneMinuteAnswer:
    'I choose a pull-based collection model so the monitoring system controls scrape load and service discovery explicitly, with agents on short-lived or firewalled workloads falling back to push where pull genuinely does not fit. Every metric is modeled as a time series with a governed label schema, and I enforce a cardinality budget at ingestion — no label may take an unbounded value like user ID or raw URL, since that is the single most common cause of a monitoring system collapsing under its own metadata. Recent data lives in a fast local time-series store sized for a short retention window, backed by a long-term storage tier that downsamples older data to keep both storage cost and query latency bounded as data ages. Alert rules evaluate rate-normalized, pre-aggregated queries rather than raw counters, and I prefer SLO burn-rate alerting — evaluated across both a short and a long window — as the primary paging signal over static per-metric thresholds, because it consistently produces fewer, more actionable pages tied to real user impact. Firing alerts route through grouping and inhibition rules modeled against the actual service dependency graph, so a single root-cause failure produces one page, not a flood of correlated symptom alerts. I treat the monitoring system\'s own health — scrape success rate, rule evaluation lag, time-series count — as a first-class signal, because a silent gap in metrics collection is itself a production incident.',
};

export const phase13AChapters: EncyclopediaChapter[] = [
  consistentHashingChapter,
  metricsMonitoringAlertingChapter,
];
