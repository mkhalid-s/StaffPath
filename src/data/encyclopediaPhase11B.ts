import type { EncyclopediaChapter } from '../domain/encyclopedia';

export const dataLakehouseChapter: EncyclopediaChapter = {
  id: 'data-lakehouse',
  title: 'Data lakehouse architecture',
  category: 'Data',
  summary: 'Combine the scale and flexibility of object-storage data lakes with the ACID guarantees, schema enforcement, and performance of a warehouse using open table formats, and organize ownership around data as a product rather than a central pipeline team.',
  problemStatement: 'Teams dump raw events into cheap object storage expecting warehouse-like reliability, then discover query engines silently return incorrect or stale results because there is no transaction guarantee, no enforced schema, and no visibility into which of ten thousand small files actually belong to a consistent snapshot.',
  interviewQuestion: 'Your company has 500TB of raw event data in S3 that analysts query via Athena, but queries are slow and stale. Design an architecture that gives analysts fresh, ACID-compliant data with sub-minute query times on a week\'s worth of events.',
  coreConcepts: [
    'Data lake versus data warehouse versus lakehouse',
    'Open table formats — Delta Lake, Apache Iceberg, Apache Hudi',
    'ACID transactions on object storage via a metadata/transaction log layer',
    'Schema evolution — add, rename, and drop columns without a full rewrite',
    'Time travel and point-in-time queries',
    'Partitioning strategy for analytical workloads',
    'Z-ordering and data skipping for query pruning',
    'Compaction and the small-file problem',
    'Data catalog and schema registry',
    'Medallion architecture — bronze, silver, gold layers',
    'Data mesh — domain ownership and federated governance',
    'Data contracts between producers and consumers',
  ],
  architectureDiagram:
    'flowchart LR\n  S[Streaming + batch sources] --> B[Bronze — raw, append-only]\n  B --> C[Compaction job]\n  C --> SI[Silver — cleaned, schema-enforced]\n  SI --> G[Gold — aggregated, business-ready]\n  T[(Transaction log / metadata)] --> B\n  T --> SI\n  T --> G\n  CAT[Data catalog] --> Q[Query engines]\n  G --> Q',
  solutionApproach: [
    'Adopt an open table format (Delta Lake, Iceberg, or Hudi) as the storage layer on top of object storage so every write is an atomic transaction recorded in a metadata log, not a bare file drop',
    'Organize data through a medallion architecture: bronze holds raw immutable ingested data, silver holds cleaned and schema-validated data, gold holds business-ready aggregates that analysts actually query',
    'Partition tables by a column with the query engine\'s pruning pattern in mind — typically ingestion date — and combine with Z-ordering on high-cardinality filter columns to enable data skipping',
    'Run continuous, non-blocking compaction jobs that merge small files produced by streaming ingest into right-sized files, since query planners open every file in a partition and thousands of tiny files destroy performance',
    'Enforce schema at the silver layer with the table format\'s built-in evolution support, so adding a nullable column never requires rewriting historical data',
    'Use time travel queries to reconstruct a past table state for debugging, auditing, or reproducing a report, rather than maintaining separate point-in-time snapshot exports',
    'Register every table in a central data catalog with ownership, schema, and freshness SLA metadata so consumers can discover and trust data without asking the producing team directly',
    'If adopting data mesh, treat it as an organizational redesign — each domain team owns and publishes its data as a product with a documented contract — not merely a migration of the same central pipeline to distributed infrastructure',
  ],
  designPatterns: [
    'Medallion architecture — bronze/silver/gold layered refinement of raw data',
    'Transaction log as source of truth — the metadata layer is authoritative, not the raw files',
    'Data contract — a producer-consumer schema and freshness agreement enforced like an API contract',
    'Continuous compaction — background merge jobs that never block concurrent reads',
    'Data mesh — domain-oriented ownership with federated computational governance',
    'Change data feed — consumers subscribe to row-level changes instead of re-scanning full tables',
  ],
  tradeoffs: [
    'Freshness versus compaction overhead: compacting aggressively keeps queries fast but consumes compute that competes with ingestion; compacting too rarely lets small files accumulate until queries degrade',
    'Central data platform versus data mesh: a central pipeline team gives consistent tooling and lower duplicated effort, but becomes a bottleneck at scale; data mesh distributes ownership but requires mature federated governance or chaos results',
    'Strict schema enforcement versus ingestion flexibility: rejecting non-conforming records at write time protects downstream consumers but can silently drop data if producers are not alerted; permissive ingestion with downstream validation catches more data but pushes cleanup cost onto every consumer',
    'Fine-grained partitioning versus partition explosion: partitioning by more dimensions improves pruning for narrow queries but creates excessive small partitions that themselves need compaction and slow metadata operations',
    'Time travel retention window versus storage cost: retaining every historical version indefinitely enables unlimited point-in-time queries but multiplies storage cost — most systems bound retention and vacuum old versions',
  ],
  failureScenarios: [
    'Small-file collapse: a streaming job writes one file per micro-batch for months without compaction; a query that used to take 10 seconds now scans 200,000 files and times out',
    'Silent schema drift: a producer adds a required field without coordinating with the table format\'s evolution rules, causing downstream jobs to fail or, worse, silently coerce the field to null',
    'Concurrent writer conflict: two jobs write to overlapping partitions of the same table simultaneously without the table format\'s optimistic concurrency control, and one writer\'s transaction is silently lost',
    'Metadata log bloat: years of transaction history accumulate in the log without checkpointing, so simply opening the table for a new query takes minutes just to reconstruct current state',
    'Data mesh without contracts: a domain team changes their published schema without notice; every downstream consumer\'s pipeline breaks simultaneously because there was no enforced data contract, only a shared understanding',
    'Catalog drift: the data catalog says a table is refreshed hourly, but the ingestion job has been silently failing for three days; analysts make decisions on stale gold-layer data believing it is current',
  ],
  productionConsiderations: [
    'Schedule compaction as a continuous background process with its own resource budget, not an ad-hoc job run when someone notices slow queries',
    'Alert on schema evolution events and require producer-side review before a breaking change reaches the silver layer',
    'Monitor transaction log size and checkpoint interval; a log that grows faster than it is checkpointed makes every table open progressively slower',
    'Publish freshness SLAs per table in the catalog and alert when actual ingestion lag exceeds the published SLA, not just when the job fails outright',
    'Version data contracts between producer and consumer domains explicitly, with a deprecation window before a breaking schema change takes effect',
    'Vacuum old table versions on a retention policy that balances time-travel utility against object storage cost — unbounded retention is a slow-growing bill, not a free safety net',
  ],
  staffDiscussion: [
    'The lakehouse pattern exists because data lakes solved cost and flexibility but broke correctness guarantees, and warehouses solved correctness but were too expensive and rigid for raw, high-volume, semi-structured data — the lakehouse is a deliberate reconciliation, not just a trend',
    'Data mesh succeeds or fails on organizational maturity, not technology choice; a domain team that publishes data without documented contracts, ownership, or an SLA has adopted the vocabulary of data mesh without its substance',
    'At Staff level the compaction and metadata management strategy deserves the same design rigor as the query patterns themselves — teams that treat compaction as an afterthought inevitably hit a small-file wall that requires an expensive full-table rewrite to escape',
    'Data contracts are the mechanism that makes decentralized data ownership survivable; without them, decentralization just means more places for a breaking change to originate from with less coordination',
    'Choosing an open table format is closer to choosing a database engine than choosing a file format — the decision commits you to that format\'s concurrency model, catalog integration, and query engine compatibility for years',
  ],
  relatedTopics: ['Database selection and sharding', 'Stream processing', 'Platform engineering and developer experience', 'CQRS and event sourcing', 'Technical strategy, RFCs, and ADRs'],
  realWorldSystems: ['Databricks Delta Lake', 'Apache Iceberg on AWS S3', 'Apache Hudi at Uber', 'Snowflake', 'Google BigQuery', 'AWS Glue Data Catalog with dbt transformations'],
  followUpQuestions: [
    'Your compaction job is falling behind streaming ingest and small files are accumulating faster than they are merged. How do you diagnose and fix this without pausing ingestion?',
    'A domain team wants to adopt data mesh for their analytics data. What organizational prerequisites would you require before approving the migration?',
    'How would you design a data contract that catches a breaking schema change before it reaches production consumers, rather than after?',
  ],
  cheatSheet: [
    'Lakehouse = object storage + open table format (Delta/Iceberg/Hudi) for ACID, schema, and time travel on cheap storage',
    'Medallion layers: bronze (raw) → silver (cleaned, schema-enforced) → gold (business-ready aggregates)',
    'Small-file problem is the #1 operational failure — run continuous, non-blocking compaction',
    'Data mesh is an organizational redesign (domain ownership + contracts), not a storage migration',
    'Publish freshness SLA and schema version per table in the catalog; alert on both drift and staleness',
  ],
  flashcards: [
    {
      question: 'What problem do open table formats like Delta Lake and Iceberg solve that a plain data lake on object storage does not?',
      answer: 'Object storage alone offers no transaction guarantee — concurrent writers can corrupt data, and there is no atomic way to update or delete records across many files. Open table formats add a metadata/transaction log layer on top of object storage that provides ACID transactions, schema enforcement and evolution, and time travel, while keeping the underlying data in cheap, scalable object storage.',
    },
    {
      question: 'Why does the small-file problem happen, and why is compaction alone not a sufficient fix if run only occasionally?',
      answer: 'Streaming or micro-batch ingestion writes one small file per batch, and query engines must open every file in a scanned partition — thousands of small files means thousands of file-open operations before any data is read, collapsing query performance. Running compaction only occasionally lets the problem regrow between runs; production systems need continuous, non-blocking compaction that keeps pace with ingestion rate.',
    },
  ],
  oneMinuteAnswer:
    'I would put an open table format — Delta Lake or Iceberg — on top of the existing S3 data, which adds a transaction log giving ACID guarantees, schema enforcement, and time travel without moving the underlying data off cheap object storage. I organize the data through a medallion architecture: bronze holds the raw immutable events, a continuous compaction job merges small streaming-ingest files into right-sized ones, silver enforces a validated schema, and gold holds the pre-aggregated views analysts actually query for sub-minute response times. I partition by ingestion date and Z-order on the columns analysts filter by most, so query engines skip irrelevant data rather than scanning the full week. The data catalog publishes each table\'s owner, schema version, and freshness SLA so analysts can trust what they are querying without asking the platform team directly. Compaction runs as a continuous background job with its own resource budget rather than an ad-hoc fix, because the single most common lakehouse failure is small-file accumulation silently degrading query performance until someone finally notices.',
};

export const doraMetricsEngineeringChapter: EncyclopediaChapter = {
  id: 'dora-metrics-engineering',
  title: 'Engineering metrics and DORA',
  category: 'Leadership',
  summary: 'Use deployment frequency, lead time, change failure rate, and mean time to restore not as dashboard vanity metrics but as evidence for specific investment arguments, while recognizing that any metric which becomes a target invites gaming rather than genuine improvement.',
  problemStatement: 'Engineering leaders report DORA metrics in quarterly reviews without connecting them to any decision, teams game deployment frequency by splitting meaningless commits, and the same metrics dashboard that was supposed to drive investment in developer experience instead becomes a scoreboard nobody trusts and nobody acts on.',
  interviewQuestion: 'Your org has a deployment frequency of once per month and a lead time of 3 weeks. The CTO asks you to improve engineering velocity. How do you diagnose the bottlenecks, what investments do you recommend, and how do you measure whether they worked?',
  coreConcepts: [
    'The four DORA metrics — deployment frequency, lead time for changes, change failure rate, mean time to restore',
    'Elite, high, medium, and low performer benchmarks',
    'SPACE framework — Satisfaction, Performance, Activity, Communication, Efficiency',
    'Toil measurement and quantification',
    'Developer experience (DevEx) surveys',
    'Flow metrics — cycle time, throughput, work in progress',
    'Goodhart\'s Law and metric gaming',
    'Causal versus correlational use of metrics',
    'Metrics as investment arguments, not report cards',
    'Balancing delivery speed metrics with quality metrics',
    'Team-level versus organization-level aggregation',
    'Lagging versus leading indicators',
  ],
  architectureDiagram:
    'flowchart LR\n  D[Diagnose bottleneck] --> H[Form a specific hypothesis]\n  H --> I[Make one investment]\n  I --> M[Measure DORA + SPACE before/after]\n  M --> C{Causal improvement?}\n  C -->|yes| SC[Scale the investment]\n  C -->|no or gamed| D\n  SC --> D',
  solutionApproach: [
    'Measure the four DORA metrics per team, not aggregated across the whole org, since averaging hides which specific teams are actually blocked and by what',
    'Pair every DORA number with a SPACE-framework signal — a fast deployment frequency achieved through developer burnout or unsafe shortcuts is not the improvement it appears to be',
    'Diagnose the actual bottleneck before proposing an investment: a 3-week lead time could be code review latency, a slow CI pipeline, a manual approval gate, or batch size — each has a different fix and metrics alone do not tell you which',
    'Frame every proposed investment as a specific causal hypothesis: "reducing CI runtime from 45 to 10 minutes should cut lead time by roughly X because Y% of lead time is currently CI wait" — not a vague appeal to "improve velocity"',
    'Quantify toil explicitly — hours per week spent on manual, repetitive, automatable work — to build a concrete business case for platform investment rather than relying on anecdote',
    'Track change failure rate and MTTR alongside deployment frequency; a team that ships fast but breaks production frequently is not actually the high performer the raw deployment number suggests',
    'Re-measure after the investment lands and check whether the improvement is causal (matches the hypothesis) or an artifact of gaming (commits got smaller without genuinely reducing batch complexity)',
    'Scale investments that show a real causal improvement to other teams facing the same diagnosed bottleneck, rather than mandating the same fix everywhere regardless of root cause',
  ],
  designPatterns: [
    'DORA four keys — deployment frequency, lead time, change failure rate, MTTR as the core delivery performance metrics',
    'SPACE framework — a counterbalance ensuring speed metrics do not mask burnout or quality erosion',
    'Toil quantification — treating repetitive manual work as a measurable, budgetable cost',
    'Hypothesis-driven investment — every platform investment stated as a testable causal claim before funding it',
    'Leading versus lagging indicator pairing — track an early signal (PR review wait time) alongside the lagging outcome (lead time) so problems surface before the quarter ends',
  ],
  tradeoffs: [
    'Team-level granularity versus reporting overhead: per-team metrics reveal exactly where bottlenecks live but require more dashboards and more nuanced interpretation than a single org-wide number',
    'Metrics as targets versus metrics as diagnostics: publishing a metric as a target invites Goodhart\'s Law gaming; using the same metric purely to diagnose and prioritize investment avoids that but requires more organizational trust and less scoreboard pressure',
    'Speed metrics versus quality metrics: optimizing deployment frequency alone can be achieved by shipping smaller, riskier increments faster; pairing it with change failure rate prevents that but adds measurement overhead',
    'Investment payback horizon versus reporting cadence: platform investments (CI infrastructure, test parallelization) often take a full quarter to show DORA improvement, which conflicts with monthly reporting pressure to show progress',
  ],
  failureScenarios: [
    'Gamed deployment frequency: a team under pressure to improve DORA numbers starts splitting single logical changes into many trivial commits, inflating deployment frequency without actually improving delivery capability or reducing batch risk',
    'Metrics without diagnosis: leadership mandates "improve lead time by 50%" without root-cause analysis; teams respond by skipping code review or lowering test coverage requirements, which improves lead time while silently increasing change failure rate',
    'Averaged-away bottleneck: the org-wide deployment frequency looks healthy because two high-performing teams pull up the average, while the three teams actually blocked by a shared CI bottleneck remain invisible in the aggregate number',
    'Burnout masked by good numbers: DORA metrics look excellent because engineers are working unsustainable hours to hit deployment targets; without a SPACE-style satisfaction signal, this shows up months later as attrition, not in the dashboard',
    'Toil left unquantified: a platform team argues for headcount to reduce manual deployment toil but has no hours-per-week estimate, loses the budget request to a team with a concrete (even if less impactful) revenue-driving proposal',
    'Correlation mistaken for causation: a team observes their deployment frequency rose after adopting a new tool and attributes the entire improvement to the tool, when the actual cause was an unrelated reduction in a different team\'s review bottleneck that happened the same quarter',
  ],
  productionConsiderations: [
    'Instrument DORA metrics automatically from CI/CD and incident systems rather than self-reported surveys, which are prone to inconsistent definitions across teams',
    'Pair every DORA dashboard with a SPACE or developer satisfaction signal collected on a regular cadence, not as a one-time survey',
    'Review metrics with the explicit question "what decision does this number inform" — a metric with no attached decision is overhead, not insight',
    'Separate metrics used for team self-diagnosis from metrics used in performance reviews; conflating the two guarantees gaming behavior',
    'Set a re-measurement date for every platform investment at the time it is approved, so causal impact assessment is planned rather than an afterthought',
  ],
  staffDiscussion: [
    'The Staff-level skill is not knowing what DORA metrics are, but using them to build a specific, falsifiable investment argument that a VP or CTO can approve or reject on evidence rather than on the loudest voice in the room',
    'Metrics reported without an attached decision or hypothesis are a symptom of measurement theater — a mature engineering organization ties every recurring metric review to a live decision it is meant to inform',
    'Goodhart\'s Law is not a hypothetical risk; it is the default outcome of publishing any metric as a target without also monitoring for the specific gaming behaviors that metric invites',
    'Toil quantification is one of the highest-leverage and most underused tools for winning platform investment — a concrete "12 engineer-hours per week across 6 teams" argument outcompetes a vague "this would help productivity" claim almost every time',
    'At Staff level you are expected to distinguish a genuine causal improvement in delivery capability from a metric that moved because teams learned to game the specific number being watched — and to design the measurement approach so that distinction is visible before the org scales the wrong intervention',
  ],
  relatedTopics: ['Platform engineering and developer experience', 'Technical strategy, RFCs, and ADRs', 'Progressive delivery and automated canary analysis', 'SLOs, observability, and incidents'],
  realWorldSystems: ['DORA State of DevOps annual report', 'Google engineering productivity research (SPACE framework origin)', 'GitHub engineering effectiveness team', 'Stripe engineering velocity tracking', 'Spotify squad health checks'],
  followUpQuestions: [
    'A team\'s deployment frequency doubled after a tooling investment, but change failure rate also doubled. How do you interpret this and what do you recommend next?',
    'How would you design a metrics program that resists being gamed once teams know their deployment frequency is being tracked?',
    'The org wants a single engineering health score across 40 teams with very different domains. What is wrong with that goal, and what would you propose instead?',
  ],
  cheatSheet: [
    'Four DORA metrics: deployment frequency, lead time for changes, change failure rate, MTTR — always read speed and stability metrics together',
    'Pair DORA with SPACE (Satisfaction, Performance, Activity, Communication, Efficiency) so fast delivery achieved through burnout is visible',
    'Every metric needs an attached decision — a metric nobody acts on is dashboard theater',
    'Quantify toil in engineer-hours per week to build investment cases that compete with feature work',
    'Goodhart\'s Law: any metric that becomes a target gets gamed — measure for diagnosis, not for scoreboard pressure',
  ],
  flashcards: [
    {
      question: 'Why must deployment frequency always be read alongside change failure rate?',
      answer: 'Deployment frequency alone can be trivially inflated by splitting changes into smaller, more frequent releases without any real improvement in delivery capability, and a team can hit high deployment frequency by shipping riskier, less-reviewed changes. Change failure rate reveals whether that increased speed is actually sustainable — a team with high deployment frequency and high change failure rate is not the elite performer the frequency number alone would suggest.',
    },
    {
      question: 'What is Goodhart\'s Law and why is it central to any engineering metrics program?',
      answer: 'Goodhart\'s Law states that when a measure becomes a target, it ceases to be a good measure — people optimize for the number rather than the underlying goal it was meant to represent. In engineering metrics this means publishing deployment frequency or lead time as an explicit performance target invites teams to game the specific number (splitting commits, skipping review) rather than genuinely improving delivery capability, so metrics are more reliable when used for diagnosis and investment decisions than as targets tied to evaluation.',
    },
  ],
  oneMinuteAnswer:
    'I start by measuring the four DORA metrics per team rather than as an org-wide average, since an aggregate number hides exactly which teams are blocked and by what. Before proposing any fix I diagnose the actual bottleneck — a 3-week lead time could come from code review latency, a slow CI pipeline, or a manual approval gate, and each needs a different investment. I frame the proposed fix as a specific causal hypothesis: reducing CI runtime by a stated amount should reduce lead time by a corresponding amount, given what fraction of current lead time is CI wait — not a vague appeal to improve velocity. I pair the DORA numbers with a SPACE-framework signal so a faster deployment frequency achieved through unsustainable hours or skipped review shows up before it becomes attrition. Toil is quantified in engineer-hours per week to make the platform investment case concrete rather than anecdotal. After the investment lands, I re-measure and check whether the improvement is genuinely causal or an artifact of gaming — smaller, more frequent but equally risky commits inflate deployment frequency without improving real delivery capability. Only investments that show a real causal effect for the diagnosed bottleneck get scaled to other teams facing the same root cause.',
};

export const phase11BChapters: EncyclopediaChapter[] = [
  dataLakehouseChapter,
  doraMetricsEngineeringChapter,
];
