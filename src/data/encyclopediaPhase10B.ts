import type { EncyclopediaChapter } from '../domain/encyclopedia';

export const mlServingInfrastructureChapter: EncyclopediaChapter = {
  id: 'ml-serving-infrastructure',
  title: 'Model serving and inference infrastructure',
  category: 'AI',
  summary: 'Serve trained models reliably at scale — request batching, GPU utilization, model versioning, canary rollout, and cold-start mitigation — the operational discipline that separates a production ML system from a notebook prototype.',
  problemStatement: 'Teams ship a model that works in a notebook, wrap it in a single-request API, and discover in production that GPU utilization sits at 5%, latency spikes under load, and there is no safe way to roll out a new model version without risking a silent quality regression.',
  interviewQuestion: 'Design the inference serving infrastructure for a model that processes 50,000 image classification requests per second with a p99 latency target of 100ms, using GPU clusters that must maintain over 80% utilization.',
  coreConcepts: [
    'Online vs batch inference',
    'Dynamic batching and micro-batching',
    'GPU utilization and memory management',
    'Model versioning and shadow deployment',
    'Canary rollout with metric-based promotion',
    'Cold-start mitigation — warm-up and pre-loading',
    'Multi-model serving (Triton, TorchServe)',
    'Serving latency budget allocation',
    'Model registry and artifact management',
    'A/B testing models in production',
    'Inference cost optimization — quantization, distillation',
    'Traffic routing between model versions',
  ],
  architectureDiagram:
    'flowchart LR\n  C[Client requests] --> Q[Request queue]\n  Q --> B[Dynamic batcher]\n  B --> R{Model registry lookup}\n  R --> V1[Model v1 — 95%]\n  R --> V2[Model v2 canary — 5%]\n  V1 --> GPU1[GPU pool]\n  V2 --> GPU1\n  GPU1 --> M[Metrics: latency, accuracy, drift]\n  M --> G{Canary healthy?}\n  G -->|yes| P[Promote v2 to 100%]\n  G -->|no| RB[Auto-rollback to v1]',
  solutionApproach: [
    'Separate the serving concern from the training concern entirely: the serving layer consumes a versioned model artifact from a registry and knows nothing about how it was trained',
    'Use dynamic batching to convert available latency budget into throughput: accumulate incoming requests for a bounded timeout window (e.g. 10ms) or until a batch-size threshold is hit, whichever comes first, then run one batched GPU inference call',
    'Tune the batching timeout explicitly against the latency budget: a longer timeout increases batch size and GPU efficiency but adds queueing delay to every request, including ones that arrive early in the window',
    'Pre-load and warm up models before they receive live traffic — a cold model on a freshly started GPU worker pays a multi-second first-inference penalty that a warm-up pass against synthetic input eliminates',
    'Treat model deployment identically to code deployment: version every model artifact in a registry, deploy a canary at a small traffic percentage, and gate promotion on both operational metrics (latency, error rate) and model-quality metrics (accuracy, prediction distribution drift)',
    'Route a fixed percentage of production traffic to the canary using the same infrastructure as blue-green code deploys, and automate rollback the instant a quality or latency metric regresses beyond threshold — do not wait for a human to notice',
    'Right-size GPU allocation to actual utilization: a model that averages 5% GPU utilization on a dedicated instance should either be co-located with other models on a multi-model server or moved to CPU serving if latency allows',
    'Apply inference-time cost optimizations — quantization (INT8 or FP16 instead of FP32), knowledge distillation to a smaller model, or ONNX Runtime/TensorRT compilation — only after profiling confirms the accuracy cost is acceptable for the actual production input distribution',
  ],
  designPatterns: [
    'Dynamic batching — accumulate requests within a bounded time window to trade latency for GPU throughput',
    'Shadow deployment — run a new model version against live traffic without serving its output, comparing predictions offline before any user is exposed to it',
    'Canary promotion — small-percentage rollout gated on operational and model-quality metrics with automated rollback',
    'Multi-model server — a single serving process hosts many models, sharing GPU memory and batching infrastructure across low-traffic models',
    'Model registry — the single source of truth for which artifact is deployed where, with full lineage back to the training run',
  ],
  tradeoffs: [
    'Batching timeout versus tail latency: a longer batching window improves GPU efficiency and throughput but adds fixed queueing delay to every request, worsening p50 to improve aggregate cost',
    'Model quantization versus accuracy: INT8 quantization can cut inference cost by more than half, but the accuracy impact is input-distribution dependent and must be validated against production data, not just a benchmark set',
    'Multi-model co-location versus isolation: packing many low-traffic models onto shared GPU infrastructure improves utilization but couples their failure domains and makes per-model capacity planning harder to reason about',
    'Canary traffic percentage versus statistical confidence: a very small canary (1%) limits blast radius but takes longer to reach statistical significance on a quality metric with high variance',
    'Self-hosted GPU serving versus managed inference endpoints: self-hosting gives full control over batching and cost but requires the team to own GPU cluster operations; managed endpoints trade that control for operational simplicity at a per-request cost premium',
  ],
  failureScenarios: [
    'Cold-start latency spike: a new GPU worker joins the pool and receives live traffic before its first warm-up inference completes, causing a multi-second latency outlier that violates the p99 SLA for that request',
    'Head-of-line blocking from an over-tuned batching window: a request arrives one millisecond into a 10ms batching window and waits the full window even though the GPU is otherwise idle, inflating tail latency under low load',
    'Silent model quality regression: a new model version is promoted based on latency and error-rate metrics alone; three weeks later a downstream team notices prediction accuracy has degraded on a specific input segment that the canary\'s traffic sample did not adequately cover',
    'GPU memory fragmentation from co-located models: several models are packed onto one GPU; a memory leak or a large-batch spike in one model causes an out-of-memory error that crashes inference for every other model sharing that device',
    'Registry-serving drift: the model artifact deployed to production is not the one recorded in the registry because a manual deployment bypassed the standard rollout path, making the next incident\'s root-cause analysis start from a false premise',
    'Autoscaling on the wrong signal: the serving fleet scales based on request count rather than GPU queue depth, so a slowdown in per-request inference time (e.g. from a larger average batch of images) causes latency to degrade well before the autoscaler reacts',
  ],
  productionConsiderations: [
    'Monitor GPU utilization per model and per instance as a first-class cost metric — a model with sustained low utilization is either mis-provisioned or a candidate for multi-model co-location',
    'Separate latency dashboards by pipeline stage — queueing time, batching wait, GPU inference time, post-processing — so a regression can be attributed to the correct stage instead of a single opaque end-to-end number',
    'Track prediction distribution drift continuously, not just accuracy against a held-out set — a shift in the distribution of model outputs is often the earliest signal of a problem, visible before ground-truth labels are even available',
    'Automate the canary promotion decision with explicit statistical thresholds rather than a human eyeballing a dashboard; humans are slow to notice a regression and inconsistent about the bar for promotion',
    'Load-test the batching and autoscaling configuration against realistic traffic shape, not synthetic uniform load — real traffic has burstiness that determines actual p99 latency far more than average throughput does',
    'Maintain a rollback path that reverts to the last known-good model artifact within seconds, not minutes — a bad model in production is functionally equivalent to a bad code deploy and deserves the same recovery speed',
  ],
  staffDiscussion: [
    'GPU underutilization is one of the most expensive and most common failure modes in ML infrastructure — a model serving at single-digit utilization on a dedicated accelerator is a budget conversation waiting to surface, and Staff engineers are expected to catch it before finance does',
    'Model deployment is a deployment problem before it is a machine learning problem: the canary, rollback, and versioning discipline that already exists for code deploys should be applied to models without inventing a parallel, weaker process',
    'The batching timeout is a latency-versus-throughput dial that must be set with the actual downstream SLA in mind, not a default value copied from a tutorial — a Staff-level design explicitly derives the timeout from the latency budget',
    'Shadow deployment is underused relative to its value: comparing a new model\'s predictions against production traffic before it ever affects a user is nearly free insurance against a class of regression that canary metrics alone can miss',
    'The question is never "does the model work" but "what is the operational contract this serving system publishes — latency, availability, rollback time — and who is paged when it is violated"',
  ],
  relatedTopics: ['Vector databases and embedding pipelines', 'LLM model router', 'Production RAG', 'Safe delivery and migrations', 'Capacity estimation'],
  realWorldSystems: ['NVIDIA Triton Inference Server', 'TorchServe', 'TensorFlow Serving', 'Seldon Core on Kubernetes', 'BentoML', 'AWS SageMaker Inference'],
  followUpQuestions: [
    'Your GPU cluster shows 85% average utilization but p99 latency has tripled over the last month with no traffic growth. What would you investigate first?',
    'A canary model version passes all automated metric gates but a downstream team reports subtly worse recommendations for a specific user segment two weeks after full rollout. How do you prevent this class of miss in future canaries?',
    'How would you decide whether to co-locate ten low-traffic models on shared GPU infrastructure versus giving each its own dedicated serving pool?',
  ],
  cheatSheet: [
    'Dynamic batching converts latency budget into GPU throughput — tune the timeout against the actual SLA',
    'Treat model deploys like code deploys: registry, canary, automated metric-gated promotion, fast rollback',
    'Warm up every model before live traffic reaches it — cold GPU inference is a multi-second latency outlier',
    'Monitor GPU utilization per model as a cost signal, not just a performance signal',
    'Shadow deployment catches quality regressions canary sampling can miss — compare predictions before exposure',
  ],
  flashcards: [
    {
      question: 'Why does dynamic batching improve GPU throughput, and what does it cost?',
      answer: 'GPUs are far more efficient processing many inputs in one batched matrix operation than one input at a time, so accumulating requests into a batch before running inference dramatically increases throughput per GPU. The cost is added latency: a request that arrives early in the batching window waits for the window to close (or the batch to fill) before being processed, so the batching timeout must be tuned against the actual latency SLA.',
    },
    {
      question: 'What is shadow deployment and what class of failure does it catch that canary deployment does not?',
      answer: 'Shadow deployment runs a new model version against live production traffic and records its predictions without ever serving them to users, comparing the shadow output against the current production model offline. It catches quality regressions on the full traffic distribution before any user is exposed, whereas a canary only samples a small percentage of traffic and can miss a regression concentrated in an underrepresented segment.',
    },
  ],
  oneMinuteAnswer:
    'I separate the serving layer from training entirely — it consumes a versioned artifact from a model registry and has no knowledge of how that artifact was produced. Incoming requests are accumulated by a dynamic batcher with a timeout tuned explicitly against the latency SLA, converting available latency budget into GPU throughput. Every model is warmed up with synthetic inference before it receives live traffic, avoiding the multi-second cold-start penalty. New model versions deploy exactly like code: registered as a new artifact, rolled out as a canary at a small traffic percentage, and promoted only when both operational metrics — latency, error rate — and model-quality metrics — accuracy, prediction distribution — pass automated thresholds, with rollback triggered automatically on regression. Where traffic per model is low, I co-locate multiple models on shared GPU infrastructure using a multi-model server rather than paying for dedicated underutilized accelerators, while watching for the memory-isolation risk that creates. I monitor GPU utilization per model as a cost signal, break down latency by pipeline stage so regressions are attributable, and use shadow deployment to compare a new model against production traffic before any user is exposed — catching the class of subtle quality regression that a small canary sample can miss entirely.',
};

export const vectorDatabasesEmbeddingsChapter: EncyclopediaChapter = {
  id: 'vector-databases-embeddings',
  title: 'Vector databases and embedding pipelines',
  category: 'AI',
  summary: 'Design the vector search infrastructure beneath semantic search and RAG — approximate nearest neighbor indexing, embedding pipeline operations, re-embedding at scale, and hybrid dense-sparse retrieval — as a distinct discipline from the retrieval logic built on top of it.',
  problemStatement: 'Teams treat a vector database as an interchangeable component behind an API call, then discover that index rebuild cost, embedding model upgrades, and recall-versus-latency tuning are each their own operational problem with no obvious owner once the corpus reaches tens of millions of vectors.',
  interviewQuestion: 'Design a vector search infrastructure for a legal document platform with 50 million documents that must support semantic search, exact keyword matching, metadata filtering, and sub-100ms p99 latency.',
  coreConcepts: [
    'Approximate nearest neighbor (ANN) search vs exact search',
    'HNSW — Hierarchical Navigable Small World graph structure',
    'IVF — Inverted File Index with quantization',
    'Product Quantization (PQ) for memory compression',
    'Recall vs latency vs index size tradeoffs',
    'Embedding model versioning and drift',
    'Bulk re-embedding at scale',
    'Hybrid retrieval — dense and sparse BM25 fusion',
    'Metadata filtering with vector search',
    'Vector index sharding and replication',
    'Freshness — incremental vs full re-index',
    'Distance metrics — cosine, dot product, Euclidean',
  ],
  architectureDiagram:
    'flowchart LR\n  D[Documents] --> E[Embedding model]\n  E --> V[(Vector index — HNSW/IVF)]\n  D --> K[(Keyword index — BM25)]\n  Q[Query] --> QE[Query embedding]\n  QE --> ANN[ANN search]\n  Q --> KW[Keyword search]\n  V --> ANN\n  K --> KW\n  ANN --> F[Score fusion]\n  KW --> F\n  F --> MF[Metadata filter]\n  MF --> RES[Ranked results]\n  UPD[Embedding model upgrade] -.triggers.-> RE[Bulk re-embed + shadow index]',
  solutionApproach: [
    'Choose the ANN index structure based on the recall-latency-memory tradeoff the workload actually requires: HNSW for the lowest query latency at higher memory cost, IVF with product quantization when the corpus is too large to fit an HNSW graph in memory affordably',
    'Never update an HNSW graph incrementally at high volume — insertions degrade graph quality over time — instead batch updates and periodically rebuild into a shadow index, validating recall against a held-out query set before cutting traffic over',
    'Combine dense vector search with sparse BM25 keyword search rather than relying on either alone: dense embeddings capture semantic similarity but miss exact term matches (case numbers, statute citations, proper nouns) that BM25 catches natively',
    'Fuse the two retrieval channels with reciprocal rank fusion or a learned score combiner, tuning the fusion weights against a labeled evaluation set specific to the domain rather than using default weights from a library',
    'Apply metadata filters (jurisdiction, document type, date range) as pre-filters on the ANN search when the index structure supports it, or as a post-filter with over-fetching when it does not — the wrong choice either misses relevant results or wastes latency budget',
    'Treat embedding model upgrades as a first-class migration event: an upgraded embedding model produces vectors in a different space that cannot be compared against vectors from the old model, requiring a full corpus re-embed before the new model can be used for retrieval',
    'Batch and rate-limit bulk re-embedding jobs against embedding API cost and rate limits, running them incrementally with checkpointing so a 50-million-document re-embed can resume after a failure without restarting from zero',
    'Validate a new embedding model against the same held-out relevance-judgment set used for the previous model before cutting over, since embedding model upgrades are exactly the kind of change that can silently regress retrieval quality without an explicit before-after comparison',
  ],
  designPatterns: [
    'Shadow index rebuild — build the new index version alongside the serving one, validate recall, then atomically cut traffic over',
    'Hybrid retrieval fusion — combine dense and sparse retrieval scores rather than choosing one exclusively',
    'Pre-filter vs post-filter metadata search — choose based on filter selectivity and index support to avoid over-fetching or under-retrieving',
    'Incremental checkpointed re-embedding — bulk corpus re-embedding jobs that resume from the last committed batch rather than restarting on failure',
    'Tiered index architecture — a smaller, frequently-updated hot index layered over a larger, periodically rebuilt cold index for corpora with high write velocity',
  ],
  tradeoffs: [
    'HNSW versus IVF-PQ: HNSW gives lower query latency and better recall at a given speed but consumes significantly more memory per vector; IVF with product quantization compresses vectors at the cost of recall, becoming necessary once the corpus no longer fits affordably in memory as full-precision HNSW',
    'Recall versus latency: increasing the search parameter (ef in HNSW, nprobe in IVF) improves recall but scans more candidates per query, directly trading latency for result quality',
    'Pre-filtering versus post-filtering metadata: pre-filtering guarantees correct results but can be slow or unsupported at the index level for high-cardinality filters; post-filtering with over-fetching is simpler but risks returning fewer than the requested number of results when the filter is highly selective',
    'Full re-embed versus incremental drift tolerance: re-embedding the entire corpus on every model upgrade guarantees a consistent vector space but is expensive at scale; running two embedding model versions in parallel temporarily is cheaper but complicates result ranking across differently-embedded documents',
    'Managed vector database versus self-hosted: a managed service (Pinecone) removes index-tuning and scaling operations at a per-vector cost premium; self-hosting (Milvus, Qdrant) gives full control over the ANN parameters and hardware but requires the team to own index rebuild and scaling operations directly',
  ],
  failureScenarios: [
    'Graph quality degradation from live insertion: an HNSW index that receives continuous incremental inserts without periodic rebuilding gradually loses recall as new nodes are attached to an aging graph structure, and query quality degrades silently over months with no single triggering event',
    'Embedding space mismatch after a partial migration: an embedding model upgrade is deployed to the ingestion pipeline for new documents but the existing 50 million vectors are never re-embedded, so new and old documents are compared in incompatible vector spaces and relevance ranking becomes effectively random across the migration boundary',
    'Metadata filter over-fetch collapse: a highly selective post-filter (a rare jurisdiction) is applied after retrieving the top-100 nearest vectors, but none of those 100 happen to match the filter, so the user receives zero results despite matching documents existing in the corpus',
    'Re-embedding job cost overrun: a bulk re-embed of 50 million documents is kicked off without rate limiting against the embedding API\'s cost and throughput limits, triggering a five-figure unplanned bill and hitting API rate limits that stall the migration halfway through',
    'Sparse-only miss on paraphrase: a user query uses different phrasing than the source document ("termination clause" versus "early exit provision"); a keyword-only or under-weighted dense retrieval system fails to surface the semantically relevant but lexically distant document',
    'Index shard imbalance: vectors are sharded by a hash that does not account for query access patterns, so one shard receives disproportionate query volume for a popular document category, becoming a latency hot spot invisible in aggregate cluster metrics',
  ],
  productionConsiderations: [
    'Maintain a held-out, human-labeled relevance judgment set for the specific domain and re-run it against every embedding model change, index rebuild, or fusion-weight tuning before deploying to production',
    'Monitor recall against the labeled set continuously in addition to latency — a fast index returning the wrong results is a worse production state than a slightly slower one returning correct results',
    'Rate-limit and checkpoint every bulk re-embedding job against the embedding provider\'s throughput and cost limits, with the ability to resume from the last completed batch after any interruption',
    'Track index rebuild cadence and staleness explicitly — define an SLA for how quickly a newly ingested or updated document becomes searchable, and alert when the actual rebuild lag exceeds it',
    'Instrument shard-level query latency and volume, not just cluster aggregates, to catch hot-spot shards before they become a customer-visible latency problem',
    'Version every embedding model deployment alongside the index it populated, so a retrieval quality regression can be immediately attributed to a specific embedding model version rather than investigated from scratch',
  ],
  staffDiscussion: [
    'The vector index is infrastructure with its own lifecycle — build, validate, cut over, retire — and deserves the same operational rigor as a database migration, not treatment as a black-box API behind a single client call',
    'An embedding model upgrade is one of the highest-blast-radius changes a Staff engineer can approve in an AI system, because it silently invalidates the comparability of every existing vector — the re-embedding migration plan should be designed and reviewed with the same weight as a schema migration on a critical table',
    'Hybrid retrieval is not a nice-to-have refinement; for any corpus containing proper nouns, codes, or exact terminology, dense-only retrieval has a structural blind spot that sparse retrieval closes, and choosing dense-only is usually an unreviewed default rather than a deliberate decision',
    'Recall measurement against a labeled evaluation set is the discipline that turns vector search from "it returns something plausible" into an engineered system with a measurable and improvable quality bar — most teams skip this until a customer complaint forces it',
    'At Staff level the operational question is not which vector database to choose but what the re-index, re-embed, and rollback runbooks look like, and whether the team can execute a full corpus migration without an extended quality regression window',
  ],
  relatedTopics: ['Production RAG', 'Model serving and inference infrastructure', 'Semantic caching', 'Database selection and sharding', 'LLM model router'],
  realWorldSystems: ['Pinecone', 'Weaviate', 'Milvus', 'Qdrant', 'pgvector on PostgreSQL', 'Elasticsearch dense vector fields'],
  followUpQuestions: [
    'Your team wants to upgrade to a new, more accurate embedding model. Walk me through the migration plan for 50 million existing documents without a retrieval quality regression window.',
    'A specific category of query — searches using internal case reference numbers — has degraded from near-perfect to poor recall after a system change. How do you diagnose whether this is an indexing, fusion, or filtering problem?',
    'How would you decide between HNSW and IVF-PQ for a 500-million-vector corpus with a strict memory budget?',
  ],
  cheatSheet: [
    'HNSW: lowest latency, highest memory; IVF-PQ: compressed, lower recall, fits larger corpora affordably',
    'Never incrementally update HNSW at scale — batch into a validated shadow index rebuild instead',
    'Hybrid (dense + sparse) retrieval closes the exact-match blind spot dense-only search has',
    'Embedding model upgrade = full corpus re-embed migration, treated with schema-migration-level rigor',
    'Measure recall against a labeled eval set on every index or model change — not just latency',
  ],
  flashcards: [
    {
      question: 'Why can an HNSW index not simply be updated incrementally forever at high insert volume?',
      answer: 'HNSW builds a multi-layer navigable graph structure where query performance depends on the graph\'s connectivity quality. Continuous incremental insertions gradually degrade that structure without the global optimization a full build provides, causing recall to silently decay over time. Production systems instead batch updates and periodically rebuild into a validated shadow index before cutting traffic over.',
    },
    {
      question: 'Why does upgrading an embedding model require re-embedding the entire existing corpus?',
      answer: 'Different embedding models — or even different versions of the same model — produce vectors in incompatible vector spaces; distances between a vector from the old model and a vector from the new model are not meaningful. Retrieval quality depends on comparing vectors from the same embedding space, so mixing old and new embeddings in one index produces effectively random relevance ranking across the migration boundary until the full corpus is re-embedded consistently.',
    },
  ],
  oneMinuteAnswer:
    'I choose the ANN index structure from the actual recall-latency-memory tradeoff: HNSW when the corpus fits memory affordably and latency is the priority, IVF with product quantization once the corpus is too large for that to hold. I never update the graph incrementally at high volume — updates batch into a shadow index that gets validated against a labeled relevance-judgment set before traffic cuts over. Retrieval combines dense vector search with sparse BM25 keyword search, fused with reciprocal rank fusion, because dense-only retrieval has a structural blind spot on exact terms like case numbers or proper nouns that this specific corpus depends on. Metadata filters are applied as pre-filters where the index supports it and as an over-fetching post-filter otherwise, tuned to the actual selectivity of the filters in use. I treat any embedding model upgrade as a full migration event on the scale of a schema migration — the entire corpus gets re-embedded in a rate-limited, checkpointed batch job, validated against the same relevance set used for the previous model, before the new model ever serves production traffic. Recall against that labeled set, not just latency, is what I monitor continuously, because a fast index returning wrong results is a worse production state than a slower one returning correct results.',
};

export const phase10BChapters: EncyclopediaChapter[] = [
  mlServingInfrastructureChapter,
  vectorDatabasesEmbeddingsChapter,
];
