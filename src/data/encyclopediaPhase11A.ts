import type { EncyclopediaChapter } from '../domain/encyclopedia';

export const timeAndClocksChapter: EncyclopediaChapter = {
  id: 'time-and-clocks',
  title: 'Time, clocks, and ordering',
  category: 'Systems',
  summary: 'Reason correctly about event ordering across machines using logical clocks, hybrid logical clocks, and bounded-uncertainty physical clocks, instead of trusting synchronized wall-clock timestamps.',
  problemStatement: 'Engineers assume NTP-synchronized clocks are close enough to order events reliably, then ship a system where two causally related writes on different machines are timestamped out of order, silently corrupting a last-writer-wins merge or an audit trail.',
  interviewQuestion: 'Two payment events are processed concurrently on different data centers. How do you determine which happened first, and what guarantees can you actually provide?',
  coreTension: 'NTP-synchronized wall-clock timestamps look precise enough to trust, but skew of tens of milliseconds is enough to invert a real causal relationship — the mechanism that looks like ordering is not actually one.',
  levelExpectations: {
    mid: 'Uses wall-clock timestamps to order events across machines. Unaware that NTP skew can invert a real happens-before relationship.',
    senior: 'Chooses Lamport timestamps or vector clocks based on whether total order or concurrency detection is needed. Understands HLC as the practical production default over both raw timestamps and TrueTime.',
    staff: 'Matches the ordering guarantee (none, causal, total) to the actual business requirement rather than defaulting to timestamps, and audits existing systems for silent timestamp-comparison assumptions. Explains why most companies cannot and should not attempt a TrueTime-equivalent.',
  },
  coreConcepts: [
    'Wall-clock time versus monotonic time',
    'NTP accuracy, clock skew, and leap seconds',
    'Happens-before relation (Lamport)',
    'Lamport timestamps for causal ordering',
    'Vector clocks for concurrency detection',
    'Hybrid logical clocks (HLC)',
    'Google TrueTime — GPS and atomic clock bounded uncertainty',
    'Commit-wait in Spanner',
    'External consistency versus causal consistency',
    'Fencing tokens as a practical logical clock',
    'Total order versus partial (causal) order',
    'Clock drift compounding across a distributed system',
  ],
  architectureDiagram:
    'flowchart TD\n  A[Event on node A] -->|Lamport: max(local,recv)+1| LA[Timestamp assigned]\n  B[Event on node B] -->|Lamport: max(local,recv)+1| LB[Timestamp assigned]\n  LA --> C{Compare timestamps}\n  LB --> C\n  C -->|causally related| O[Happens-before order established]\n  C -->|concurrent, same timestamp| V[Vector clock needed to detect concurrency]\n  T[TrueTime interval] --> W[Commit-wait until interval passes]\n  W --> X[External consistency without vector clocks]',
  solutionApproach: [
    'Start by asking what ordering guarantee the use case actually needs — many systems need only causal order (did A happen before B in a chain of communication) rather than a true global total order',
    'Never use raw wall-clock timestamps from independent machines to decide "last write wins" ordering when writes can be causally related — NTP skew of tens of milliseconds is enough to invert a legitimate happens-before relationship',
    'Use Lamport timestamps when you need a total order for events that pass messages to each other and concurrent events can be broken arbitrarily (e.g. assigning a tiebreak by node ID)',
    'Use vector clocks when you specifically need to detect that two events are concurrent (neither happened before the other) rather than force an arbitrary total order — the classic case is multi-master conflict detection',
    'Adopt a hybrid logical clock (HLC) in production systems that want both physical-time-like values (for human debugging and TTL-style logic) and the causal ordering guarantee of a logical clock, without vector clocks\' O(n) size',
    'If you control specialized hardware and need genuine external consistency (a stronger guarantee than causal order — real-time ordering across the whole system), evaluate a bounded-uncertainty clock like TrueTime, understanding it requires atomic clocks and GPS receivers in every datacenter',
    'For a TrueTime-style design, implement commit-wait: a transaction does not report commit until the clock uncertainty interval has definitely passed, guaranteeing that any later-starting transaction on any node sees a strictly later timestamp',
    'Use fencing tokens (a monotonically increasing integer from a single authoritative source) as the pragmatic middle ground for ordering competing operations against shared state, without implementing a full logical clock',
  ],
  designPatterns: [
    'Lamport timestamp — single integer counter incremented on every event and message receipt',
    'Vector clock — per-node counter vector attached to every event for concurrency detection',
    'Hybrid logical clock — physical time component plus logical counter component, combined',
    'TrueTime interval — clock reading is a bounded range, not a point, with explicit uncertainty',
    'Fencing token — a cheap, centrally-issued monotonic counter used to reject stale operations',
  ],
  tradeoffs: [
    'Simplicity versus guarantee strength: raw wall-clock timestamps are simplest but provide no real ordering guarantee; TrueTime provides the strongest guarantee but requires specialized hardware most companies cannot deploy',
    'Vector clock accuracy versus size: vector clocks correctly detect all concurrency but grow linearly with the number of nodes, becoming impractical past a few dozen replicas without pruning',
    'HLC practicality versus purity: HLC does not give the same external consistency guarantee as TrueTime, but is deployable on commodity hardware and NTP, making it the pragmatic default for most teams',
    'Commit-wait latency versus consistency: Spanner\'s commit-wait adds real latency to every write (waiting out the uncertainty interval) in exchange for external consistency — a cost most systems do not need to pay',
    'Fencing token simplicity versus expressiveness: a fencing token correctly orders competing writers against one resource but does not give you general-purpose causal ordering across the whole system the way Lamport or vector clocks do',
  ],
  failureScenarios: [
    'Last-writer-wins corruption: two updates to the same record happen 2ms apart on different machines with 10ms of NTP skew between them; the "later" wall-clock timestamp actually belongs to the causally earlier write, and the wrong value wins permanently',
    'Vector clock explosion: a system with 200 replicas attaches a 200-entry vector to every event; the metadata overhead exceeds the event payload, and the team is forced to prune history, silently losing the ability to detect some concurrency',
    'Stale writer without a fencing token: a lock holder pauses for garbage collection past its lease expiry, another node acquires the lock and writes, then the original writer resumes and overwrites the newer data because it was never told its authority had expired',
    'Leap second discontinuity: a system using raw wall-clock time experiences a leap-second adjustment that makes time appear to go backward for one second, breaking any code that assumes monotonically increasing timestamps',
    'HLC physical component drift: a node with a badly drifted physical clock inflates its HLC physical component, and every event that later synchronizes with it "jumps forward" in logical time even though nothing causally significant happened',
    'False concurrency assumption: a system assumes two events with different Lamport timestamps are ordered, when in fact Lamport timestamps only guarantee that a smaller timestamp does not happen after a larger one — they do not prove the reverse causal claim',
  ],
  productionConsiderations: [
    'Monitor NTP sync status and clock skew per host as a first-class metric — a host silently falling out of NTP sync is a correctness risk, not just an observability gap',
    'Never expose raw wall-clock ordering as an API contract to external consumers; if ordering matters, expose an explicit sequence number or logical clock value instead',
    'Test concurrent-write scenarios explicitly in CI using controlled clock skew injection, since production clock skew is intermittent and easy to miss in normal testing',
    'When adopting vector clocks, implement a pruning or summarization strategy before scale forces an emergency migration',
    'If evaluating a TrueTime-style architecture, budget for the actual hardware requirement (GPS and atomic clock references per datacenter) — this is not a software-only decision',
    'Document explicitly, per API and per data model, what ordering guarantee is actually provided (none, causal, or total) so downstream teams do not assume a guarantee that does not exist',
  ],
  staffDiscussion: [
    'The instinct to reach for "just add a timestamp" is the single most common correctness bug in distributed systems built by teams without a Staff engineer who understands clocks — the fix is recognizing which ordering guarantee is actually required before choosing a mechanism',
    'TrueTime is frequently name-dropped in interviews as if it is a universally available solution; the Staff-level answer explains why most companies cannot and should not build a TrueTime-equivalent, and what the practical HLC or vector-clock alternative looks like',
    'Fencing tokens are underused relative to their cost-to-benefit ratio — they solve the single most dangerous distributed locking bug (a paused writer resuming with stale authority) with a single monotonic counter, far simpler than a general logical clock',
    'At Staff level, auditing an existing system for "silent" timestamp-based ordering assumptions — code that compares two `createdAt` fields across services and assumes the comparison is meaningful — is a recurring and high-value review task',
    'The choice between Lamport timestamps, vector clocks, and HLC is not about which is "better" in the abstract; it is about matching the guarantee to the actual business requirement, since each carries a real operational and complexity cost',
  ],
  relatedTopics: ['Consensus and distributed coordination', 'CAP, PACELC, and consistency', 'Distributed locks and leader election', 'Replication protocols', 'Multi-region active-active architecture'],
  realWorldSystems: ['Google Spanner TrueTime', 'CockroachDB hybrid logical clocks', 'Amazon DynamoDB version vectors', 'Riak vector clocks', 'Cassandra last-write-wins timestamps', 'MongoDB oplog timestamps'],
  followUpQuestions: [
    'Why can Lamport timestamps tell you that event A did not happen after event B, but cannot prove that A happened before B?',
    'What specifically does Spanner\'s commit-wait protect against, and what would break if commit-wait were removed but TrueTime intervals were kept?',
    'Your vector clocks have grown to 500 entries per event after years of node churn. How do you safely prune historical node entries without losing correctness for currently active nodes?',
  ],
  cheatSheet: [
    'Raw wall-clock timestamps across machines are not a safe ordering mechanism — NTP skew can invert real happens-before relationships',
    'Lamport timestamp: total order, cheap, cannot detect concurrency',
    'Vector clock: detects concurrency precisely, O(n) size in node count',
    'HLC: physical-time-like value plus logical counter — the practical production default',
    'TrueTime + commit-wait: strongest guarantee (external consistency), requires atomic clock/GPS hardware',
    'Fencing token: cheap monotonic counter that solves the "stale writer resumes" locking bug specifically',
  ],
  flashcards: [
    {
      question: 'What is the difference between causal ordering and external (total, real-time) consistency?',
      answer: 'Causal ordering guarantees that if event A causally influenced event B (through a chain of communication), every observer sees A before B — but concurrent events with no causal relationship can be observed in any order. External consistency guarantees a single global real-time order for every event in the system, including events with no causal relationship, which requires bounding physical clock uncertainty (as TrueTime does) rather than just tracking message-passing causality.',
    },
    {
      question: 'Why do fencing tokens solve the "paused lock holder" problem that a plain distributed lock does not?',
      answer: 'A plain lock can be held by a process that pauses (garbage collection, scheduling delay) past its lease expiry; another process then acquires the lock and writes, but the original process resumes unaware its lease expired and overwrites the newer write. A fencing token is a monotonically increasing number issued alongside every lock grant; the resource being protected rejects any write carrying a token lower than the highest it has already seen, so the stale writer\'s resumed write is rejected even though it still believes it holds the lock.',
    },
  ],
  oneMinuteAnswer:
    'I first ask what ordering guarantee the use case genuinely requires — most systems need only causal order, not a full real-time total order, and conflating the two leads to over-engineering. I never trust raw wall-clock timestamps from independent machines to order causally related writes, since NTP skew of tens of milliseconds is enough to invert a legitimate happens-before relationship, corrupting a last-writer-wins merge silently. For a total order over communicating events, Lamport timestamps are cheap and sufficient, accepting an arbitrary tiebreak for genuinely concurrent events. When I specifically need to detect concurrency — the classic multi-master conflict case — I use vector clocks, accepting their O(n) size cost in node count. In production, hybrid logical clocks are usually the pragmatic default: they combine a physical-time-like value with a logical counter, deployable on commodity NTP-synchronized hardware, without vector clocks\' overhead. If the business genuinely requires external consistency across the whole system, I would evaluate a TrueTime-style bounded-uncertainty clock with commit-wait, while being clear that this requires atomic clock and GPS hardware most companies cannot deploy. For the narrower and very common problem of a paused lock holder resuming with stale authority, a fencing token — a single monotonically increasing counter — solves it far more cheaply than any general logical clock.',
};

export const chaosEngineeringChapter: EncyclopediaChapter = {
  id: 'chaos-engineering',
  title: 'Chaos engineering as a discipline',
  category: 'Reliability',
  summary: 'Validate system resilience through hypothesis-driven production experiments with a defined steady state, controlled blast radius, and measured outcomes — not random fault injection.',
  problemStatement: 'Teams run ad hoc "let\'s break something and see what happens" exercises with no hypothesis, no steady-state baseline, and no blast radius control, producing either meaningless noise or an actual customer-facing outage with nothing learned from either outcome.',
  interviewQuestion: 'You want to validate that your payment service handles a database primary failure correctly. Design the chaos experiment: hypothesis, steady state, variables, blast radius controls, and how you would measure success.',
  coreTension: 'Without a stated hypothesis and a measured steady-state baseline, chaos engineering is indistinguishable from randomly breaking things — the discipline lives entirely in the measurement, not the fault injection.',
  levelExpectations: {
    mid: 'Can kill a pod or inject latency in staging. Has not defined a hypothesis or steady-state baseline before running the experiment.',
    senior: 'States a falsifiable hypothesis, measures steady state first, and scopes blast radius with an automated abort condition. Verifies the rollback mechanism works before depending on it.',
    staff: 'Treats a divergent result as the primary deliverable, tracked like an incident finding with an owner and remediation deadline. Frames chaos engineering ROI against the cost of the same fragility being discovered during a real incident instead.',
  },
  coreConcepts: [
    'Steady-state hypothesis',
    'Minimal blast radius principle',
    'Controlled experiment versus random fault injection',
    'GameDay design and facilitation',
    'Chaos variables — latency injection, resource exhaustion, network partition, dependency failure, data corruption',
    'Observability as a prerequisite, not an afterthought',
    'Experiment automation versus manual execution',
    'Rollback criteria and abort conditions',
    'Failure injection tooling',
    'Learning from near-misses, not only failures',
    'Chaos engineering maturity model — reactive, proactive, continuous',
    'Cost of chaos versus cost of unknown fragility',
  ],
  architectureDiagram:
    'flowchart TD\n  H[Form hypothesis: system behaves X under condition Y] --> S[Measure steady state]\n  S --> V[Choose minimal chaos variable]\n  V --> B[Define blast radius + abort condition]\n  B --> E[Run experiment — production or prod-like]\n  E --> M[Measure actual behavior]\n  M -->|matches hypothesis| L1[Confidence increases]\n  M -->|diverges| L2[Real weakness found — fix it]\n  L1 --> R[Expand blast radius next round]\n  L2 --> R2[Remediate, then re-test]',
  solutionApproach: [
    'Start every experiment with an explicit, falsifiable hypothesis stated before any fault is injected — "we believe checkout will continue to process orders with a 200ms latency increase when the primary database fails over," not just "let\'s see what happens"',
    'Measure and define the steady state first: the normal-operation metrics (error rate, latency percentiles, throughput) that the experiment will compare against — without this baseline, you cannot tell if the experiment caused a deviation',
    'Choose the smallest chaos variable that can test the hypothesis — inject the specific fault (kill one replica, add 200ms latency to one dependency call) rather than a broad or compound failure that makes attribution impossible',
    'Define blast radius and abort conditions before running anything: which percentage of traffic or which specific canary population is exposed, and the exact metric threshold that triggers an automatic abort and rollback',
    'Verify the rollback mechanism works before running the experiment that depends on it — an untested rollback is the single most common cause of a chaos experiment becoming a real incident',
    'Ensure sufficient observability exists to actually detect the experiment\'s effect at the granularity needed — running a chaos experiment without the metrics to see its impact defeats the purpose and delays incident response if it goes wrong',
    'Run the experiment, starting in a lower-stakes environment or a tightly bounded production canary, and only expand blast radius in subsequent rounds after the smaller round validates the hypothesis and the tooling',
    'Treat a divergence between hypothesis and observed behavior as the primary deliverable — file it as a tracked resilience gap with an owner and a remediation deadline, the same as a production incident finding',
  ],
  designPatterns: [
    'Hypothesis-driven experiment — explicit prediction stated and tested, not exploratory poking',
    'GameDay — a facilitated, time-boxed team exercise running one or more chaos experiments together with incident-response practice',
    'Blast radius control — percentage-based or canary-scoped exposure with automated abort',
    'Steady-state comparison — automated statistical comparison of experiment group against control group',
    'Chaos maturity ladder — start in staging with manual execution, progress to automated continuous production experiments',
  ],
  tradeoffs: [
    'Production realism versus risk: experiments in production reveal real failure modes staging cannot reproduce, but carry real customer-impact risk that staging experiments do not',
    'Experiment frequency versus organizational bandwidth: continuous automated chaos experiments surface regressions fast but require an observability and on-call maturity that not every team has yet',
    'Broad experiments versus attribution: a compound failure scenario (network partition plus disk pressure simultaneously) is more realistic but makes it far harder to attribute which variable caused which observed effect',
    'Comprehensive blast radius controls versus experiment realism: an overly narrow blast radius (one request in a million) may never actually exercise the failure path meaningfully, understating real risk',
    'Manual GameDay facilitation versus automation: manual GameDays build team incident-response muscle memory that pure automation does not, but do not scale to the frequency needed to catch regressions continuously',
  ],
  failureScenarios: [
    'No hypothesis, no learning: a team runs "let\'s kill a pod and see" with no baseline defined; the service recovers, and the team incorrectly concludes resilience is proven, when in fact no measurable claim was actually tested',
    'Untested rollback during a real experiment: the automated fault-injection tool cannot actually reverse the injected latency once the abort threshold triggers, turning a controlled experiment into an unplanned multi-hour incident',
    'Blast radius leak: an experiment scoped to "1% of traffic" actually routes through a shared connection pool that every request depends on, so the fault affects 100% of requests despite the intended scoping',
    'Insufficient observability to detect the effect: an experiment silently degrades a downstream batch job that has no real-time metrics, and the team declares the experiment a success because the dashboards they were watching showed nothing wrong',
    'GameDay without psychological safety: an engineer whose service fails during a GameDay is publicly blamed, so the next quarter\'s participants quietly avoid volunteering their services for testing, and the practice loses its most valuable input — real production services',
    'Chaos theater: experiments run only in a sanitized staging environment that does not resemble production traffic patterns or data volume, producing false confidence that never gets validated against the systems that actually matter',
  ],
  productionConsiderations: [
    'Do not run chaos experiments against a system without first confirming sufficient dashboards and alerting exist to detect the experiment\'s intended and unintended effects at the required granularity',
    'Automate the abort condition rather than relying on a human watching a dashboard — the reaction time needed to prevent real customer impact is often shorter than a manual response allows',
    'Track experiment results the same way as incident postmortems: hypothesis, outcome, divergence, remediation owner, and re-test date',
    'Start the chaos engineering maturity curve in a lower-stakes environment and move to continuous, automated production experiments only after the tooling and team have proven the smaller-scale version repeatedly',
    'Budget engineering time explicitly for remediation of findings — a chaos program that only finds gaps and never funds fixing them will lose organizational support within a few quarters',
    'Rotate which services and teams participate, and treat every result — success or failure — as organizational learning rather than an individual or team performance signal',
  ],
  staffDiscussion: [
    'The discipline is entirely in the hypothesis and the measurement; without a stated prediction and a steady-state comparison, "chaos engineering" is indistinguishable from randomly breaking things, and randomly breaking things is not a defensible practice to bring into production',
    'The most valuable experiments are the ones that disprove the hypothesis — if every experiment confirms what the team already believed, either the experiments are too gentle or the team is not being honest about what it does not know',
    'Blast radius control and a verified rollback mechanism are the non-negotiable prerequisites; a Staff engineer proposing a chaos program should be expected to demonstrate the abort path works before proposing the experiment that depends on it',
    'Chaos engineering maturity is organizational, not just technical — a team\'s willingness to volunteer its service for testing, and to treat a discovered weakness as a learning opportunity rather than a blame event, determines whether the practice survives past its first quarter',
    'At Staff level, the return on chaos engineering is best framed in terms of the cost of the alternative: an unknown fragility discovered during a real incident costs far more — in customer impact and in the postmortem\'s credibility — than the same fragility found in a controlled, hypothesis-driven experiment',
  ],
  relatedTopics: ['SLOs, observability, and incidents', 'Retries, circuit breakers, and bulkheads', 'Progressive delivery and automated canary analysis', 'Multi-region active-active architecture', 'Safe delivery and migrations'],
  realWorldSystems: ['Netflix Chaos Monkey and the Simian Army', 'AWS Fault Injection Simulator', 'Gremlin', 'ChaosToolkit', 'Google DiRT (Disaster Recovery Testing)', 'Microsoft Azure Chaos Studio'],
  followUpQuestions: [
    'Your chaos experiment\'s automated abort condition failed to trigger during a real test, and the experiment ran longer than intended. Walk me through the incident response and what you change in the tooling afterward.',
    'How would you get a reluctant team to volunteer their service for its first chaos experiment, and what would you change about the experiment design to earn that trust?',
    'What is the smallest, lowest-risk chaos experiment you would run first on a system with no prior resilience testing, and why start there?',
  ],
  cheatSheet: [
    'State a falsifiable hypothesis before injecting any fault — "chaos engineering" without one is just breaking things',
    'Measure steady state first; you cannot detect a deviation you never baselined',
    'Choose the smallest chaos variable that tests the hypothesis — compound failures defeat attribution',
    'Define blast radius and automated abort conditions before running anything in production',
    'Verify the rollback works before the experiment that depends on it — untested rollback is the most common cause of chaos-turned-incident',
    'A divergent result is the deliverable — track it like an incident finding, with an owner and a re-test date',
  ],
  flashcards: [
    {
      question: 'What distinguishes chaos engineering from simply "breaking things to see what happens"?',
      answer: 'Chaos engineering requires an explicit, falsifiable hypothesis about system behavior stated before the experiment runs, a measured steady-state baseline to compare against, a deliberately minimal blast radius with a verified rollback, and a systematic process for tracking and remediating any divergence found. Without the hypothesis and the steady-state comparison, there is no way to know whether the system behaved as expected or whether anything was actually learned.',
    },
    {
      question: 'Why is an untested rollback mechanism considered the most dangerous prerequisite gap in a chaos engineering program?',
      answer: 'A chaos experiment intentionally introduces a real fault into a running system. If the abort/rollback mechanism that is supposed to reverse that fault does not actually work when triggered, the "controlled" experiment becomes an uncontrolled incident with no faster path to recovery than a normal outage — except the team caused it deliberately. Verifying the rollback path independently, before running any experiment that depends on it, is the single highest-leverage safety step in the discipline.',
    },
  ],
  oneMinuteAnswer:
    'I start every chaos experiment with an explicit, falsifiable hypothesis — for this scenario, "checkout continues processing orders with an acceptable latency increase when the database primary fails over" — stated before touching anything. I measure the steady-state baseline first: current error rate, p99 latency, and throughput under normal operation, since I cannot detect a deviation I never baselined. I choose the smallest chaos variable that tests this specific hypothesis — actually failing over the primary, rather than a compound scenario that would make attribution ambiguous. Blast radius is scoped to a canary population with an automated abort condition tied to a concrete metric threshold, and critically, I verify the rollback and failover mechanism works independently before running the experiment that depends on it, since an untested rollback is the most common way a chaos experiment turns into a real incident. I confirm sufficient observability exists to actually detect both the intended effect and any unintended blast radius leak at the granularity needed. If the observed behavior matches the hypothesis, confidence increases and I expand scope in the next round; if it diverges — say, checkout actually drops orders during failover — that divergence is the real deliverable, tracked with an owner and a remediation deadline exactly like a production incident finding, not treated as a one-off surprise.',
};

export const phase11AChapters: EncyclopediaChapter[] = [
  timeAndClocksChapter,
  chaosEngineeringChapter,
];
