import type { EncyclopediaChapter } from '../domain/encyclopedia';

export const streamProcessingChapter: EncyclopediaChapter = {
  id: 'stream-processing',
  title: 'Stream processing',
  category: 'Data',
  summary: 'Compute continuous aggregations and joins over unbounded event streams with explicit handling of event time, out-of-order arrival, and exactly-once state updates.',
  problemStatement: 'Teams treat streaming like fast batch: they window by wall-clock arrival time, ignore late events, and checkpoint state inconsistently — producing pipelines that look correct in a demo and silently undercount or double-count in production under real network jitter and late-arriving data.',
  interviewQuestion: 'Design a real-time fraud detection pipeline that processes 500,000 payment events per second with per-user windowed aggregations and sub-second detection latency.',
  coreTension: 'Events arrive out of order in the real world, but correctness requires grouping them by when they happened — so every windowing decision trades completeness (waiting for late data) against latency (firing on time).',
  levelExpectations: {
    mid: 'Can implement a windowed aggregation using processing time. Aware that events might arrive late but treats it as an edge case rather than a design input.',
    senior: 'Windows by event time with watermarks and an explicit allowed-lateness bound. Chooses tumbling/sliding/session windows appropriately and checkpoints state for recovery.',
    staff: 'Treats the allowed-lateness window as a business decision, not an engineering constant, and gets product sign-off on the completeness-versus-latency trade. Designs idempotent sinks assuming at-least-once delivery regardless of engine guarantees, and reasons about failure modes (hot keys, watermark stalls, schema evolution) as a system, not per-bug.',
  },
  coreConcepts: [
    'Event time versus processing time',
    'Watermarks — the engine\'s estimate of "no more events before time T"',
    'Tumbling, sliding, and session windows',
    'Stateful operators and local state stores',
    'Checkpointing and state snapshots',
    'Exactly-once versus at-least-once in streaming engines',
    'Backpressure propagation through the topology',
    'Late event handling and allowed lateness',
    'Stream-table join (enrichment)',
    'Changelog streams for state recovery',
    'Windowed aggregation triggers',
    'Reprocessing and replay from offset',
  ],
  architectureDiagram:
    'flowchart LR\n  S[Source topic] --> P[Partitioned consumer group]\n  P --> W{Windowing + watermark}\n  W -->|on time| AGG[Stateful aggregation]\n  W -->|late, within lateness| AGG\n  W -->|too late| DLQ[Late-event side output]\n  AGG --> CP[(Checkpoint / changelog)]\n  AGG --> SINK[Sink: alert / store / downstream topic]\n  CP -.recover.-> AGG',
  solutionApproach: [
    'Choose event time as the windowing basis, not processing time — a payment event carries its own timestamp, and windowing must group by when it happened, not when it arrived',
    'Configure watermarks with an explicit allowed-lateness bound; a watermark is a heuristic, not a guarantee, so define what happens to events that arrive after the window has fired',
    'Select the window type from the question being asked: tumbling for non-overlapping fixed intervals, sliding for overlapping trend detection, session for activity-gap-based grouping (a user\'s burst of transactions)',
    'Keep aggregation state local to each partition keyed by the same field used for partitioning (user ID) so state stays co-located with the events that update it',
    'Checkpoint state to durable storage (RocksDB + changelog topic in Kafka Streams, or distributed snapshots in Flink) frequently enough that recovery time meets your latency SLA',
    'Design idempotent sinks: a stream processor that restarts from the last checkpoint will reprocess some events, so downstream writes must be safe to repeat',
    'Handle late events explicitly with a side output rather than silently dropping them; route them to correction pipelines or delayed re-aggregation',
    'Load-test with production-realistic out-of-order arrival, not just in-order synthetic data — most streaming bugs only appear under real network jitter',
  ],
  designPatterns: [
    'Windowed aggregation with watermarks',
    'Stream-table join for enrichment (join a fast-moving stream with a slower reference table)',
    'Kappa architecture — single streaming pipeline replaces separate batch and speed layers',
    'Changelog-based state recovery',
    'Side-output routing for late or malformed events',
    'Exactly-once sink via idempotent writes or transactional output',
  ],
  tradeoffs: [
    'Event time versus processing time: event time gives correct results regardless of delivery delay but requires watermarks and adds latency (you wait for the watermark before firing a window); processing time is simpler and lower-latency but wrong under any reordering',
    'Allowed lateness window versus latency: a longer lateness window catches more late events but delays final results and grows state size; a shorter window is faster but drops more legitimately late data',
    'Exactly-once semantics versus throughput: transactional exactly-once (Kafka Streams EOS, Flink two-phase commit) adds coordination overhead; at-least-once with idempotent sinks is often the better trade for high-throughput pipelines',
    'Local state versus external state store: local RocksDB state is fast but ties recovery to changelog replay time; external state stores (Redis, DynamoDB) decouple state from the processor but add network latency per lookup',
    'Streaming versus batch: a stream processor adds real operational complexity (watermarks, checkpointing, state migration) that batch simply does not have; if sub-minute latency is not a real requirement, batch is usually more correct, simpler, and cheaper',
  ],
  failureScenarios: [
    'Clock skew between producers causes events to appear "late" even though they were emitted on time, silently discarding a subset of legitimate events if the watermark is too aggressive',
    'A downstream consumer that is not idempotent double-processes events after a checkpoint-triggered restart, corrupting a financial aggregate',
    'Unbounded session window state grows without limit for a bot or scraper generating continuous activity, eventually exhausting the state store',
    'A schema change to the event payload silently breaks deserialization mid-stream, and the pipeline either crashes or drops all events without alerting',
    'State store recovery after a node failure takes longer than the SLA because the changelog topic was not compacted, forcing replay of the entire history',
    'A hot key (one celebrity user or one instrument) receives disproportionate event volume, overwhelming the single partition responsible for that key\'s state while others sit idle',
    'Watermark stalls when one partition has no traffic for an extended period, blocking the overall pipeline watermark from advancing and delaying all downstream windows',
  ],
  productionConsiderations: [
    'Monitor watermark lag (event time versus wall clock) as the primary correctness signal — a growing lag means the pipeline is falling behind or a partition is stalled',
    'Alert on late-event rate and dropped-event rate separately from processing errors; a rising late-event rate signals a clock or network issue upstream',
    'Test recovery time explicitly: kill a processing node and measure how long state recovery takes from the changelog, then compare against your latency SLA',
    'Version event schemas in a registry and enforce backward compatibility so an in-flight pipeline does not break on a producer deploy',
    'Size state stores and window retention based on the actual key cardinality and traffic pattern, not the average — hot keys dominate state size',
    'Run a canary consumer group in parallel with production during major pipeline changes and compare output before cutting over',
  ],
  staffDiscussion: [
    'The distinguishing Staff-level question is not "can this compute the aggregation" but "what happens to correctness when events arrive out of order, and is that behavior specified or accidental"',
    'Choosing streaming over batch is a latency-versus-complexity trade that should be justified by an actual SLA requirement, not defaulted to because streaming is the more exciting technology',
    'Watermark and lateness configuration is a business decision disguised as an engineering parameter — the allowed lateness window directly trades completeness against latency, and product stakeholders should weigh in',
    'State management (checkpointing, recovery time, state store sizing) is the operational surface that separates a demo pipeline from a production one; most streaming outages are state-recovery incidents, not computation bugs',
    'At Staff level you are expected to reason about the entire pipeline\'s failure modes under partition, late data, and schema evolution simultaneously — not just the happy-path transformation logic',
  ],
  relatedTopics: ['Messaging and delivery semantics', 'CQRS and event sourcing', 'Distributed transactions', 'Capacity estimation', 'SLOs, observability, and incidents'],
  realWorldSystems: ['Kafka Streams at LinkedIn', 'Apache Flink at Alibaba and Uber', 'Google Cloud Dataflow', 'AWS Kinesis Data Analytics', 'Spark Structured Streaming at Databricks'],
  followUpQuestions: [
    'How would you migrate this pipeline from at-least-once to exactly-once semantics without reprocessing the entire event history?',
    'A single user is generating 10,000 events per second and skewing one partition. How do you redesign the keying strategy?',
    'How would you test that your windowed aggregation produces correct results under realistic out-of-order event arrival before deploying to production?',
  ],
  cheatSheet: [
    'Window by event time, not processing time — use watermarks with an explicit allowed-lateness bound',
    'Tumbling = fixed non-overlapping; sliding = overlapping trend; session = activity-gap-based',
    'Checkpoint state to durable changelog; size recovery time against your latency SLA',
    'Idempotent sinks are required — at-least-once delivery means reprocessing after any restart',
    'Streaming is a latency-complexity trade — justify it against an actual SLA, not novelty',
  ],
  flashcards: [
    {
      question: 'What is a watermark in stream processing, and why is it a heuristic rather than a guarantee?',
      answer: 'A watermark is the engine\'s estimate that no more events with an event time earlier than T will arrive. It is a heuristic because the engine cannot know for certain a late event will never arrive — it is configured with an allowed-lateness bound as a deliberate trade-off between completeness and latency, and events arriving after the watermark passes are handled via a side output or dropped.',
    },
    {
      question: 'Why does a stream processor need idempotent sinks even when the engine provides "exactly-once" semantics?',
      answer: 'Exactly-once in engines like Kafka Streams or Flink guarantees exactly-once state updates and internal message processing, but the final write to an external sink (a database, an API call) is outside that transactional boundary in many configurations. A checkpoint-triggered restart can replay events into the sink, so external side effects must be idempotent regardless of the engine\'s internal guarantee.',
    },
  ],
  oneMinuteAnswer:
    'I window by event time using watermarks rather than processing time, because payment events can arrive out of order under real network conditions. I configure an explicit allowed-lateness bound as a deliberate trade-off: events within that window are included in a corrected re-aggregation, events later than that go to a side output rather than being silently dropped. For a per-user fraud aggregation I use a session or sliding window keyed by user ID, keeping state co-located with the partition that owns that key to avoid cross-partition coordination. State is checkpointed to a changelog topic frequently enough that recovery time after a node failure stays within the detection latency SLA. Because checkpoint-triggered restarts cause at-least-once delivery even with engine-level exactly-once guarantees, I design the alerting sink to be idempotent — deduplicating on a stable event ID. I monitor watermark lag as the primary correctness signal and test recovery time and out-of-order handling explicitly before any production deploy, since most streaming incidents are state-recovery or late-event handling failures, not computation bugs.',
};

export const cqrsEventSourcingChapter: EncyclopediaChapter = {
  id: 'cqrs-event-sourcing',
  title: 'CQRS and event sourcing',
  category: 'Architecture',
  summary: 'Separate the write model from read models and, where temporal correctness or audit requirements justify it, make the append-only event log the durable source of truth instead of current-state tables.',
  problemStatement: 'A single normalized table serving both high-throughput writes and diverse read patterns forces every new query shape into schema changes or expensive joins, and a system with only current-state storage cannot answer "what did the balance look like on this date" without a separate, usually inconsistent, audit mechanism.',
  interviewQuestion: 'A financial audit system requires the ability to query an account balance at any point in the past with full traceability of every change. Design the storage and query architecture.',
  coreTension: 'Making the event log the source of truth gives perfect audit and temporal query capability for free, but every projection, snapshot, and schema migration becomes permanent infrastructure that must be operated forever.',
  levelExpectations: {
    mid: 'Understands CQRS as splitting reads and writes. Can sketch a basic event-sourced write path but has not thought through replay cost or projection rebuild.',
    senior: 'Designs aggregates as consistency boundaries, builds multiple projections for different query shapes, and adds snapshotting to bound replay time. Handles idempotent event handlers.',
    staff: 'Justifies event sourcing against a genuine requirement (audit, temporal query, divergent read models) rather than defaulting to it, and is explicit that CQRS alone often solves the actual problem more cheaply. Treats snapshot strategy and schema versioning/upcasting as day-one requirements, not later optimizations.',
  },
  coreConcepts: [
    'Command versus query separation',
    'Write model versus read model (projection)',
    'Event store as the append-only source of truth',
    'Event replay to rebuild projections',
    'Snapshotting to bound replay time',
    'Event schema versioning and upcasting',
    'Idempotent event handlers',
    'Eventual consistency between write and read sides',
    'Saga coordination expressed as event choreography',
    'Event-carried state transfer between services',
    'Temporal (point-in-time) queries',
    'Aggregate boundaries (DDD) as the unit of consistency',
  ],
  architectureDiagram:
    'flowchart LR\n  CMD[Command] --> AGG[Aggregate — validate + emit event]\n  AGG --> ES[(Event store — append only)]\n  ES --> PROJ1[Projection: current balance]\n  ES --> PROJ2[Projection: transaction history]\n  ES --> PROJ3[Projection: audit report]\n  ES -.snapshot.-> SNAP[(Periodic snapshot)]\n  SNAP -.bounds replay.-> AGG\n  PROJ1 --> Q1[Query: current state]\n  PROJ3 --> Q2[Query: point-in-time state]',
  solutionApproach: [
    'Model the domain as aggregates (DDD) where each aggregate is the consistency boundary — a command is validated against the aggregate\'s current state, reconstructed by replaying its events',
    'Persist every state change as an immutable, append-only event rather than overwriting a current-state row; the event log becomes the single source of truth',
    'Build one or more read-model projections from the event stream, each optimized for a specific query shape — current balance, transaction history, audit report — rather than forcing one schema to serve all of them',
    'Answer point-in-time queries by replaying events up to the requested timestamp against a projection, or by maintaining a projection that already indexes by time',
    'Add periodic snapshots of aggregate state so replay does not require reading the entire event history from the beginning — snapshot every N events or every fixed time interval',
    'Version event schemas explicitly and write upcasting logic that transforms old event versions into the current shape when replaying — never mutate historical events',
    'Make every event handler idempotent, keyed by event ID, so replaying the same event during recovery or projection rebuild does not double-apply side effects',
    'Accept eventual consistency between the write side and read projections; define the maximum acceptable staleness per projection and monitor projection lag explicitly',
  ],
  designPatterns: [
    'Command-Query Responsibility Segregation (CQRS)',
    'Event sourcing with append-only event store',
    'Snapshotting to bound replay cost',
    'Saga via event choreography (each service reacts to events and emits its own)',
    'Event-carried state transfer (publish enough data in the event that consumers avoid a callback)',
    'Projection rebuild from event replay (a new read model can be built retroactively)',
  ],
  tradeoffs: [
    'Audit and temporal query capability versus operational complexity: event sourcing gives you a perfect audit trail and point-in-time queries for free, but every projection, snapshot strategy, and schema migration becomes a new piece of infrastructure to operate',
    'Read-model flexibility versus consistency: separate projections let each read pattern have its own optimized schema, but introduce eventual consistency — a write is not immediately visible in every projection',
    'Event schema stability versus evolution: because events are immutable and permanent, a schema mistake lives forever in the log and must be handled via upcasting rather than a simple migration',
    'Replay-based recovery versus snapshot investment: skipping snapshots keeps the system simpler initially but makes rebuilding a projection from scratch increasingly expensive as event volume grows',
    'CQRS without event sourcing versus full event sourcing: you can split read and write models without making the event log authoritative — this captures much of the read-scaling benefit with far less operational cost, and is the right choice when audit and temporal queries are not actual requirements',
  ],
  failureScenarios: [
    'A projection rebuild after a bug fix takes eighteen hours because no snapshotting exists and the event store has grown to hundreds of millions of events',
    'An event schema change ships without upcasting logic, and the projection crashes trying to deserialize old-format events during a routine rebuild',
    'A non-idempotent event handler double-applies a balance-changing event during a projection rebuild, producing an audit report that does not match the ledger',
    'Consumers of "event-carried state transfer" events depend on fields that were only ever meant as a snapshot at publish time, and drift silently from the aggregate\'s true current state',
    'A saga implemented as event choreography has no single service that tracks overall completion, and a partial failure leaves the business process in an undetected stuck state',
    'Two aggregates that should have been modeled as one are updated by racing commands, producing an invalid combined state neither aggregate\'s own validation could have caught',
  ],
  productionConsiderations: [
    'Monitor projection lag (event store position versus projection\'s last-processed position) per projection, and define staleness SLAs per read model',
    'Automate snapshot creation on a schedule tied to event volume, and test full projection rebuild time regularly, not just after an incident forces it',
    'Store event schema version alongside each event and maintain upcasting functions in code, tested against real historical event samples',
    'Treat the event store\'s durability and backup strategy as the highest-priority operational concern in the system — it is the only true source of truth',
    'Instrument saga/choreography flows with a correlation ID across all participating events so a stuck business process can be traced end to end',
    'Build tooling to replay a single aggregate\'s history for debugging without needing to rebuild an entire projection',
  ],
  staffDiscussion: [
    'Event sourcing is justified by specific requirements — audit trail, temporal queries, multiple divergent read models — not by architectural fashion; the first Staff-level judgment is deciding whether CQRS alone (without event sourcing) already solves the actual problem',
    'Snapshot strategy is not an optimization to add later — treat replay-time bound as a first-class non-functional requirement from the start, because retrofitting snapshots onto a production system with years of unsampled event history is a painful migration',
    'Schema evolution is the hardest long-term cost of event sourcing; a team that has not written and tested an upcasting function before their first schema change will discover the real complexity during an incident, not during design',
    'Aggregate boundary design (what belongs in one aggregate versus two) determines your actual consistency guarantees; getting this wrong produces the same "two writers racing" bugs that CQRS was meant to prevent',
    'At Staff level, explaining event sourcing to a skeptical team means being honest about its operational cost — the pitch should never be "it is more scalable" without acknowledging the schema and replay-time burdens that come with it',
  ],
  relatedTopics: ['Distributed transactions', 'Messaging and delivery semantics', 'Database selection and sharding', 'Stream processing', 'Idempotent payment webhooks'],
  realWorldSystems: ['EventStoreDB', 'Axon Framework in Java/Spring ecosystems', 'Kafka used as a durable event log', 'Banking ledger systems with append-only transaction logs', 'E-commerce order-management event streams'],
  followUpQuestions: [
    'How would you migrate an existing CRUD system with years of production data to an event-sourced model without a big-bang cutover?',
    'A new read-model requirement appears that nobody anticipated when the events were first designed. How do you build it without touching historical events?',
    'How do you test that your upcasting logic correctly transforms every historical event version when a projection is rebuilt from scratch?',
  ],
  cheatSheet: [
    'CQRS = split read/write models; event sourcing = event log is the source of truth (can do one without the other)',
    'Snapshot every N events to bound replay time — treat this as a day-one requirement, not an optimization',
    'Version every event schema; write and test upcasting before the first schema change ships',
    'Idempotent event handlers, keyed by event ID — replay must not double-apply side effects',
    'Justify event sourcing with a real requirement: audit trail, temporal query, or genuinely divergent read models',
  ],
  flashcards: [
    {
      question: 'What is the difference between CQRS and event sourcing, and can you have one without the other?',
      answer: 'CQRS separates the write model (commands) from the read model (queries), allowing each to be optimized independently. Event sourcing makes the append-only event log the authoritative source of truth, with current state derived by replaying events. You can implement CQRS with a normal current-state database as the write model — this is common and much lower operational cost. Full event sourcing typically implies CQRS but adds event storage, replay, and snapshotting.',
    },
    {
      question: 'Why is snapshotting a day-one design requirement rather than a later optimization in event sourcing?',
      answer: 'Without snapshots, rebuilding a projection or recovering an aggregate\'s state requires replaying every event from the beginning of that aggregate\'s history. As event volume grows into the millions, replay time grows unboundedly, eventually violating recovery-time SLAs. Retrofitting snapshotting onto a production system with years of unsampled history requires a painful backfill; treating replay-time bound as a first-class requirement from the start avoids this.',
    },
  ],
  oneMinuteAnswer:
    'For a system requiring point-in-time audit queries, I model the domain as aggregates using event sourcing: every state change is an immutable, append-only event, and current state is derived by replaying events rather than overwriting rows. I build separate read-model projections optimized for each query shape — a current-balance projection for fast lookups and a time-indexed projection for point-in-time audit queries — rather than forcing one schema to serve both. To bound replay cost I snapshot aggregate state on a schedule from day one, since retrofitting snapshots onto a large existing event history is expensive. Every event carries an explicit schema version, and I write and test upcasting logic before any schema change ships, because historical events are immutable and must remain readable forever. Event handlers are idempotent, keyed by event ID, so replaying during recovery or projection rebuild never double-applies a balance change. I accept eventual consistency between the write side and projections, with an explicit staleness SLA per projection, and monitor projection lag as the primary operational health signal.',
};

export const microservicesServiceMeshChapter: EncyclopediaChapter = {
  id: 'microservices-service-mesh',
  title: 'Microservices and service mesh',
  category: 'Systems',
  summary: 'Decompose a system along team and domain boundaries rather than technical convenience, then manage inter-service communication, security, and observability through a service mesh once the number of services makes per-service infrastructure code unsustainable.',
  problemStatement: 'Teams split a monolith into services along arbitrary technical lines, producing a distributed system with all the coordination overhead of microservices and none of the team-autonomy benefit, then bolt on service-to-service security and observability inconsistently per service until a mesh becomes the only way to enforce policy uniformly.',
  interviewQuestion: 'Your monolith has grown to encompass roughly 200 services worth of functionality across a 400-engineer organization. Design the decomposition strategy and the service communication infrastructure for the target state.',
  coreTension: 'Service boundaries that maximize team autonomy also multiply the number of network calls, failure modes, and on-call surfaces the organization must operate — decomposition trades coordination cost for distributed-systems cost, it does not eliminate cost.',
  levelExpectations: {
    mid: 'Can split a monolith into services along visible technical seams (a "database service," an "auth service"). Understands service discovery and basic REST communication.',
    senior: 'Decomposes along bounded contexts and team ownership. Chooses sync vs async deliberately, uses strangler fig for incremental migration, and can justify when a service mesh becomes worthwhile.',
    staff: 'Treats decomposition as an organizational design decision (Conway\'s Law) expressed in code, designing target team structure first. Maintains the inter-service call graph as an ongoing artifact and recognizes when a decomposition has gone wrong (a distributed monolith) as readily as designing the original split.',
  },
  coreConcepts: [
    'Bounded context (DDD) as the basis for service boundaries',
    'Synchronous request-response versus asynchronous event-driven inter-service communication',
    'Service registry and discovery — client-side versus server-side',
    'Sidecar proxy pattern',
    'Data plane versus control plane in a service mesh',
    'Mutual TLS (mTLS) between services',
    'Traffic management — canary routing, circuit breaking, retries at the mesh layer',
    'Layer 7 observability — request-level metrics, distributed tracing',
    'Service mesh operational overhead (latency, resource cost)',
    'Conway\'s Law and organizational alignment',
    'Strangler fig decomposition ordering',
  ],
  architectureDiagram:
    'flowchart TB\n  subgraph "Control plane"\n    CP[Mesh control plane — policy, config, certs]\n  end\n  subgraph "Service A pod"\n    A[Service A] <--> SA[Sidecar proxy]\n  end\n  subgraph "Service B pod"\n    B[Service B] <--> SB[Sidecar proxy]\n  end\n  SA <-->|mTLS, retries, circuit breaking| SB\n  CP -.pushes config.-> SA\n  CP -.pushes config.-> SB\n  SA --> OBS[Tracing / metrics collector]\n  SB --> OBS',
  solutionApproach: [
    'Define service boundaries around bounded contexts and team ownership, not around technical layers — a boundary is correct when a single team can deploy independently without coordinating with another team for most changes',
    'Apply Conway\'s Law deliberately: design the target team structure first, then let service boundaries follow team boundaries, rather than drawing a service diagram and hoping teams reorganize around it',
    'Choose synchronous communication only for genuine request-response needs where the caller cannot proceed without an answer; default to asynchronous events for anything that can tolerate eventual processing, to avoid coupling availability across services',
    'Use the strangler fig pattern to decompose incrementally: route a thin vertical slice of functionality to a new service while the monolith still owns everything else, and expand the slice only after the new service is proven in production',
    'Introduce a service mesh once the number of services makes consistent per-service implementation of mTLS, retries, and observability unsustainable — typically past a few dozen services, not from day one with three services',
    'Separate the data plane (per-service sidecar proxies handling actual traffic) from the control plane (centralized policy and configuration distribution) so policy changes do not require redeploying application code',
    'Instrument request-level tracing with a consistent correlation ID propagated through every hop, and treat the mesh\'s L7 metrics as the primary signal for service-to-service health, not just per-service dashboards',
    'Budget for mesh overhead explicitly: sidecar proxies add latency (typically single-digit milliseconds) and resource cost per pod — measure this against the operational consistency gained before adopting a mesh universally',
  ],
  designPatterns: [
    'Sidecar proxy — intercepts all inbound/outbound traffic for a service, implementing cross-cutting concerns without application code changes',
    'Service registry and discovery — services register their location; callers resolve a logical name to a current healthy instance',
    'Strangler fig decomposition — new service absorbs functionality incrementally while old system keeps running',
    'Circuit breaking at the mesh layer — the sidecar trips before the application code needs to implement its own',
    'Canary and traffic-shifting routing at the mesh layer, decoupled from application deployment',
    'Bulkhead isolation per service call, enforced consistently by the mesh rather than per-service library',
  ],
  tradeoffs: [
    'Team autonomy versus coordination overhead: more services along the right boundaries let teams deploy independently, but the same decomposition multiplies the number of network calls, failure modes, and on-call surfaces the organization must operate',
    'Service mesh consistency versus operational and latency cost: a mesh enforces mTLS, retries, and observability uniformly, but every request now traverses two additional proxy hops, and running the mesh control plane is itself a new critical-path system',
    'Synchronous simplicity versus availability coupling: request-response is easier to reason about locally, but couples the caller\'s availability to the callee\'s; async decoupling improves availability at the cost of harder end-to-end debugging',
    'Fine-grained services versus operational blast radius: very small services minimize per-team scope but multiply the number of moving parts an incident responder must reason about during an outage that spans several of them',
    'Mesh adoption timing: adopting a mesh before the service count justifies it adds complexity with no payoff; delaying too long means dozens of services each reimplementing mTLS and retries inconsistently, which is worse',
  ],
  failureScenarios: [
    'Services are split along technical layers (a "database service," a "validation service") rather than business capability, so nearly every feature change requires coordinated deployment across several services — the worst of both monolith and microservice architectures',
    'A synchronous call chain five services deep means one slow downstream service degrades the response time of every upstream caller, and a single service failure cascades into an apparent platform-wide outage',
    'The mesh control plane itself becomes a single point of failure: a bad policy push from the control plane breaks mTLS or routing for every service simultaneously',
    'Service discovery returns a stale or unhealthy instance because health check propagation lags actual instance health, sending traffic into a service that is mid-restart',
    'Two teams both need to change the same shared service\'s contract in incompatible ways in the same release window, revealing that the "team boundary" claimed for that service does not match the actual ownership',
    'Distributed tracing is enabled per-service inconsistently, so a request spanning ten services only has trace spans for six of them, making root-cause diagnosis of a latency spike far slower than in the monolith it replaced',
  ],
  productionConsiderations: [
    'Track the full inter-service call graph and alert when a new synchronous dependency is introduced between services that previously had none — this is how accidental coupling creeps back in',
    'Measure the actual added latency and resource cost of the mesh sidecar per request and per pod, and revisit the decision if the overhead grows disproportionate to the operational benefit',
    'Test control-plane failure scenarios explicitly: what happens to existing traffic when the mesh control plane is unreachable but the data plane sidecars are still running',
    'Require distributed tracing instrumentation as a service onboarding requirement, not an optional add-on, so trace coverage does not degrade as new services are added',
    'Maintain an explicit service ownership map (which team owns which service, and their on-call rotation) and treat any service without a clear owner as a decomposition-boundary bug to fix',
    'Run consistent canary and circuit-breaking policy through the mesh control plane rather than per-service configuration, so a policy change does not require touching every service\'s deployment',
  ],
  staffDiscussion: [
    'Service decomposition is fundamentally an organizational design decision expressed in code — the technical boundary should follow the desired team and ownership structure (Conway\'s Law), not the other way around',
    'The strangler fig ordering — which capability to extract first — is itself a Staff-level judgment call: extract the highest-value, most independently-testable capability first to build organizational confidence before tackling the riskiest coupling',
    'Adopting a service mesh is a build-versus-defer decision that should be justified by the actual pain of inconsistent per-service mTLS and observability implementation, not adopted preemptively because it is the current industry pattern',
    'The real cost of microservices is almost never expressed in any single service\'s code — it is the incident response cost of reasoning about failure across a call graph nobody has fully mapped; Staff engineers are expected to keep and maintain that map',
    'Recognizing when a decomposition has gone wrong — services that must always deploy together, or a "distributed monolith" — and proposing a deliberate re-consolidation is as much a Staff-level skill as the original decomposition',
  ],
  relatedTopics: ['API and protocol selection', 'Load balancing and rate limiting', 'Resilience patterns', 'Technical strategy, RFCs, and ADRs', 'Safe delivery and migrations'],
  realWorldSystems: ['Istio and Envoy at Google and Lyft', 'Linkerd at Buoyant', 'AWS App Mesh', 'HashiCorp Consul Connect', 'Netflix Eureka and Ribbon (pre-mesh service discovery era)'],
  followUpQuestions: [
    'How would you decide which of the 200 service-equivalents to extract from the monolith first, and in what order?',
    'Your service mesh control plane just pushed a bad configuration that broke mTLS between two critical services. Walk me through the incident response and the prevention you would add afterward.',
    'Two services that were supposed to be independently deployable have needed to deploy together for the last six releases. How do you diagnose and fix the underlying coupling?',
  ],
  cheatSheet: [
    'Decompose along bounded contexts and team ownership, not technical layers — apply Conway\'s Law deliberately',
    'Default to async for anything that can tolerate eventual processing; sync only for genuine request-response needs',
    'Strangler fig: extract highest-value, most testable capability first; expand only after production-proven',
    'Adopt a mesh once per-service consistency (mTLS, retries, observability) becomes unsustainable — not from day one',
    'Track the call graph; alert on new synchronous coupling; require trace instrumentation as onboarding, not optional',
  ],
  flashcards: [
    {
      question: 'What problem does a service mesh solve that per-service libraries do not?',
      answer: 'A service mesh implements cross-cutting concerns (mTLS, retries, circuit breaking, L7 observability) once in a sidecar proxy per service, enforced consistently through a central control plane. Per-service libraries require every team to correctly implement, upgrade, and configure the same concerns independently, leading to inconsistent security posture and observability gaps as the number of services grows — the mesh centralizes that policy enforcement.',
    },
    {
      question: 'Why should service boundaries follow team boundaries rather than the reverse?',
      answer: 'Conway\'s Law observes that system architecture mirrors organizational communication structure regardless of intent. If service boundaries are drawn independently of team structure, the services will either force cross-team coordination on every change (negating microservice autonomy benefits) or teams will informally reorganize around the services anyway. Designing the target team structure first and aligning service boundaries to it avoids this friction.',
    },
  ],
  oneMinuteAnswer:
    'I decompose the monolith along bounded contexts that match the target team structure, applying Conway\'s Law deliberately rather than drawing a service diagram and hoping teams reorganize around it. I use the strangler fig pattern, extracting the highest-value and most independently testable capability first to build confidence before tackling the riskiest coupling. Communication defaults to asynchronous events for anything that can tolerate eventual processing, reserving synchronous request-response for genuine query needs, since synchronous chains couple callers\' availability to every downstream service. Once the service count makes per-service mTLS, retries, and observability implementation inconsistent — typically past a few dozen services — I introduce a service mesh, separating the sidecar data plane from a centralized control plane so policy changes do not require redeploying application code. I track the full inter-service call graph and alert on new synchronous dependencies, require distributed tracing as a service onboarding requirement, and budget the mesh\'s added per-request latency explicitly against the operational consistency it buys. The real cost of this architecture is incident response complexity across a call graph, so I maintain that map as an ongoing responsibility, not a one-time design artifact.',
};

export const phase9AChapters: EncyclopediaChapter[] = [
  streamProcessingChapter,
  cqrsEventSourcingChapter,
  microservicesServiceMeshChapter,
];
