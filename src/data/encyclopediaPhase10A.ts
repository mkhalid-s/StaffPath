import type { EncyclopediaChapter } from '../domain/encyclopedia';

export const kubernetesForEngineersChapter: EncyclopediaChapter = {
  id: 'kubernetes-for-engineers',
  title: 'Kubernetes patterns for engineers',
  category: 'Systems',
  summary: 'Reason about how production workloads actually run on Kubernetes — scheduling, resource limits, stateful workloads, and the operator pattern — not how to operate a cluster.',
  problemStatement: 'Engineers treat resource requests and limits as performance tuning knobs rather than safety mechanisms, deploy stateful workloads as ordinary Deployments, and are blindsided when a "healthy" cluster is silently throttling or killing their pods under normal load.',
  interviewQuestion: 'Your service is experiencing intermittent latency spikes in production. The Kubernetes cluster appears healthy, CPU utilization is normal, and no errors are logged. How do you diagnose and resolve this?',
  coreConcepts: [
    'Pod lifecycle and restart policies',
    'Resource requests versus limits (CPU throttling versus OOMKill)',
    'QoS classes — Guaranteed, Burstable, BestEffort',
    'Node affinity and pod anti-affinity',
    'StatefulSets versus Deployments for stateful workloads',
    'PersistentVolumes and storage classes',
    'Horizontal Pod Autoscaler and custom metrics',
    'Operators and CRDs as the extension pattern',
    'Network policies for micro-segmentation',
    'Readiness, liveness, and startup probes',
    'ConfigMaps and Secrets management',
    'Pod Disruption Budgets for safe node drains',
  ],
  architectureDiagram:
    'flowchart TD\n  S[Scheduler] --> N{Node selection}\n  N --> P[Pod: requests + limits]\n  P --> Q{QoS class}\n  Q -->|Guaranteed| G[req = limit, no throttle risk]\n  Q -->|Burstable| B[req < limit, throttle risk]\n  Q -->|BestEffort| E[no limits, evict first]\n  P --> R{Readiness probe}\n  R -->|fail| X[Removed from Service endpoints]\n  R -->|pass| SVC[Receives traffic]\n  P --> L{Liveness probe}\n  L -->|fail| RS[Container restarted]\n  ST[StatefulSet] --> PV[(PersistentVolume per replica)]\n  ST --> ID[Stable network identity]',
  solutionApproach: [
    'Set resource requests to the actual steady-state usage measured from production telemetry, not a guess — this determines scheduling and QoS class',
    'Set limits deliberately: for latency-sensitive services, set CPU limit equal to request (Guaranteed QoS) to avoid throttling; for memory, size the limit above peak usage with headroom, since exceeding it triggers an immediate OOMKill rather than graceful degradation',
    'Distinguish readiness (safe to receive traffic) from liveness (process is alive but may be stuck) from startup (slow-starting container needs a longer initial grace period) — conflating them causes premature restarts or traffic sent to unready pods',
    'Use StatefulSets, not Deployments, for any workload requiring stable network identity, ordered startup/shutdown, or a dedicated PersistentVolume per replica — databases, brokers, and consensus-based systems all need this',
    'Configure Pod Disruption Budgets before any workload goes to production, so voluntary disruptions (node drains, cluster upgrades) cannot take down more replicas than the service can tolerate simultaneously',
    'Use pod anti-affinity to spread replicas across failure domains (nodes, availability zones) so a single node or zone failure does not remove all replicas of a service at once',
    'Adopt the Operator pattern for any stateful system with non-trivial operational procedures (backup, failover, scaling) — encode the runbook as a reconciliation loop instead of a wiki page',
    'Apply network policies as default-deny with explicit allow rules between services that actually communicate — the default Kubernetes network is fully open between pods, which is rarely the intended security posture',
  ],
  designPatterns: [
    'Operator pattern — a custom controller that encodes operational knowledge (backup, scaling, failover) as a reconciliation loop against a Custom Resource',
    'Sidecar container — a helper container in the same pod sharing network and storage namespace (used by service meshes, log shippers, and secret injectors)',
    'Init container — runs to completion before the main container starts, used for schema migrations or dependency readiness checks',
    'StatefulSet with headless Service — provides stable per-replica DNS names and ordered, sequential pod management',
    'Pod Disruption Budget — bounds how many replicas can be voluntarily disrupted at once during maintenance operations',
  ],
  tradeoffs: [
    'Guaranteed QoS versus resource utilization: setting CPU limit equal to request eliminates throttling risk but reduces the scheduler\'s ability to bin-pack workloads efficiently across nodes',
    'StatefulSet operational complexity versus correctness: StatefulSets are harder to reason about during scaling and upgrades but are the only correct choice when replica identity matters',
    'Operators reduce operational toil but add a new failure mode: a buggy operator can take destructive action across every instance of the resource it manages simultaneously',
    'Strict network policies improve security posture but increase the debugging surface — a connectivity failure could now be caused by policy misconfiguration rather than the application itself',
    'Aggressive liveness probes restart genuinely stuck processes quickly but can create restart loops if the probe itself is too strict or the dependency it checks is transiently unavailable',
  ],
  failureScenarios: [
    'A pod has generous memory limits but requests set far below actual usage: the scheduler over-packs the node believing it has capacity, and under real load multiple pods compete for memory, triggering OOMKills across supposedly unrelated services',
    'CPU limit is set below actual need for a latency-sensitive service: the container is throttled by the kernel CFS scheduler every period, producing periodic latency spikes with no visible errors and normal-looking average CPU utilization',
    'A StatefulSet-appropriate workload is deployed as a Deployment: during a rolling update, multiple replicas briefly claim the same identity or PersistentVolume, corrupting shared state',
    'Liveness probe checks a downstream dependency instead of process health: a transient database blip causes the orchestrator to restart every replica simultaneously, turning a minor blip into a full outage',
    'No Pod Disruption Budget is configured: a routine node drain during a cluster upgrade removes all replicas of a service at once because nothing constrained the eviction rate',
    'Network policy is added without auditing existing traffic first: a default-deny policy silently breaks a legitimate but undocumented service-to-service dependency, causing a partial outage that is hard to attribute to the policy change',
  ],
  productionConsiderations: [
    'Monitor CPU throttling (container_cpu_cfs_throttled_seconds_total) as a first-class signal — it is invisible in average CPU utilization dashboards but directly explains latency spikes',
    'Alert on OOMKill events per workload; a recurring OOMKill on a Burstable pod means the memory limit is undersized for real production behavior',
    'Test node drain and cluster upgrade procedures in a staging environment with production-representative Pod Disruption Budgets before relying on them in production',
    'Audit and document actual pod-to-pod traffic before introducing default-deny network policies; retrofit policies from observed traffic rather than guessing',
    'Version and test Operator upgrades as carefully as application deployments — an Operator bug has blast radius across every instance of the resource type it manages',
    'Separate readiness probe logic from liveness probe logic explicitly in code review — a probe that checks the same condition for both purposes usually indicates a design mistake',
  ],
  staffDiscussion: [
    'Resource limits are a safety mechanism against a single misbehaving workload consuming shared infrastructure, not a lever for squeezing more density out of a cluster — conflating the two produces silent production degradation',
    'The gap between "cluster is healthy" and "workload is healthy" is where most Kubernetes production incidents live; node-level and cluster-level health dashboards routinely miss per-pod throttling and eviction pressure',
    'Choosing StatefulSet versus Deployment is a correctness decision, not a preference — the question is whether replica identity and ordering matter to the workload\'s invariants, and getting this wrong corrupts data rather than merely degrading performance',
    'Operators are a Staff-level investment: they encode institutional operational knowledge into code, but a Staff engineer must also own the operator\'s own reliability, since a bug there has platform-wide blast radius',
    'At Staff level you are expected to reason about Kubernetes as a scheduling and resource-isolation substrate, not a black box — being able to explain exactly why a "healthy" cluster produces an unhealthy service is a core diagnostic skill',
  ],
  relatedTopics: ['Load balancing and rate limiting', 'Platform engineering and developer experience', 'Resilience patterns', 'Capacity estimation', 'Safe delivery and migrations'],
  realWorldSystems: ['Google Borg (the internal predecessor to Kubernetes)', 'Strimzi Operator for Kafka on Kubernetes', 'Zalando PostgreSQL Operator', 'Kubeflow for ML training on Kubernetes', 'Amazon EKS, Google GKE, and Azure AKS managed control planes'],
  followUpQuestions: [
    'How would you migrate a stateful database workload from a hand-managed VM deployment to a StatefulSet on Kubernetes without data loss?',
    'A new default-deny network policy just broke a production service. Walk me through your rollback and root-cause process.',
    'When would you write a custom Operator versus using a Helm chart with a simple Deployment?',
  ],
  cheatSheet: [
    'CPU limit = request for Guaranteed QoS on latency-sensitive workloads — throttling is invisible in average CPU metrics',
    'StatefulSet for anything needing stable identity, ordered rollout, or per-replica storage — never a Deployment',
    'Readiness ≠ liveness ≠ startup — conflating them causes premature restarts or traffic to unready pods',
    'Pod Disruption Budgets before production — otherwise a routine node drain can remove every replica at once',
    'Default-deny network policy only after auditing real traffic — retrofit from observed dependencies, not assumption',
  ],
  flashcards: [
    {
      question: 'Why can a Kubernetes cluster show normal average CPU utilization while a service experiences latency spikes?',
      answer: 'CPU throttling operates on a per-period basis (typically 100ms) enforced by the kernel CFS scheduler when a container exceeds its CPU limit within that period — this produces latency spikes invisible in average utilization metrics measured over longer windows. The correct signal is container_cpu_cfs_throttled_seconds_total, not average CPU percentage.',
    },
    {
      question: 'Why must a database on Kubernetes use a StatefulSet instead of a Deployment?',
      answer: 'StatefulSets provide stable, unique network identities and ordered, sequential pod creation/termination, along with a dedicated PersistentVolume per replica that survives rescheduling. A Deployment provides none of these guarantees — replicas are interchangeable and unordered, which can cause a database cluster to briefly have duplicate node identities or lose the binding between a replica and its data during a rolling update.',
    },
  ],
  oneMinuteAnswer:
    'I start by separating cluster-level health from pod-level health — a cluster dashboard showing normal average CPU can hide per-pod CPU throttling, which occurs on a per-period basis and produces exactly the kind of intermittent latency spike with no errors described here. I would check container_cpu_cfs_throttled_seconds_total for the affected pods first. If throttling is occurring, the CPU limit is set too low relative to actual burst behavior; I would either raise the limit to match request (Guaranteed QoS) for this latency-sensitive workload or increase both request and limit based on real usage percentiles. I would also check whether readiness and liveness probes are correctly separated, since a probe checking a flaky downstream dependency for liveness can cause unnecessary restarts that look like latency spikes externally. More broadly, I treat resource requests and limits as safety mechanisms informed by production telemetry, not performance tuning guesses, and I make sure Pod Disruption Budgets and anti-affinity rules are in place so routine node maintenance cannot silently remove enough replicas to cause the symptom I am diagnosing.',
};

export const distributedLocksLeaderElectionChapter: EncyclopediaChapter = {
  id: 'distributed-locks-leader-election',
  title: 'Distributed locks and leader election in practice',
  category: 'Systems',
  summary: 'Implement distributed locks and leader election correctly using lease-based coordination and fencing tokens, and recognize when a distributed lock is the wrong tool entirely.',
  problemStatement: 'Engineers reach for a distributed lock to protect shared state without bounding how long it can be held, without a fencing mechanism to invalidate a slow or paused holder, and often without checking whether the protected operation could simply be made idempotent instead — eliminating the need for a lock altogether.',
  interviewQuestion: 'Design a distributed job scheduler where each job must run on exactly one worker node, even during node failures and network partitions.',
  coreConcepts: [
    'Lease-based locks versus session-based locks',
    'Fencing tokens for STONITH (Shoot The Other Node In The Head)',
    'etcd and ZooKeeper lock recipes',
    'Split-brain and the dual-master problem',
    'The Redlock algorithm and its criticisms',
    'Epoch numbers and generation counters',
    'Leader election via watch semantics',
    'Lock TTL and session expiry',
    'Fail-open versus fail-closed on lock store failure',
    'Bounded lock hold time',
    'Idempotent operations as an alternative to locking',
    'Single-leader versus multi-leader designs',
  ],
  architectureDiagram:
    'flowchart TD\n  W1[Worker 1] -->|acquire lease| LS[(Lock service: etcd/ZK)]\n  W2[Worker 2] -->|acquire lease| LS\n  LS -->|grants token=42| W1\n  W1 -->|holds lock, slow GC pause| PAUSE[Paused past lease TTL]\n  LS -->|lease expires| W2\n  LS -->|grants token=43| W2\n  W2 --> DS[(Shared resource)]\n  PAUSE -.resumes, writes with stale token=42.-> DS\n  DS -->|rejects: 42 < 43| REJECT[Write rejected by fencing check]',
  solutionApproach: [
    'Before reaching for a distributed lock, check whether the protected operation can be made idempotent instead — an idempotent write with a unique operation ID often eliminates the need for mutual exclusion entirely',
    'When a lock is genuinely required, use a lease-based lock with a bounded TTL rather than an indefinite hold — the lock service (etcd, ZooKeeper) automatically releases the lease if the holder fails to renew it within the TTL',
    'Always issue a fencing token (a monotonically increasing integer) alongside lock acquisition, and require every write to the protected resource to include and validate this token — the resource rejects any write carrying an older token than one it has already seen',
    'Do not treat lock acquisition failure as exceptional — under partition or contention it is the normal operating condition, and the caller must have a defined behavior (retry, back off, or abandon) rather than assuming acquisition always succeeds quickly',
    'For leader election, use the lock service\'s native primitives (etcd leases with watch, ZooKeeper ephemeral sequential nodes) rather than building a custom heartbeat protocol — these primitives already handle the split-brain-prevention logic correctly',
    'Decide and document the fail-open versus fail-closed behavior when the lock service itself becomes unavailable: fail-closed halts all protected operations (safe but reduces availability); fail-open allows uncoordinated writes to proceed (available but risks the exact corruption the lock existed to prevent)',
    'Bound the maximum time any process can legitimately hold a lock, and alert when a holder approaches that bound — an unusually long hold time is itself a signal of a stuck or degraded process, not just an operational curiosity',
    'For a distributed job scheduler specifically, combine leader election (one node owns scheduling decisions) with per-job fencing tokens (so a job cannot be executed twice even if the scheduler leader changes mid-dispatch)',
  ],
  designPatterns: [
    'Fencing token — a monotonically increasing number issued with each lock grant; the protected resource rejects any operation carrying a token older than the highest it has seen',
    'Lease with TTL and renewal — the lock is automatically released if the holder fails to heartbeat within the TTL, bounding worst-case unavailability',
    'Ephemeral sequential node (ZooKeeper) — each candidate creates a sequential node, and the lowest-numbered node becomes the leader, avoiding herd-effect on failover',
    'Watch-based leader election — clients watch the current leader\'s key and are notified immediately when it disappears, rather than polling',
    'Compare-and-swap on a versioned key — an alternative to full locking for single-key updates, avoiding lock acquisition overhead entirely',
  ],
  tradeoffs: [
    'Lease TTL length: a short TTL detects failed holders quickly but risks premature expiry under transient network delay or GC pauses, potentially granting the lock to a second holder while the first is still legitimately working',
    'Fail-open versus fail-closed during lock service outage: fail-closed protects correctness at the cost of availability; fail-open protects availability at the cost of the exact race condition the lock was meant to prevent',
    'Building on a general-purpose lock service (etcd, ZooKeeper) versus a purpose-built consensus system: general-purpose services are operationally simpler to run but were not always designed with locking\'s specific latency and fencing requirements in mind',
    'Fencing token adoption cost: fencing only works if every writer to the protected resource actually validates the token, which requires touching every write path — a partial rollout provides no real protection',
    'Idempotency versus locking: making an operation idempotent is often more work upfront than acquiring a lock, but it eliminates an entire class of coordination failures permanently rather than just mitigating them',
  ],
  failureScenarios: [
    'A lock holder experiences a long garbage collection pause or network partition, its lease expires, a second process acquires the lock and begins working, and the first process resumes and writes to the shared resource unaware its lock is gone — without fencing tokens, both writes succeed and corrupt state',
    'The Redlock pattern is used across multiple independent Redis instances, but a process pause between acquiring the majority of locks and using them means the lock could have already expired by the time the protected operation actually executes — the algorithm cannot bound this gap',
    'A ZooKeeper session times out due to a garbage collection pause on the client, the ephemeral node is deleted and leadership transfers to another candidate, but the original process\'s GC pause ends and it continues believing it is still the leader',
    'A lock service becomes unavailable during a network partition; the system is configured to fail-open, and two previously coordinated workers now both act as leader simultaneously, each unaware of the other, producing a split-brain write conflict',
    'A lease TTL is set too short relative to realistic processing time under load; under a traffic spike, legitimate lock holders are repeatedly evicted mid-operation, causing the lock to thrash between candidates and no work to complete',
    'A team implements a hand-rolled heartbeat-based leader election without a fencing mechanism, and a slow leader that has been deposed continues issuing writes that a faster new leader has already superseded, silently overwriting more recent correct state with stale data',
  ],
  productionConsiderations: [
    'Monitor lock hold time distribution and alert on outliers approaching the configured TTL — an unusually long hold is an early signal of a stuck process before it causes a lease-expiry race',
    'Load-test lock acquisition and lease renewal under realistic contention and network latency, not just the happy path — most distributed lock bugs only manifest under partition or GC pause',
    'Require every write path to the protected resource to validate fencing tokens in code review; audit for write paths that bypass the fencing check entirely',
    'Explicitly test and document the fail-open versus fail-closed behavior for every lock-dependent system, and verify it matches the actual business risk tolerance rather than a default the lock library happened to ship with',
    'Instrument leader election transitions (leader changes, split-brain detection) as first-class incident-relevant events, not just debug logs — frequent unexpected leader changes indicate lease tuning or network problems worth investigating before they cause data corruption',
  ],
  staffDiscussion: [
    'The Staff-level question is not "how do I implement a distributed lock" but "does this operation actually need mutual exclusion, or can I make it idempotent instead" — the latter is usually the more durable and simpler solution',
    'Fencing tokens are the detail that separates a correct distributed lock from a dangerous one; a lock without fencing provides the illusion of safety while leaving the exact race condition it was meant to prevent fully open',
    'The Redlock controversy (Kleppmann versus Antirez) is instructive less for who was "right" and more for demonstrating that most engineers reaching for a distributed lock have not precisely defined what guarantee they actually need — safety under asynchrony requires more than a majority-vote lock acquisition',
    'Choosing fail-open versus fail-closed behavior during a lock service outage is a business risk decision as much as a technical one, and should be made explicitly and documented, not left as whatever the client library defaults to',
    'At Staff level you are expected to recognize when a proposed distributed lock design is solving the wrong problem — often the actual requirement is exactly-once execution, which is better solved with idempotency keys and deduplication than with mutual exclusion',
  ],
  relatedTopics: ['Consensus and distributed coordination', 'Messaging and delivery semantics', 'Resilience patterns', 'Microservices and service mesh', 'Distributed transactions'],
  realWorldSystems: ['ZooKeeper leader election in Kafka (legacy) and HBase', 'etcd as the Kubernetes control-plane leader election and coordination store', 'Chubby at Google (the original inspiration for ZooKeeper)', 'Redis Redlock (widely used but contested for strict correctness guarantees)', 'DynamoDB TTL-based lease patterns for distributed coordination'],
  followUpQuestions: [
    'How would you retrofit fencing tokens into a system that currently uses a distributed lock without them, without a full outage?',
    'Your lock service just became unavailable during a regional network partition. Walk me through what happens to in-flight operations and how you designed for this.',
    'When would you choose idempotent operation design over a distributed lock, and what would change your mind back toward locking?',
  ],
  cheatSheet: [
    'Before locking, ask: can this be made idempotent instead? Often yes, and it is more durable',
    'Fencing token = monotonically increasing integer issued with the lock; resource rejects any older token',
    'Lease TTL bounds worst-case unavailability; too short causes premature eviction, too long delays failover',
    'Decide fail-open vs fail-closed on lock-service outage explicitly — do not inherit the library default',
    'A lock without a fencing check is not actually safe — it only reduces the probability of the race, not eliminates it',
  ],
  flashcards: [
    {
      question: 'What problem does a fencing token solve that a lease-based lock alone does not?',
      answer: 'A lease-based lock can expire while its holder is still alive but paused (GC pause, network delay), allowing a second process to acquire the lock and begin working. When the first process resumes, it may still write to the shared resource unaware its lock has expired. A fencing token — a monotonically increasing number issued with each lock grant — lets the shared resource reject any write carrying an older token than one it has already accepted, safely invalidating the stale holder\'s writes even though the holder itself does not know it lost the lock.',
    },
    {
      question: 'Why did the Redlock algorithm become controversial, and what is the core critique?',
      answer: 'Martin Kleppmann\'s critique of Redlock is that it provides no bound on the time between acquiring the lock and using it — a process can experience a pause after successfully acquiring a majority of Redis locks but before performing its protected operation, and the lock may have already expired by the time the operation actually executes. Redlock addresses distributed consensus on lock ownership but does not address process pauses, making it insufficient for use cases requiring strict correctness guarantees (e.g. preventing double-writes to a file), though it remains useful as an efficiency optimization where occasional violations are tolerable.',
    },
  ],
  oneMinuteAnswer:
    'I combine leader election with per-job fencing rather than relying on locking alone. One node is elected leader using the lock service\'s native primitives — etcd leases with watch, or ZooKeeper ephemeral sequential nodes — which correctly handle the split-brain-prevention logic I would not want to reimplement. The leader owns scheduling decisions and dispatches jobs to workers, but critically, each job dispatch carries a fencing token from the leader\'s current lease generation. Workers and any shared state store validate that token before accepting a job\'s result, so even if the scheduler leader changes mid-dispatch — due to a network partition or a paused leader resuming late — a stale dispatch cannot cause the job to run twice or have its result accepted out of order. I bound the leader\'s lease TTL based on realistic scheduling latency under load, monitor lease renewal and leader-change frequency as production signals, and explicitly decide and document whether the system fails open or closed if the coordination store itself becomes unavailable during a partition, since that is a business risk decision, not just a technical default.',
};

export const phase10AChapters: EncyclopediaChapter[] = [
  kubernetesForEngineersChapter,
  distributedLocksLeaderElectionChapter,
];
