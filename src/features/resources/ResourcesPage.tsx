import { useMemo, useState } from 'react';
import { learningResources } from '../../data/resources';

export function ResourcesPage() {
  const [query, setQuery] = useState('');
  const results = useMemo(() => { const terms = query.toLowerCase().split(/\s+/).filter(Boolean); return learningResources.filter((item) => terms.every((term) => `${item.title} ${item.provider} ${item.domain} ${item.purpose}`.toLowerCase().includes(term))); }, [query]);
  return <div className="page resources-page"><div className="page-heading"><div><p className="eyebrow">CURATED PRIMARY REFERENCES</p><h1>Resource library</h1><p>Use references for depth and verification; use StaffPath to apply, explain, and retain what matters.</p></div><div className="chapter-count"><strong>{learningResources.length}</strong><span>authoritative resources</span></div></div><div className="search-panel"><label><span>⌕</span><input aria-label="Search resources" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search reliability, networking, AI, leadership…" /></label></div><div className="resource-grid">{results.map((item) => <a href={item.url} target="_blank" rel="noreferrer" key={item.url}><div><span>{item.domain}</span><em>{item.type}</em></div><h2>{item.title}</h2><strong>{item.provider}</strong><p>{item.purpose}</p><b>Open primary source ↗</b></a>)}{!results.length && <div className="interview-empty">No resources match that search.</div>}</div><section className="resource-policy"><strong>Editorial policy</strong><p>Company-specific claims use authoritative public sources. Product brands illustrate capabilities, not memorization. “Exactly once,” body-language interpretation, and other commonly overstated ideas are qualified explicitly.</p></section></div>;
}
